# v13 Tranche 6 — Receipts + Launch (Context Explorer, Claims 14–19, Before/After Demo)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - T1–T5 landed: intent on all manifests; `tokens.catalog.json` + `context.json` +
>   `context/<name>.md` + `specs.json`/`boundaries.json`/`exceptions.json` generated;
>   `cascade audit --ai` works; MCP `get_tokens`/`get_context`/`select_component`; CSS-native
>   pilot shipped with fallback gate.
> - `apps/docs` is the Preact docs app (signals; per-component pages; the "Why cascade" page
>   exists from prior versions). React-app rule: `useSignals()` where signals are read.
> - README + `llms.txt` generators exist (`scripts/readme`, `scripts/llms`).

**Goal:** Make the context layer visible and prove it works: a Context Explorer page that
browses intent/boundaries/specs/catalog and runs the `audit --ai` demo client-side; the "Why
cascade" page gains claims 14–19 each with a reproducible receipt; a documented before/after
agent-generation comparison; README/llms refreshed; the DoD walked end to end.

**Architecture:** A new docs page (`ContextExplorerPage.tsx`) reading the generated JSON
artifacts (`context.json`, `tokens.catalog.json`, `boundaries.json`) and running the four
`audit --ai` checkers client-side against pasted code (the checkers from T3 must be importable
as pure functions — verify/extract if needed). The "Why" page and README link the receipts.
The before/after demo is a recorded artifact + a documented method (not a CI model call).

**Tech stack:** unchanged. Docs app: Preact + signals + house rules.

---

### Task 1: Make the audit checkers browser-importable

**Files:**

- Verify/refactor: `packages/cli/src/audit-ai/*` (T3) export pure functions with no Node-only
  deps in the checker core
- Create (if needed): a thin `@cascade-ui` entry the docs app can import the checkers from

- [ ] **Step 1:** Confirm `css-literals`, `jsx-props`, `required-props`, `raw-strings` take
      `(source, contract)` and return findings with no `fs`/`process` usage in the core. If a
      checker reads files, split the pure analyzer from the file IO.
- [ ] **Step 2:** Expose the analyzers + `loadContract` (fed a fetched JSON, not the CLI
      fetch) for the docs app. Test the import path resolves in the docs build.
- [ ] **Step 3: Commit** — `refactor(cli): pure, importable audit-ai analyzers`

---

### Task 2: Context Explorer page

**Files:**

- Create: `apps/docs/src/pages/ContextExplorerPage.tsx` (+ route + nav entry)

- [ ] **Step 1:** Load `context.json`, `tokens.catalog.json`, `boundaries.json` (signals;
      `useSignals()` per app rule). Sections:
  - **Intent browser:** pick a component → show its when-to-use/not, anti-patterns, related
    edges (clickable to navigate), a11y rationale, content tone.
  - **Boundaries map:** global strict/flexible + the selected component's flexibility.
  - **Specs:** render the `specs.json` index with links.
  - **Token catalog:** filterable table (group/layer) from `tokens.catalog.json`.
- [ ] **Step 2:** Note the bundle size (master-plan risk #12) somewhere honest on the page.
- [ ] **Step 3: Commit** — `feat(docs): Context Explorer page (intent/boundaries/specs/catalog)`

---

### Task 3: Interactive `audit --ai` demo

**Files:**

- Modify: `apps/docs/src/pages/ContextExplorerPage.tsx` (add the demo panel)

- [ ] **Step 1:** A paste-code textarea (prefilled with a deliberately-dirty snippet:
      hardcoded color, invented prop, missing required prop, raw chrome string). On input
      (signal), run the imported analyzers against the loaded contract and render the findings
      table live — the same output `cascade audit --ai` produces in the terminal.
- [ ] **Step 2:** A "Fix" toggle showing the safe literal→token rewrite result.
- [ ] **Step 3:** Verify against the same expectations as the T3 `dirty/` fixture (same code →
      same findings) so the demo and the CLI agree.
- [ ] **Step 4: Commit** — `feat(docs): client-side audit --ai demo`

---

### Task 4: Before/after agent-generation demo (receipt for claim 19)

**Files:**

- Create: `docs/specs/context-impact-demo.md` (method + recorded results)

- [ ] **Step 1:** Define a reproducible method: pick 3 prompts that historically trip agents
      (e.g. "a transient success message", "a destructive confirm", "a themed primary
      button"). Run each twice — (A) giving the agent only the component manifest/props; (B)
      giving the full context bundle (`get_context` + `select_component` + catalog). Record
      the generated code for both.
- [ ] **Step 2:** Score each output with `cascade audit --ai` (objective, reproducible):
      count hardcoded-value, unknown-prop, missing-prop, raw-string findings + whether the
      _correct_ component was selected. Tabulate A vs B.
- [ ] **Step 3:** Write up the method + results honestly (state it's a documented run, not a
      CI gate; state model/version used). The receipt is the audit-score delta, not a vibe.
- [ ] **Step 4: Commit** — `docs: before/after context-impact demo (audit-scored)`

---

### Task 5: "Why cascade" claims 14–19 + README/llms refresh

**Files:**

- Modify: the "Why cascade" docs page
- Modify: README source / `scripts/readme` inputs; run `readme:generate`
- Run: `llms:generate`

- [ ] **Step 1:** Add claims 14–19 (from `ROADMAP-V13.md`), each linking its receipt:
      14 → an intent example / Context Explorer; 15 → `tokens.catalog.json`; 16 → the audit
      demo + CLI; 17 → `context.json` + per-component `context.md`; 18 → the CSS-native pilot
      with the fallback-proof screenshot pair; 19 → the before/after demo.
- [ ] **Step 2:** Refresh README (mention the context layer, `audit --ai`, MCP context tools)
      and regenerate `llms.txt`. Drift gate.
- [ ] **Step 3: Commit** — `docs: Why-cascade claims 14–19 + README/llms refresh`

---

### Task 6: DoD walkthrough + roadmap close-out

**Files:**

- Modify: `docs/ROADMAP-V13.md` (status → shipped, check the DoD boxes that are met)

- [ ] **Step 1:** Walk every box in the `ROADMAP-V13.md` Definition of Done against reality;
      check the ones met, and for any not met, record the gap honestly (don't check a box you
      didn't verify — evidence before assertion).
- [ ] **Step 2:** Full CLAUDE.md gate end to end: `vp check` → build → `vp run -r check` →
      test → full regen (registry + catalog + specs + context + readme + llms) →
      `vp check --fix` → `git diff --exit-code`. All exit 0.
- [ ] **Step 3: Commit** — `chore(v13): roadmap close-out + final gate sweep`

---

## Tranche exit criteria

1. The Context Explorer page browses intent, boundaries, specs, and the token catalog from
   the generated artifacts, and runs a client-side `audit --ai` demo whose findings match the
   CLI on the same input.
2. The "Why cascade" page states claims 14–19, each linked to a reproducible receipt.
3. The before/after demo is documented with an audit-scored A-vs-B comparison and stated
   method/model.
4. README + `llms.txt` reflect the context layer; drift gate clean.
5. The `ROADMAP-V13.md` DoD is walked; met boxes checked with evidence, gaps recorded.
6. Root gate + drift check exit 0.
