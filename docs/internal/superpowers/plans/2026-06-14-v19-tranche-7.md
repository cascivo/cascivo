# v19 Tranche 7 — Test coverage expansion

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.
>
> Pin every audit finding with a test so it can't silently regress. Today the harness skips half the
> routes: mobile-overflow only covers `/` and `/guides`; axe skips `/guides`; nothing verifies in-page
> anchors resolve. Close those gaps.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - `test/mobile.spec.ts` covers `/` and `/guides` at 320/375/390/414. `/accessibility` and
>   `/performance` are NOT covered.
> - `test/a11y.spec.ts` covers `/`, `/accessibility`, `/performance` (light+dark) + modal; `/guides`
>   wiring was added in T5 — confirm and assert here.
> - No test verifies that in-page `#anchor` links have a matching `id` (finding #17). `#axe` is valid
>   and must pass such a check.
> - `playwright.config.ts` excludes `@og` via grep `^(?!.*@og)`.

**Goal:** Mobile-overflow covers all four content routes; axe covers all four (incl. `/guides`); a new
anchor/link-integrity spec asserts every in-page `#anchor` resolves to an `id`; (optional) a perf-budget
assertion guards the T6 win. All deterministic.

**Architecture:** `test/mobile.spec.ts`, `test/a11y.spec.ts`, new `test/links.spec.ts`,
`playwright.config.ts` (only if a new project/grep is needed). No package changes.

**Tech stack:** unchanged. Playwright + axe-core (already deps).

---

### Task 1: Mobile-overflow for all content routes

**Files:** `apps/landing/test/mobile.spec.ts`

- [ ] **Step 1:** Add `/accessibility` and `/performance` to the route list the overflow harness
      iterates at 320/375/390/414, mirroring how `/` and `/guides` are registered.
- [ ] **Step 2:** Run it; fix any overflow found in the owning page's CSS (commit the fix in this
      tranche, noting it). The two highest-risk spots are wide tables/charts on `/performance` and the
      axe-comparison bars on `/accessibility` — verify they scroll within their box, not the page.

### Task 2: axe for /guides (assert)

**Files:** `apps/landing/test/a11y.spec.ts`

- [ ] **Step 1:** Confirm `/guides` (wired in T5) is in the axe matrix (light + dark) and the run is
      green. If T5's wiring is missing, add it here.

### Task 3: Anchor / link-integrity spec

**Files:** Create `apps/landing/test/links.spec.ts`

- [ ] **Step 1:** For each public route, collect every in-page anchor (`a[href^="#"]`) and assert a
      matching `id` exists in the rendered DOM. This pins finding #17 and proves `#axe`, `#console`,
      `#signals`, `#quickstart`, `#agents`, `#migrate`, `#customize`, `#use-cases` all resolve.
- [ ] **Step 2:** (Optional, deterministic only) Assert internal nav `href`s map to known routes in
      `ROUTES`. Do NOT make network calls to external URLs (non-deterministic/flaky) — external links
      are out of this test's scope.

### Task 4: (Optional) perf budget

**Files:** a small build-size assertion (script or test)

- [ ] **Step 1:** If cheap and deterministic, assert the home entry chunk stays under a budget derived
      from the T6 result (e.g. fail if it grows >10% over the recorded post-split size). Skip if it
      would be flaky; note the decision.

### Task 5: Run all + commit

- [ ] **Step 1:** `pnpm exec playwright test apps/landing/test` — mobile (all four routes), a11y (all
      four, light+dark), links, smoke, motion all green.
- [ ] **Step 2: Commit** — `test(landing): mobile+axe all routes, anchor/link integrity (v19-t7)`

---

## Tranche exit criteria

1. Mobile-overflow passes for `/`, `/accessibility`, `/performance`, `/guides` at 320/375/390/414.
2. axe passes for all four content routes in light + dark.
3. `links.spec.ts` asserts every in-page `#anchor` has a matching `id` and passes (`#axe` included).
4. Any overflow uncovered is fixed in-tranche; the perf-budget decision is recorded (added or skipped).
5. The whole `apps/landing/test` suite is green and deterministic.
