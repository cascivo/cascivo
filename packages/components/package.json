{
  "name": "@cascade-ui/components",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'components: registry source — no npm build'",
    "test": "echo 'components: no tests yet (add in Plan 3)'"
  },
  "devDependencies": {
    "@cascade-ui/core": "workspace:*",
    "@cascade-ui/tokens": "workspace:*",
    "@types/react": "catalog:",
    "react": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  }
}
