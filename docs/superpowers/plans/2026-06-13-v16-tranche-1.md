# v16 Tranche 1 — Accuracy fixes

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.
>
> Start here. Fix every wrong or stale claim on the landing before adding new content. A lie
> corrected before the new receipts land is cleaner than a lie still on the page after they do.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - `Hero.tsx` in `apps/landing/src/sections/` has chips including `"WCAG 2.1 AA"` and a stats
>   badge reading something like `"{componentCount}+ components · 5 themes · MIT"`.
> - The copy command in `Hero.tsx` shows `npx cascivo add button` (unscoped invocation — deferred
>   per v15 decision 4; should be `npx @cascivo/cli add button` unless the unscoped package has
>   since been published).
> - No other stale accuracy issues are known, but a full sweep is required.

**Goal:** Every static claim on the landing is accurate. Chips, stat badges, install commands, and
prose copy match the current shipped state: WCAG 2.2 AA (not 2.1), 10 themes (not 5), correct CLI
invocation, an `RSC-compatible` chip added.

**Architecture:** `apps/landing/src/sections/Hero.tsx` + any other sections with stale copy.
No CSS changes, no new sections, no new files.

**Tech stack:** unchanged.

---

### Task 1: Audit all static copy on the landing

**Files:**

- Read: all files in `apps/landing/src/sections/` + `apps/landing/src/demo/`

- [ ] **Step 1:** Read every section file and record every static claim that could be stale:
      counts, version strings, command strings, chip labels, stat descriptions.
- [ ] **Step 2:** Cross-reference against ground truth:
  - WCAG version: `2.2 AA` (v14)
  - Themes: 10 (light, dark, warm, brutalist, corporate, flat, midnight, minimal, pastel, terminal)
  - Component count: verify against `registry.json` (`jq '.components | length' registry.json`)
  - CLI command: `npx @cascivo/cli add button` (verify unscoped `cascivo` package is not yet published
    before changing — run `npm view cascivo 2>&1`; if 404, use scoped form)
  - WCAG in axe stat description in `StatsBand.tsx`: should say 2.2 AA if mentioned
- [ ] **Step 3:** Create a findings list: each stale claim, its current value, its correct value,
      and which file + line it's in. Use this list to drive Task 2.

---

### Task 2: Fix Hero.tsx chips + stats badge + copy command

**Files:**

- Modify: `apps/landing/src/sections/Hero.tsx`

- [ ] **Step 1:** Fix the WCAG chip: `"WCAG 2.1 AA"` → `"WCAG 2.2 AA"`.
- [ ] **Step 2:** Fix the theme count in the stats badge: `"5 themes"` → `"10 themes"` (or confirm
      the badge is dynamic and update the data source if so).
- [ ] **Step 3:** Add `"RSC-compatible"` as a new chip alongside the existing chips.
- [ ] **Step 4:** Fix the CLI copy command if wrong: `npx cascivo add button` →
      `npx @cascivo/cli add button` (only if the unscoped package is not yet published per Step 2
      of Task 1). Update the `CopyCommand` component invocation and any surrounding prose.
- [ ] **Step 5:** If `Hero.tsx` uses a `GitHub` link pointing at `urbanisierung/cascade-ui` (old
      repo name), update to `urbanisierung/cascivo`.

---

### Task 3: Fix any remaining stale copy found in Task 1

**Files:**

- Modify: whatever sections Task 1 found with stale claims

- [ ] **Step 1:** Apply every fix from the Task 1 findings list not already covered by Task 2.
      Common candidates: `StatsBand.tsx` WCAG version in stat descriptions, `Footer.tsx` repo link,
      `ProofTeasers.tsx` WCAG version mention in the a11y card copy.
- [ ] **Step 2:** Run a grep to confirm no "2.1 AA", "WCAG 2.1", or "5 themes" remain in
      `apps/landing/src/` (excluding comments that are deliberately historical):
      `sh
grep -rn "2\.1 AA\|WCAG 2\.1\|5 themes" apps/landing/src/
`
      Should return 0 results (or only inline comments that explain the old value).
- [ ] **Step 3: Commit** — `fix(landing): accuracy fixes — WCAG 2.2 AA, 10 themes, correct CLI command (v16-t1)`

---

### Task 4: Gate check

- [ ] **Step 1:** `pnpm exec vp check` — 0 errors, 0 new warnings beyond pre-existing baseline.
- [ ] **Step 2:** `pnpm build` — build passes.
- [ ] **Step 3:** Visually verify the landing at `localhost` — hero chips show "WCAG 2.2 AA" and
      "RSC-compatible"; stats badge shows "10 themes"; copy command shows the correct invocation.

---

## Tranche exit criteria

1. Every static chip, stat badge, and CLI command on the landing page reflects current reality.
2. "WCAG 2.2 AA" chip present; "WCAG 2.1 AA" gone.
3. "10 themes" in stats badge; "5 themes" gone.
4. "RSC-compatible" chip present.
5. CLI copy command is the correct, invocable form.
6. Grep confirms zero stale WCAG/theme count claims in `apps/landing/src/`.
