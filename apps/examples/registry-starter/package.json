{
  "name": "@cascivo/registry-starter",
  "version": "0.0.0",
  "private": true,
  "description": "Starter template for publishing a third-party cascivo component registry",
  "type": "module",
  "scripts": {
    "build": "cascivo registry build --in cascivo-registry.json --out public/r"
  },
  "devDependencies": {
    "cascivo": "workspace:^"
  }
}
