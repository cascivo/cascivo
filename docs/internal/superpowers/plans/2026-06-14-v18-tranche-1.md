# v18 Tranche 1 — Parity audit + backlog queue

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax. This tranche produces the
> _scoreboard_ and the _work queue_ — the foundation everything else in v18 reads from. No component or
> primitive code yet.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - `registry.json` has **118** components (`jq '.components | length' registry.json`).
> - `factory-backlog.json` is `{ queue: [...] }`, 65 entries — 21 done, 28 review, 16 pending
>   (`jq -r '.queue[] | .status' factory-backlog.json | sort | uniq -c`). Pending entries include
>   `icon-button`, `code-snippet`, `notification`, `structured-list`, `tile`, `scroll-area`, `calendar`,
>   `date-range-picker`, `tree-view`, `color-picker`, `carousel`, `timeline`, `data-list`, `collapsible`,
>   `code-block`, `splitter`. RE-READ for exact names/specs — some may differ.
> - shadcn/ui registry = **58** components (master plan lists them, fetched 2026-06-14). RE-VERIFY at
>   `ui.shadcn.com/docs/components`.
> - Carbon roster — the master-plan list came from knowledge; the live page fetch truncated. RE-VERIFY at
>   `carbondesignsystem.com/components/overview/components` before finalizing the matrix.
> - Generation pattern to mirror: `scripts/context/generate.ts` + `scripts/catalog/generate.ts`, both
>   drift-gated. Check how `pnpm regen` is wired in the root `package.json`.

**Goal:** A formal, honest parity audit of cascivo against shadcn/ui + Carbon, expressed as a
machine-readable matrix that generates `parity.json`, plus a fully-specced, prioritized `factory-backlog.json`
queue covering every gap. This is the "list them all" deliverable from the request.

**Architecture:** New `docs/specs/parity-matrix.md` (human source) → new `scripts/parity/generate.ts` →
`apps/docs/public/parity.json` (generated, drift-gated). `factory-backlog.json` edited to add/upgrade
entries. `package.json` regen script extended.

**Tech stack:** unchanged. The generator is a Node/TS script consistent with `scripts/context` and
`scripts/catalog`.

---

### Task 1: Verify the competitor rosters

**Files:** Read: `registry.json`, `packages/react/src/index.ts`, `factory-backlog.json`.

- [ ] **Step 1:** Re-fetch the shadcn/ui component list from `ui.shadcn.com/docs/components`; confirm the
      58 names in the master plan. Note any added/removed since 2026-06-14.
- [ ] **Step 2:** Re-fetch the Carbon roster from `carbondesignsystem.com/components/overview/components`.
      Reconcile against the master-plan list; correct any names. Carbon groups some (e.g. Notification =
      inline/toast/actionable; UI shell = header/side-nav). Record the canonical names.
- [ ] **Step 3:** Build the cascivo inventory from `registry.json` (the 118) + `@cascivo/react` exports.
      This is the "covered" source of truth — a competitor entry is only "covered" if a real cascivo
      component maps to it.

---

### Task 2: Author the parity matrix

**Files:** Create: `docs/specs/parity-matrix.md`.

- [ ] **Step 1:** Write a markdown file with two tables (shadcn/ui, Carbon). Each row:
      `competitor component | status | cascivo component | note`. Status ∈
      `covered | partial | gap | by-convention`. Use the mapping in the master plan as the starting point;
      correct against Task 1.
- [ ] **Step 2:** Be honest about partials and conventions: - `Drawer` → `partial` (cascivo `sheet`; no mobile gesture) until T6 ships `Drawer`. - `Direction` → `by-convention` (CSS logical properties; no JS provider). - `Native Select` → `gap` (cascivo `select` is a custom listbox, not the native control). - Carbon `AI label/slug`, `Fluid` → `deferred` (out of v18 scope; mark explicitly).
- [ ] **Step 3:** Add a short header explaining the methodology (what "covered" means; that the JSON is
      generated; that gaps map to `factory-backlog.json` entries). Add a summary line: counts of
      covered/partial/gap per competitor.
- [ ] **Step 4:** Define a stable, parseable structure (e.g. a fenced table or front-matter the generator
      can read). Keep it simple — the generator parses this file, so its format is a contract.

---

### Task 3: Build the parity generator

**Files:** Create: `scripts/parity/generate.ts`. Modify: root `package.json` (regen script).

- [ ] **Step 1:** Write `generate.ts` to parse `docs/specs/parity-matrix.md`, cross-check each "covered"
      cascivo mapping against `registry.json` + `packages/react/src/index.ts` (fail loudly if a matrix row
      claims a cascivo component that doesn't exist), and emit `apps/docs/public/parity.json`:
      `{ generatedAt, competitors: { shadcn: {total, covered, partial, gap, rows[]}, carbon: {...} } }`.
- [ ] **Step 2:** Make coverage _derived_: the JSON's `covered` count is computed from rows whose mapped
      cascivo component is present in the exports, not from a typed number. A row marked `covered` whose
      component isn't exported yet must downgrade to `gap` in the JSON (so the page can't overstate).
- [ ] **Step 3:** Wire `scripts/parity/generate.ts` into `pnpm regen` (mirror how `context`/`catalog`
      generators are invoked). Run it; confirm `parity.json` is produced.
- [ ] **Step 4:** Add a drift assertion: regen → `git diff --exit-code` must be clean. Confirm editing the
      matrix and regenerating updates the JSON (and that hand-editing the JSON then regenerating reverts it).

---

### Task 4: Queue every gap into the backlog

**Files:** Modify: `factory-backlog.json`.

- [ ] **Step 1:** For each `gap`/`partial` row in the matrix, ensure a `factory-backlog.json` queue entry
      exists. Reconcile first: upgrade the existing `pending` entries (`icon-button`, `notification`,
      `structured-list`, `tile`, `scroll-area`, `calendar`, `date-range-picker`, `tree-view`,
      `color-picker`, `carousel`, `timeline`, `data-list`, `collapsible`; rename/clarify `code-block`→
      `code-snippet`, `splitter`→`resizable` or note the relationship) with a fuller spec. Do NOT create
      duplicates.
- [ ] **Step 2:** ADD genuinely-new entries: `label`, `field`, `button-group`, `toggle-group`,
      `inline-loading`, `navigation-menu`, `menubar`, `menu-button`, `toggletip`, `drawer`,
      `native-select`, `item`, `contained-list`. Each entry: `{ name, category, priority, spec, status:
"pending", milestone, attempts: 0 }`. Spec must name: purpose, variants/sizes, states, a11y
      role + keyboard, and the `@cascivo/core` primitive(s) it depends on (from the master-plan tables).
- [ ] **Step 3:** Set `priority` so Tier-1 (T5) entries sort before Tier-2 (T6), and within Tier-1,
      `label`/`field` (a11y substrate) come first. Record the tier in the spec or a field so T5/T6 can
      select the right slice.
- [ ] **Step 4:** Validate the JSON (`jq . factory-backlog.json > /dev/null`) and confirm counts:
      `jq -r '.queue[] | .status' factory-backlog.json | sort | uniq -c`.
- [ ] **Step 5: Commit** — `chore(parity): parity matrix + generator + backlog queue (v18-t1)`

---

## Tranche exit criteria

1. `docs/specs/parity-matrix.md` exists, mapping cascivo against shadcn/ui (58) and Carbon honestly —
   every competitor entry marked covered/partial/gap/by-convention with a cascivo component or note.
2. `scripts/parity/generate.ts` emits `apps/docs/public/parity.json` with _derived_ coverage; wired into
   `pnpm regen`; `git diff --exit-code` clean after regen.
3. The generator fails if a matrix row claims a non-existent cascivo component (verified by a deliberate
   bad row in a scratch test, then reverted).
4. `factory-backlog.json` has a real, prioritized, primitive-aware spec for every gap; pre-existing
   `pending` entries reconciled (no duplicates); new entries added; JSON valid.
5. No component or primitive source written yet — this tranche is audit + queue only.
   </content>
