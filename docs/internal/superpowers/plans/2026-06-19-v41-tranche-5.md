# v41 — Tranche 5: HeroUI Study Doc + Final Gate

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land the documentation for everything v41 added, run the full CI gate + drift, and prove the new
components reached every registration surface. Optionally emit a vendor-neutral `AGENTS.md` as the one
remaining HeroUI AI-parity item (cascivo's other AI surfaces already lead). This tranche writes **no new
runtime code** — it documents, regenerates, and verifies.

**Architecture:** Update the roadmap status, fold the interaction-state tokens into `THEME-PROPOSALS.md`,
document the new components and core hooks, then run the exact CI steps from `CLAUDE.md` (`pnpm ready` /
`pnpm ready:ci`) plus the drift and grep sweeps.

**Tech Stack:** Markdown docs; `pnpm regen`; vite+ (`vp`) full gate; `git diff --exit-code` drift; grep sweep.

---

## Files

- Modify: `docs/ROADMAP-V41.md` (status → done as tranches land; check the DoD boxes)
- Modify: `docs/THEME-PROPOSALS.md` (document `--cascivo-disabled-opacity` / `--cascivo-hover-opacity`)
- Modify: component docs (the `image`/`user`/`avatar-group` pages if docs are authored) + `packages/core/README.md` (hooks)
- Create (optional stretch): `AGENTS.md` (vendor-neutral agent guide — HeroUI parity) **or** explicitly defer
- Verify: `pnpm regen`, full gate, drift, grep sweep

---

### Task 1: Document the study + new surfaces

**Files:** `docs/ROADMAP-V41.md`, `docs/THEME-PROPOSALS.md`, `packages/core/README.md`

- [ ] **Step 1:** Flip `docs/ROADMAP-V41.md` **Status** to in-progress/shipped as tranches land and tick the
      Definition-of-Done checkboxes for T1–T4.
- [ ] **Step 2:** In `docs/THEME-PROPOSALS.md`, add a short section recording the two new **interaction-state
      tokens** (`--cascivo-disabled-opacity`, `--cascivo-hover-opacity`), their defaults, that they live in all 12
      themes (parity), and the HeroUI layout-token motivation — noting cascivo's `radius` one-knob already covered
      the rest.
- [ ] **Step 3:** Confirm `packages/core/README.md` documents `useDisclosure`/`useInfiniteScroll`/`useDraggable`
      (added in T3); confirm `image`/`user`/`avatar-group` carry complete manifests (so auto-docs/MCP/Storybook
      render them — no separate hand-written page required unless the docs app uses one).

### Task 2: Optional AI-parity stretch — `AGENTS.md`

**Files:** `AGENTS.md` (repo root)

- [ ] **Step 1:** Decide: cascivo already exceeds HeroUI's AI layer (manifests, `@cascivo/mcp`, `skills/`,
      `llms.txt` in docs + landing, an existing `doctor` CLI). The only parity item is a **vendor-neutral
      `AGENTS.md`** (read by Cursor/Copilot/Windsurf, complementing the Claude-specific `CLAUDE.md`).
- [ ] **Step 2:** If adopting: create a concise `AGENTS.md` that points agents at the registry, the MCP server,
      the skills, and `llms.txt` — derived from existing sources, **no** duplication of `CLAUDE.md`'s rules
      verbatim. If deferring: note the decision in `docs/ROADMAP-V41.md` and skip — it must not block the gate.

### Task 3: Regenerate + drift

- [ ] **Step 1:** `pnpm regen` — confirm `registry.json` contains `image`, `user`, `avatar-group` with correct
      category/files/meta, and any generated component listings include them.
- [ ] **Step 2:** Drift gate: `pnpm regen && pnpm exec vp check --fix && git diff --exit-code` — clean (commit any
      regenerated/formatted files).

### Task 4: Grep sweep (registration coverage)

- [ ] **Step 1:** `grep -rl "image\b" packages/react/src/index.ts packages/components/src/_all-metas.ts registry.json`
      (and likewise `user`, `avatar-group`) — confirm each new component appears in **all three** surfaces.
- [ ] **Step 2:** Confirm the three new hooks are exported from `packages/core/src/index.ts`.

### Task 5: Full CI gate

- [ ] **Step 1:** Run the single command that covers everything: `pnpm ready`
      (`regen` → `vp check --fix` → build → type check → tests). All green.
- [ ] **Step 2:** Run the cold-cache CI simulation if build config/deps changed: `pnpm ready:ci`.
- [ ] **Step 3:** Run the remaining gates explicitly: `pnpm breakpoint:check`, `pnpm fallback:check`,
      `pnpm brand:check` — all exit 0.

### Task 6: Commit T5 + final roadmap status

- [ ] **Step 1: Stage and commit**

  ```bash
  git add docs/ROADMAP-V41.md docs/THEME-PROPOSALS.md packages/core/README.md AGENTS.md registry.json
  git commit -m "$(cat <<'EOF'
  docs(v41): record HeroUI study; document new components, hooks, tokens (v41 T5)

  Final tranche: roadmap status + DoD ticked; interaction-state opacity
  tokens documented in THEME-PROPOSALS; core hooks documented; optional
  vendor-neutral AGENTS.md for HeroUI AI parity (cascivo's manifests/MCP/
  skills/llms.txt already lead). pnpm regen + drift clean; full gate
  (vp check, build, type check, tests, breakpoint/fallback/brand) green;
  grep sweep confirms image/user/avatar-group reached every registration
  surface.

  Co-Authored-By: Claude <noreply@anthropic.com>
  EOF
  )"
  ```
</content>
