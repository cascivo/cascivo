# v11 Tranche 5 — Gap Components (Factory Specs), Virtualized Table, Standard Schema Forms

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - `factory-backlog.json`: 61 items (`{ name, category, priority, spec, status,
milestone, attempts }`), 28 in `review` — check for name collisions before queueing
>   anything new (master-plan risk #12).
> - `date-picker`/`time-picker` are native-input wrappers (no calendar grid); no
>   `calendar`, `date-range-picker`, `tree-view`, or `color-picker` exist anywhere
>   (including in-flight `review` items — verify).
> - `data-table` (`packages/components/src/data-table/`): signal-driven sort/filter/
>   pagination/selection/expandable rows, `content-visibility` for large sets — no
>   windowing.
> - `form` (`packages/components/src/form/`): `createForm`/`useForm`, signal-based fields,
>   sync/async validators as plain functions; no schema-library integration; built-in
>   messages — check whether any exist and where (hardcoded vs i18n).
> - `@cascivo/i18n` builtin catalog pattern: `t(builtin.<component>.<key>)`.

**Goal:** The "free what others paywall" claim gains its missing pieces: real calendar +
date-range picking, tree view, and color picker enter the factory pipeline with
v11-milestone specs; data-table handles 100k+ rows via an opt-in windowed mode; and
`createForm` accepts zod/valibot/arktype validators through one Standard Schema adapter
with i18n'd built-in messages.

**Architecture:** New components are **specs in `factory-backlog.json`**, built by the dark
factory under its existing generate → verify → PR loop (this tranche writes the specs and
runs the factory; it does not hand-build components). Data-table windowing and the form
adapter are surgical enhancements to existing components, done directly here.

**Tech stack:** unchanged. `zod`, `valibot`, `arktype` enter as **devDependencies of
`packages/components` only** (test-time proof of the adapter; the runtime depends only on
the Standard Schema interface shape).

---

### Task 1: Factory specs for the gap components

**Files:**

- Modify: `factory-backlog.json`

- [ ] **Step 1:** Verify none of the four names collide with existing/in-review items.
- [ ] **Step 2:** Append four entries, `milestone: "v11"`, priority ordered as listed,
      each `spec` written to the factory's working level of detail (study 2–3 existing
      done specs first and match their depth). Required content per spec:
  - **`calendar`** (`inputs`): month-grid date selection; ARIA APG grid pattern (role
    grid/row/gridcell, roving tabindex, arrow/Home/End/PageUp/PageDown keys); `min`/`max`/
    `disabled` dates; localized weekday/month names via `Intl` through `@cascivo/i18n`
    (first-day-of-week per locale); controlled + uncontrolled value; FSM only if the
    component drives transitions (per CLAUDE.md rules); tokens for cell states; sizes.
  - **`date-range-picker`** (`inputs`): composes `calendar` (registryDependency!) in a
    popover (reuse T4's platform-native popover); start/end selection with hover preview;
    presets slot (today/last-7/last-30); keyboard completable; emits `{ start, end }`;
    text-input fallbacks remain accessible.
  - **`tree-view`** (`display`): ARIA APG TreeView pattern; expand/collapse via signals;
    lazy children (async loader prop); selection (single/multi); keyboard per APG;
    `content-visibility` for large trees; icons optional via `@cascivo/icons`.
  - **`color-picker`** (`inputs`): oklch-first (house color space) area + hue/alpha
    sliders + swatches + text input with format toggle (oklch/hex/rgb); wraps native
    `<input type="color">` as fallback/eyedropper where available; a11y per APG slider
    pattern; emits canonical oklch string.
- [ ] **Step 3:** `vp check` (json formatting) and commit —
      `feat(factory): queue calendar, date-range-picker, tree-view, color-picker (v11)`

---

### Task 2: Run the factory on the v11 queue

- [ ] **Step 1:** Run the factory skill (`/factory`) for the four queued items, in priority
      order — `calendar` must merge (or at least pass verification) before
      `date-range-picker` starts, since the latter depends on it.
- [ ] **Step 2:** Factory outcomes follow the standing pipeline (self-heal ≤5, then
      escalate). For each component that lands: confirm registry regen picked it up,
      `packages/react` export exists, docs/Storybook render.
- [ ] **Step 3:** If an item exhausts attempts, leave it `pending` with `attempts`
      recorded and note it in the tranche summary — do NOT hand-write the component in
      this tranche (scope control).

---

### Task 3: Data-table windowed mode

**Files:**

- Modify: `packages/components/src/data-table/*` (tsx, css, meta, tests)

- [ ] **Step 1:** Re-read the component; design the opt-in: `virtualized?: boolean` (or
      `rowHeight`-driven — decide against the existing API style). Windowing via signals:
      a scroll-position signal (`useSignalEffect` listener on the scroll container) →
      computed visible slice + top/bottom spacer rows. No external virtualization lib.
- [ ] **Step 2:** A11y (master-plan risk #8): `aria-rowcount` on the table reflects the
      full set; rendered rows carry `aria-rowindex`; selection state survives scroll
      (selection keyed by row id, already signal-held — verify); keyboard row navigation
      still works across window boundaries.
- [ ] **Step 3:** Default path untouched: `content-visibility` behavior remains for
      non-virtualized tables; zero changes to existing tests.
- [ ] **Step 4:** Tests: 100k-row fixture renders ≤ (window + overscan) rows; scroll
      updates the slice; `aria-rowcount`/`aria-rowindex` correct; selection across scroll.
      Perf sanity: docs `PerfDataTable` page gains a virtualized variant (check
      `apps/docs/src/pages/PerfDataTable.tsx` and extend).
- [ ] **Step 5:** Manifest + regen. **Commit** —
      `feat(data-table): opt-in windowed rendering for 100k+ rows`

---

### Task 4: Standard Schema adapter for forms

**Files:**

- Modify: `packages/components/src/form/*` (createForm/useForm + tests)
- Modify: `packages/components/package.json` (dev deps: zod, valibot, arktype)

- [ ] **Step 1:** Vendor the Standard Schema V1 interface type (it is a tiny TS interface —
      `~standard: { version: 1, vendor, validate(value) }`; copying the published
      interface avoids a runtime dep — confirm the canonical shape from
      standardschema.dev at implementation).
- [ ] **Step 2:** Extend field/form config: `schema?: StandardSchemaV1` alongside existing
      function validators (both allowed; schema runs first, both contribute issues).
      Async `validate` results supported (the interface allows Promise). Map issues →
      existing field-error signal shape (path-aware for form-level schemas).
- [ ] **Step 3:** Built-in messages to i18n: any hardcoded default strings found in Step
      0's re-verify (e.g. required/invalid) move to the builtin catalog
      (`builtin.form.*`), overridable per instance — per house rule "never hardcoded
      English fallbacks".
- [ ] **Step 4:** Tests: one identical form validated via zod, valibot, AND arktype —
      same assertions pass for all three; function-validator regression tests untouched;
      async schema test; i18n message override test.
- [ ] **Step 5:** Manifest + regen. **Commit** —
      `feat(form): Standard Schema validation (zod/valibot/arktype) + i18n messages`

---

### Task 5: Tranche gate

- [ ] Full CLAUDE.md gate; regen includes any factory-landed components; drift check;
      docs + Storybook build.

---

## Tranche exit criteria

1. Four v11 specs queued; factory run executed; landed components verified in registry/
   react-package/docs (any non-landing item documented honestly with attempts).
2. `data-table` renders 100k rows smoothly behind the opt-in, with correct
   `aria-rowcount`/`aria-rowindex` and intact selection; default path byte-identical.
3. One form, three schema libraries, one adapter — test-proven; built-in messages come
   from the i18n catalog.
4. Root gate + drift check exit 0.
