# v38 — Tranche 1: Cyberpunk Theme — Palette & Structure

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Author the new `cyberpunk` theme CSS — a dark, neon (magenta + electric cyan) palette on
neo-brutalist structure (radius 0, thick borders, hard glow-tinted shadows) — so it satisfies the two hard
theme gates (token parity + chart-CVD) and is exported from `@cascivo/themes`. **No animations yet** (T2)
and **no downstream registration yet** (T3/T4) — this tranche is the foundation file plus its tests.

**Architecture:** A theme is `@import '@cascivo/tokens';` then
`@layer cascivo.theme { [data-theme='cyberpunk'] { …--cascivo-* overrides… } }`. `parity.test.ts` requires
the **identical** `--cascivo-*` key set as every other theme, so the file is authored by copying a **dark**
theme's token skeleton (`terminal.css` or `midnight.css`) and re-coloring — never by writing tokens from
scratch (guarantees no missing/extra keys). `chart-palette.test.ts` simulates color-vision deficiency, so
the chart ramp is borrowed from the dark skeleton and only lightly adjusted.

**Tech Stack:** CSS-only; vite+ (`vp`) for the theme test.

---

## Files

- Create: `packages/themes/src/cyberpunk.css`
- Modify: `packages/themes/src/parity.test.ts` (add `cyberpunk` to the theme array)
- Modify: `packages/themes/src/chart-palette.test.ts` (add `cyberpunk`)
- Modify: `packages/themes/package.json` (`exports`, `description`, `keywords`)
- Modify: `packages/themes/src/all.css` (`@import` the theme)
- Modify: `packages/themes/README.md` (list the theme)

---

### Task 1: Scaffold `cyberpunk.css` from a dark theme skeleton

**Files:** `packages/themes/src/cyberpunk.css`

- [ ] **Step 1:** Copy `packages/themes/src/terminal.css` (a dark, radius-0 theme) to
      `packages/themes/src/cyberpunk.css`. This guarantees the exact `--cascivo-*` key set parity requires.
- [ ] **Step 2:** Rename the selector to `[data-theme='cyberpunk']`, keep `color-scheme: dark`, and update the
      header comment to `/* cascivo — Cyberpunk Theme */` + a one-line description
      (`/* Brutalism × cyberpunk: near-black base, neon magenta/cyan, hard glow shadows */`).
- [ ] **Step 3:** Do **not** add or remove any `--cascivo-*` keys in this step — only re-value them in the
      following tasks. (Adding/removing keys breaks `parity.test.ts`.)

### Task 2: Surface, border & text — dark brutalist base

**Files:** `packages/themes/src/cyberpunk.css`

- [ ] **Step 1:** Set a near-black, slightly blue-violet base: `--cascivo-color-background` ≈
      `oklch(0.16 0.03 285)`; `--cascivo-color-surface` a hair lighter; `--cascivo-color-surface-2`/
      `-raised`/`-overlay` stepped lighter for layering. Keep `-bg`/`-bg-subtle` aliases mapped as in the
      skeleton.
- [ ] **Step 2:** Brutalist borders — high-contrast neon-leaning edges: `--cascivo-color-border` ≈
      `oklch(0.6 0.18 320)` (dim magenta) with `--cascivo-color-border-strong` brighter; map
      `--cascivo-border-{subtle,default,strong}` consistently. (Thick **width** comes from component CSS via
      the ring/shadow tokens, not here.)
- [ ] **Step 3:** Text — high luminance on the dark base: `--cascivo-color-foreground` ≈ `oklch(0.96 0.02 300)`;
      `-foreground-muted`/`-text-muted`/`-text-subtle` stepped down but ≥4.5:1 on the base.
      `--cascivo-color-text-on-accent` chosen for contrast against the neon accent (likely near-black).

### Task 3: Accent & primary — neon magenta + cyan

**Files:** `packages/themes/src/cyberpunk.css`

- [ ] **Step 1:** Accent = neon magenta: `--cascivo-color-accent` ≈ `oklch(0.7 0.25 330)`; `-hover`/`-active`
      slightly brighter/dimmer; `-subtle`/`-muted` low-chroma tints of it. Set
      `--cascivo-color-accent-foreground` to a near-black for legible text on the magenta.
- [ ] **Step 2:** Map `--cascivo-color-primary*` to the accent (as `brutalist.css` does), with
      `--cascivo-color-primary-fg` near-black and `--cascivo-color-active-bg` a translucent accent tint.
- [ ] **Step 3:** Use **electric cyan** as the secondary role: `--cascivo-color-secondary` ≈
      `oklch(0.78 0.16 200)` with matching `-content`/`-hover`/`-subtle`. (Magenta + cyan is the cyberpunk
      signature; the secondary role is where cyan lives without disturbing the primary accent.)

### Task 4: Status colors, radius, shadows & focus ring (brutalist structure)

**Files:** `packages/themes/src/cyberpunk.css`

- [ ] **Step 1:** Status colors tuned for the dark base (`error`/`destructive` neon red ≈ `oklch(0.62 0.25 20)`,
      `warning` amber, `success` neon green, `info` cyan), each with `-subtle`/`-foreground`/`-content`
      counterparts present (copy the skeleton's key list; re-value).
- [ ] **Step 2:** Zero radius across all `--cascivo-radius-*` tokens (mirror `brutalist.css`).
- [ ] **Step 3:** Hard offset shadows **tinted with neon glow** (not pure black): e.g.
      `--cascivo-shadow-md: 4px 4px 0 oklch(0.7 0.25 330)` and a complementary cyan tint where appropriate;
      `-xs`/`-sm`/`-overlay`/`-lg` follow the brutalist offset progression. (Optional subtle blur for "glow"
      is acceptable but keep the hard-offset brutalist read dominant.)
- [ ] **Step 4:** Fat brutalist focus ring in neon cyan: `--cascivo-ring-width: 3px`, `--cascivo-ring-offset: 2px`,
      `--cascivo-ring-color`/`--cascivo-color-focus-ring` ≈ `oklch(0.78 0.16 200)`; set `--cascivo-focus-ring`
      accordingly.

### Task 5: Chart ramp — keep CVD-safe

**Files:** `packages/themes/src/cyberpunk.css`

- [ ] **Step 1:** Keep the dark skeleton's `--cascivo-chart-1..8` ramp as the starting point (it already passes
      CVD). Adjust only lightness/chroma for the dark base — **do not** replace all eight with neon hues.
- [ ] **Step 2:** Set `--cascivo-chart-grid`/`--cascivo-chart-axis` to low-contrast neon-neutral values readable
      on the dark base.

### Task 6: Register exports + parity/chart test lists

**Files:** `packages/themes/package.json`, `packages/themes/src/all.css`, `packages/themes/src/parity.test.ts`, `packages/themes/src/chart-palette.test.ts`, `packages/themes/README.md`

- [ ] **Step 1:** Add `"./cyberpunk": "./src/cyberpunk.css"` and `"./cyberpunk.css": "./src/cyberpunk.css"` to
      `packages/themes/package.json` `exports`; append `cyberpunk` to `keywords` description list and update the
      `description` string's enumerated theme list.
- [ ] **Step 2:** Add `@import '@cascivo/themes/cyberpunk.css';` (or the relative `./cyberpunk.css`, matching the
      existing pattern) to `packages/themes/src/all.css`.
- [ ] **Step 3:** Add `'cyberpunk'` to the `themeFiles` array in `parity.test.ts` and the theme list in
      `chart-palette.test.ts`.
- [ ] **Step 4:** Add `cyberpunk` to the README's theme list.

### Task 7: Verify the gates

- [ ] **Step 1:** `pnpm exec vp run @cascivo/themes#test` — `parity.test.ts` passes (identical token set) and
      `chart-palette.test.ts` passes (CVD-distinguishable). If parity fails, diff the new file's
      `--cascivo-*` keys against `terminal.css` and reconcile (a re-color must not add/drop keys).
- [ ] **Step 2:** `pnpm exec vp check` (format + lint) on the new CSS.
- [ ] **Step 3:** Spot-check contrast: foreground/foreground-muted on background, and accent-foreground on
      accent, should read ≥4.5:1 (eyeball or a quick contrast check) — this theme is dark with neon accents,
      easy to under-contrast.

### Task 8: Commit T1

- [ ] **Step 1: Stage and commit**

  ```bash
  git add packages/themes
  git commit -m "$(cat <<'EOF'
  feat(themes): add cyberpunk theme — brutalism × neon palette (v38 T1)

  Add packages/themes/src/cyberpunk.css: a dark, neon (magenta + electric
  cyan) palette on neo-brutalist structure (zero radius, thick neon borders,
  hard glow-tinted offset shadows, fat cyan focus ring). Authored from the
  dark theme skeleton so it satisfies token parity; chart ramp kept on the
  CVD-safe base. Registered in package exports, all.css, README, and both
  theme test lists (parity + chart-palette). Animations and downstream
  registration follow in later tranches.

  Co-Authored-By: Claude <noreply@anthropic.com>
  EOF
  )"
  ```
