# v56 — Tranche 5: Prove It End-to-End (template + deploy example + cookbook)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Prove the four new primitives (T1 buffer, T2 `LogViewer`, T3 stream charts, T4 scope) in **real surfaces**,
so the next person building a streaming dashboard copies a working recipe instead of re-inventing the leaky buffer the
feedback reached for. Wire them into the v55 `templates/dashboard`, make `apps/examples/deploy` actually stream a mock
build log + a live usage chart, and ship a `docs/cookbooks/vercel-dashboard.md`. This is **composition** of existing
shells/charts/templates — **not** a new shell or template (M-5, Master Decisions 5/6; corrects C-1) — driven by the
existing seeded mock API, **no real backend** (Decision 7).

**Architecture:** three surfaces, all reusing existing layout/charts/template machinery.
- **`templates/dashboard` (v55 seed):** add (a) a **build-log panel** — a `LogViewer` fed by a `createStreamBuffer`
  whose appends come from the mock stream; (b) a **live-metrics chart** — an `AreaChart` driven by `useStreamSeries`
  (capped + decimated); (c) **scoped workspace state** — the per-workspace store created via `createScope`, disposed
  on workspace switch. Rebuild the template's `public/r/*.json` so the published template carries the new files +
  `registryDependencies` (`log-viewer`, `charts`).
- **`apps/examples/deploy`:** add a deployment **detail view** that streams a mock build log (the seeded mock API
  emits lines on a `setInterval`/tick into the buffer) and a **usage** panel with a live `AreaChart`; route
  per-workspace signals through a `createScope` that disposes when the active scope changes. React app ⇒ each
  component reads signals ⇒ **must call `useSignals()` first** (CLAUDE.md).
- **`docs/cookbooks/vercel-dashboard.md`:** the end-to-end recipe — `AppShell` + `SideNav` + `DashboardLayout` for the
  frame, `createScope` for workspace isolation, `createStreamBuffer` + `LogViewer` for logs, `useStreamSeries` +
  `AreaChart` for metrics, `DataTable` for deployments — naming where a real WS/SSE/poll transport plugs in.

**Tech Stack:** `@cascivo/layouts` (`AppShell`/`DashboardLayout`/`Grid`/`Stack`/`SideNav`), `@cascivo/charts`
(`AreaChart` + T3 `useStreamSeries`), `packages/components` (`LogViewer` T2, `DataTable`), `@cascivo/core` (T1 buffer,
T4 scope), `@cascivo/storage` (`persistedSignal` for view state, already used by deploy), the v55 templates pipeline,
the deploy mock API.

---

## Files

- Modify: `templates/dashboard/*` (page source + fixtures: log panel, live chart, scoped store) + `templates/cascivo-registry.json` + rebuilt `public/r/*.json`
- Modify: `apps/examples/deploy/src/*` (build-log detail view streaming via `LogViewer`; live usage `AreaChart`; `createScope` workspace store)
- Create: `docs/cookbooks/vercel-dashboard.md`
- Modify: `docs/ROADMAP-V56.md` → Status: Shipped + implementation log

---

### Task 1: Enrich the v55 `templates/dashboard`

**Files:** `templates/dashboard/*`, `templates/cascivo-registry.json`

- [ ] **Step 1:** Read the current `templates/dashboard` source + its `*.template.ts` manifest. Add a **build-log
      panel** section: a `createStreamBuffer({ capacity: 1000 })` fed by a fixture stream (a tick that appends mock log
      lines), rendered with `LogViewer` (auto-follow on). Add a **live-metrics** section: `useStreamSeries({ capacity,
      decimateTo, source })` → `AreaChart` (a mock metrics tick as `source`).
- [ ] **Step 2:** Wrap the template's per-workspace state in a `createScope`; on the (mock) workspace switcher,
      `dispose()` the old scope and create a new one. Keep all reactivity in signals (no `useState`/`useEffect`); any
      DOM/tick side effect uses `useSignalEffect`.
- [ ] **Step 3:** Update the template manifest: add the new files to `files[]`/`fileRoles`, add `log-viewer` (and
      `charts` if not already) to `registryDependencies`, refresh `screenshots`/`intent`. Rebuild via
      `cascivo registry build` so `public/r/*.json` reflects the new bundle. Validate with `validateTemplate`.

### Task 2: Stream in `apps/examples/deploy`

**Files:** `apps/examples/deploy/src/*`

- [ ] **Step 1:** Read the deploy example's structure (`AppShell`, `Deployments`, the mock API + `persistedSignal`).
      Add a **deployment detail** view: selecting a deployment opens a build-log panel where the mock API streams lines
      (a seeded `setInterval`/tick) into a `createStreamBuffer`, rendered by `LogViewer` with auto-follow + search.
- [ ] **Step 2:** Add a **live usage** panel: `useStreamSeries` fed by a mock metrics tick → an `AreaChart` (optionally
      two-axis: bandwidth vs requests/sec to exercise T3's multi-axis). Cap + decimate so the demo never degrades.
- [ ] **Step 3:** Route per-workspace state through a `createScope`; switching the mock workspace disposes the prior
      scope (prove no leaked ticks). Every component that reads `signal.value` in render calls `useSignals()` as its
      first statement (React app — CLAUDE.md). Update `apps/examples/deploy/readme.body.md`'s "What it demos" list to
      add `LogViewer`, `AreaChart`, streaming, scope.

### Task 3: Cookbook, roadmap, full gate + commit

**Files:** `docs/cookbooks/vercel-dashboard.md`, `docs/ROADMAP-V56.md`

- [ ] **Step 1:** `docs/cookbooks/vercel-dashboard.md`: the assembled recipe (shell → scope → streaming logs → live
      charts → data table), each section linking the primitive it uses (`createStreamBuffer`, `LogViewer`,
      `useStreamSeries`, `createScope`) and the cookbook (`charts-streaming.md`). A clear "**plug your real transport
      here**" callout on the `source`/append seam. Reference that the layout shells + `templates/dashboard` already
      existed (correcting the feedback's premise).
- [ ] **Step 2:** Flip `docs/ROADMAP-V56.md` Status to **✅ Shipped** and append an implementation log (one bullet per
      tranche, mirroring ROADMAP-V55's log), noting the corrections (C-1 shells existed; C-2 charts already strong; the
      feedback's O(n) buffer was replaced by an O(1) ring).
- [ ] **Step 3:** Full gate: `pnpm ready` (regen → check --fix → build → type check → tests). If workspace deps /
      build config changed (new template files, new exports), run `pnpm ready:ci`. `pnpm breakpoint:check` for the new
      UI. `git diff --exit-code` after `pnpm regen`.
- [ ] **Step 4: Stage and commit**

  ```bash
  git add templates apps/examples/deploy docs/cookbooks/vercel-dashboard.md docs/ROADMAP-V56.md
  git commit -m "$(cat <<'EOF'
  feat(dashboard): wire streaming primitives into template + deploy demo (v56 T5)

  Proves T1-T4 in real surfaces: the v55 templates/dashboard and
  apps/examples/deploy now stream a mock build log (LogViewer + stream buffer)
  and a live usage chart (useStreamSeries + AreaChart) under a disposable
  workspace scope. Adds docs/cookbooks/vercel-dashboard.md and flips ROADMAP-V56
  to Shipped. Composes existing shells/charts/template -- no new shell, no
  backend.

  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01CrqMjmeuAQZhxDpAu5UYN7
  EOF
  )"
  ```

**Definition of done:** the v55 `templates/dashboard` and `apps/examples/deploy` both stream a mock build log + a live
chart under a `createScope`, using only the new primitives + existing shells/charts; `docs/cookbooks/vercel-dashboard.md`
documents the recipe and the real-transport seam; `docs/ROADMAP-V56.md` is Shipped with a log; `pnpm ready` (and
`pnpm ready:ci` if needed) green; `breakpoint:check` + drift clean. Nothing rebuilt an existing shell or template
(C-1); the charts engine was reused (C-2); no backend, no new runtime dependency.
</content>
