---
'cascivo': patch
---

`cascivo audit --ai` now signs its summary lines as `cascivo` — "cascivo audit --ai: no
findings." and "cascivo audit --ai --fix: rewrote N literals to tokens." — instead of the
project's old name.
