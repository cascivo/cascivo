# v11 Tranche 7 — Positioning, Directory Dogfood, Launch Gates

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - T1–T6 merged: registry protocol v2 + static `/r/` output, multi-registry CLI with
>   lockfile/merge updates, directory + starter + MCP, platform-native overlays (audit doc
>   has the bytes-saved table), factory components landed (or honestly recorded),
>   data-table virtualization, Standard Schema forms, DTCG, llms-full, conformance page.
> - Landing app (`apps/landing`) is real React — `useSignals()` rule applies to any new
>   section reading signals.
> - `README.md` and llms intro are partially generated (`pnpm readme:generate`,
>   `pnpm llms:generate`) — find what is generated vs. hand-written before editing either.

**Goal:** v11's work becomes a story people can evaluate in five minutes: a "why cascade"
page that states the seven claims with receipts, a dogfooded external registry proving the
protocol end-to-end, refreshed README/landing positioning, and the full Definition-of-Done
walkthrough against `docs/ROADMAP-V11.md`.

**Architecture:** Content + verification tranche. One real artifact: the dogfood registry —
a first-party theme pack published as if we were a third party, listed in the directory,
installable via namespace. It is both a marketing asset and a continuous integration test
of the whole T1–T3 pipeline.

**Tech stack:** unchanged.

---

### Task 1: Dogfood registry — `@cascade-themes`

**Files:**

- Create: a theme-pack registry (location decision: separate public repo
  `urbanisierung/cascade-themes` is the honest dogfood — it exercises GitHub addressing;
  if a second repo is not desired at execution time, fall back to a subdirectory published
  via the docs host and record the tradeoff)
- Modify: `directory/registries.json` (second entry)

- [ ] **Step 1:** Build a small theme pack (2–3 new `type: 'theme'` registry items — e.g.
      a high-contrast theme and a brand-color sample generated via
      `cascade tokens import`/`create_theme`), authored exactly per
      `docs/CONTRIBUTING-REGISTRY.md` — the guide IS the test script; any friction found
      gets fixed in the guide (or the tooling) as part of this task.
- [ ] **Step 2:** Each theme passes the v9 theme-parity key-set check (reuse the helper)
      before publishing — verified-tier quality for our own dogfood.
- [ ] **Step 3:** List it in `directory/registries.json` (verified once CI checks pass);
      confirm `cascade add @cascade-themes/<theme>` works with zero config and shows up in
      `cascade search` + MCP `list_registries`.
- [ ] **Step 4: Commit** — `feat(directory): dogfood theme-pack registry, second directory entry`

---

### Task 2: "Why cascade" page

**Files:**

- Create: `apps/docs/src/pages/WhyPage.tsx` (+ nav wiring) — and/or a landing section;
  decide placement by where comparison-shoppers land (docs gets the page; landing gets a
  teaser linking to it)

- [ ] **Step 1:** Structure = the seven claims from `docs/ROADMAP-V11.md` ("The pitch"),
      each with its receipt, links not adjectives:
  1. Versioned updates → 30-second `cascade update` demo (asciinema-style code block or
     scripted output) showing a three-way merge that preserves local edits.
  2. No Tailwind/Radix/CSS-in-JS → dependency-count comparison table (ours vs. typical
     shadcn install), RSC note.
  3. Platform-native overlays → T4's bytes-removed table + supported-engine matrix.
  4. Distinct by default → theme strip (five themes + dogfood pack + theme generator
     pointer).
  5. AI-first ecosystem → MCP/llms/manifest pointers; community items inherit it.
  6. Compliance → conformance page link + EAA framing.
  7. Free what others paywall → component coverage vs. paywalled equivalents (name the
     facts — data table, date pickers, 16 chart types, calendar/date-range/tree-view as
     landed).
     Honesty rule: every number on this page must be reproducible from the repo or cited;
     no projections, no claims for components that didn't land (check T5 outcomes).
- [ ] **Step 2:** Landing teaser section (respect `useSignals()` rule; reveal-on-scroll
      pattern from v9 if reused).
- [ ] **Step 3: Commit** — `feat(docs,landing): "why cascade" positioning with receipts`

---

### Task 3: README + llms intro refresh

**Files:**

- Modify: `README.md` (hand-written parts only — re-verify what readme:generate owns),
  llms intro source in `scripts/llms/generate.ts`

- [ ] **Step 1:** README: ecosystem section (registry protocol, directory, starter,
      lockfile updates), claims aligned with the why-page, links to schemas/directory/
      conformance/llms-full. Surgical — don't restructure unrelated sections.
- [ ] **Step 2:** llms.txt intro mentions: multi-registry CLI, directory URL,
      `cascade.lock` semantics (agents will operate the CLI — they need the update flow
      described), conformance JSON URL.
- [ ] **Step 3: Commit** — `docs: README + llms intro — open ecosystem positioning`

---

### Task 4: Definition-of-Done walkthrough + final gate

- [ ] **Step 1:** Walk `docs/ROADMAP-V11.md` DoD checkbox by checkbox; for each, run the
      verifying action (fresh-clone starter build; the four address forms against the
      dogfood + fixtures; lock/merge scenario; directory CI on a deliberately-broken
      fixture branch; overlay fallback eyeball; 100k-row table; tri-library form test;
      DTCG round-trip; llms-full + conformance + why-page served from a production docs
      build). Tick boxes in the roadmap doc as verified; anything failing gets fixed or
      honestly re-scoped in the roadmap's Deferred section.
- [ ] **Step 2:** Full CLAUDE.md gate one final time:

```bash
pnpm exec vp check && pnpm build && pnpm exec vp run -r check && pnpm test
pnpm registry:generate && pnpm readme:generate && pnpm llms:generate
pnpm exec vp check --fix && git diff --exit-code
```

- [ ] **Step 3:** Update `docs/ROADMAP-V11.md` status line to ✅ Done (with checked DoD)
      — only if it is true.
- [ ] **Step 4: Commit** — `chore(v11-t7): DoD walkthrough, roadmap close-out`

---

## Tranche exit criteria

1. The dogfood registry is live, listed, verified, and installable via namespace AND
   GitHub addressing — the protocol proven outside the monorepo.
2. The why-page states all seven claims with reproducible receipts; landing teases it.
3. README/llms reflect the ecosystem accurately.
4. Every ROADMAP-V11 DoD item verified and ticked (or honestly moved to Deferred with
   rationale); status updated.
5. Root gate + drift check exit 0.
