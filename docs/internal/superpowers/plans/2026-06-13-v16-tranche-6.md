# v16 Tranche 6 — Final gate + DoD close

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.
>
> Final tranche. T1–T5 are done. This verifies everything, runs the full gate, re-runs mobile
> harnesses on the complete build, and closes the v16 roadmap.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - All 5 prior tranches committed and on `roadmap-v16-implementation` (or whichever branch).
> - `apps/landing/test/mobile.spec.ts` has been updated in T2 and T4 to include the new sections.
> - `apps/docs/src/pages/WhyCascivoPage.tsx` has claims 28–30 added (T5).
> - `docs/ROADMAP-V16.md` DoD boxes are still unchecked — this tranche checks them.

**Goal:** Full CI gate exits 0 on the complete v16 build. Every ROADMAP-V16.md DoD box is checked
with evidence. Playwright mobile proves zero overflow including all new sections.

**Architecture:** Verification + documentation only. No new code unless the gate reveals a bug.

---

### Task 1: Full CLAUDE.md gate

- [ ] **Step 1:** Format + lint: `pnpm exec vp check` — 0 errors, 0 new warnings.
- [ ] **Step 2:** Build: `pnpm build` — all packages + apps build clean.
- [ ] **Step 3:** Type check: `pnpm exec vp run -r check` — 0 type errors.
- [ ] **Step 4:** Tests: `pnpm test` — all existing tests pass (no regressions). The new code in
      `apps/landing` has no unit tests (UI sections are verified by Playwright); confirm no test
      files in `apps/landing/src/` reference removed or renamed symbols.
- [ ] **Step 5:** Drift check: `pnpm regen` → `pnpm exec vp check --fix` → `git diff --exit-code`.
      The regen must be clean. If `registry.json` or any generated artifact is dirty, commit the
      generated files before the final diff check.
- [ ] **Step 6:** If any gate step fails: fix the issue before marking this task complete. Do not
      mark gates passed if they are failing.

---

### Task 2: Playwright mobile re-verification

- [ ] **Step 1:** Start the landing dev server: `pnpm exec vp dev @cascivo/landing` (or equivalent)
      in the background.
- [ ] **Step 2:** Run the full landing mobile harness:
      `pnpm exec playwright test apps/landing/test/mobile.spec.ts`
      — ALL assertions pass including the new ChartShowcase and Ecosystem sections at 320/375/390/414.
      Zero horizontal overflow everywhere.
- [ ] **Step 3:** Run the docs mobile harness (confirm no regressions from T5 docs changes):
      `pnpm exec playwright test apps/docs/test/mobile.spec.ts`
      — all assertions pass.
- [ ] **Step 4:** Spot-check the landing visually at 375px width in a browser:
  - Hero chips show "WCAG 2.2 AA", "RSC-compatible", "10 themes" in stats badge
  - ChartShowcase section: charts visible, CVD callout readable, no overflow
  - AgentLayer: 6 cards visible, audit card shows before/after code, llms.txt card links
  - Ecosystem section: 4 package cards stacked vertically, install snippets legible
  - Why-cascivo in docs: claims 1–30 all render
- [ ] **Step 5: Commit** — `chore(v16-t6): full gate + mobile verification`

---

### Task 3: DoD walkthrough — ROADMAP-V16.md

**Files:**

- Modify: `docs/ROADMAP-V16.md`

Walk every DoD box. Check each one with evidence from the completed tranches:

- [ ] **Box 1:** "Hero chip reads 'WCAG 2.2 AA'; hero stats show '10 themes'; CLI copy command
      correct; 'RSC-compatible' chip present."
      → Verify in `Hero.tsx` + visual check. If true, mark `[x]` and record evidence.

- [ ] **Box 2:** "A Charts showcase section live: ≥3 chart types with active-theme styling, CVD-safe
      callout, keyboard tooltip instruction; zero overflow at 320–414px."
      → Verify in `ChartShowcase.tsx` + Playwright output. Mark `[x]`.

- [ ] **Box 3:** "AgentLayer has an audit artifact card (before/after agent code) and an llms.txt
      explanation card."
      → Verify in `AgentLayer.tsx`. Mark `[x]`.

- [ ] **Box 4:** "An Ecosystem section cards @cascivo/charts, @cascivo/layouts, @cascivo/i18n,
      @cascivo/icons with one-line value props + docs links."
      → Verify in `Ecosystem.tsx`. Mark `[x]`.

- [ ] **Box 5:** "'Why cascivo' claims 1–27 all render; claims 28–30 live with receipts linked to
      new sections."
      → Verify in `WhyCascivoPage.tsx` (or `WhyCascadePage.tsx`). Mark `[x]`.

- [ ] **Box 6:** "Full CLAUDE.md gate exits 0."
      → Evidence: Task 1 output above. Mark `[x]`.

- [ ] **Box 7:** "Playwright mobile at 320/375/390/414 passes including all new sections."
      → Evidence: Task 2 output above. Mark `[x]`.

- [ ] **Box 8:** "ROADMAP-V16.md DoD boxes all checked; status → ✅ Shipped."
      → Update the status line: `**Status:** 🔲 Planned` → `**Status:** ✅ Shipped`.
      Record today's date on the `**Last updated:**` line.

- [ ] **Step: Commit** — `chore(v16-t6): final DoD walkthrough + roadmap close`

---

## Tranche exit criteria

1. Full CLAUDE.md gate exits 0 (`vp check` → build → `vp run -r check` → test → regen → diff).
2. Playwright mobile harness passes at 320/375/390/414 for landing + docs — zero overflow on all
   new sections (ChartShowcase, Ecosystem).
3. Every DoD box in `ROADMAP-V16.md` is checked with inline evidence.
4. `ROADMAP-V16.md` status is `✅ Shipped`.
5. Branch is ready for PR to main.
