# v15 Tranche 7 — Receipts & launch (brand page, mobile showcase, go-live)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.
>
> Final tranche. The rename (T1–T4) and mobile work (T5–T6) are done. This ships the user-facing
> receipts for claims 25–27, refreshes OG/meta/README/llms with the new name, and walks the external
> go-live checklist from T4.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - `docs/specs/cascivo-launch-checklist.md` exists (from T4).
> - "Why cascade" surface exists (`apps/landing` "Why cascade" page / `apps/docs` WhyCascivoPage) —
>   v14 added claims 20–24. Locate the current claims list.
> - OG/meta live in `apps/landing/index.html` + an `/og` route (`OgCard`); favicons in `public/`.
> - README(s) + `llms.txt`/`llms-full.txt` carry the brand name + install snippets.

**Goal:** A brand page + mobile showcase exist; "Why cascivo" states claims 25–27 each linked to a
receipt; OG/meta/favicons/README/llms carry the new name and brand visuals; the external go-live
checklist is executed (or its remaining human steps clearly flagged). Full DoD gate green.

**Architecture:** Docs/landing pages + meta/asset updates. No package changes. Uses the brand assets
(T1), the renamed surfaces (T2–T4), and the mobile-first layouts (T5–T6).

**Tech stack:** unchanged.

---

### Task 1: Brand page + mobile showcase

**Files:**

- Create/Modify: a brand page in `apps/landing` (or `apps/docs`) — name story, logo, palette
- Modify: the "Why cascivo" surface

- [ ] **Step 1:** Build a brand page: the `cascade-ui → cascivo` story (from `BRAND.md`), the logo
      (all lockups, light/dark), and the brand palette swatches with their oklch values + contrast
      results. This is the receipt for **claim 25**.
- [ ] **Step 2:** Add a mobile showcase (or a section on the brand/why page): the responsive
      screenshots from T5/T6 at representative widths, framed as proof — receipts for **claims 26–27**.
- [ ] **Step 3:** Update "Why cascivo" to add claims 25 (coherent brand), 26 (flawless mobile
      landing), 27 (mobile docs parity), each linking its receipt. Keep claims 1–24 intact.
- [ ] **Step 4: Commit** — `feat(docs): brand page + mobile showcase + Why-cascivo 25–27 (v15-t7)`

---

### Task 2: OG, meta, favicons, titles

**Files:**

- Modify: `apps/landing/index.html`, `apps/docs/index.html`, the `/og` route (`OgCard`),
  `public/` favicons, page `<title>`s

- [ ] **Step 1:** Update `<title>`, meta description, `og:title`/`og:image`/`og:url`,
      `twitter:*`, and canonical to `cascivo` / `cascivo.com`. Regenerate/replace the OG card with
      the new logo + brand gradient.
- [ ] **Step 2:** Wire favicons from the T1 mark (`favicon.svg` + any PNG fallbacks); update
      `apple-touch-icon` and the web manifest if present (name, theme-color = brand primary).
- [ ] **Step 3:** Verify OG renders (open the `/og` route; check a link-preview validator if
      reachable). Confirm theme-color and favicons load.
- [ ] **Step 4: Commit** — `feat(apps): cascivo OG, meta, favicons (v15-t7)`

---

### Task 3: README + llms + docs copy sweep

**Files:**

- Modify: root `README.md`, package READMEs, `llms.txt`/`llms-full.txt`, any remaining brand copy

- [ ] **Step 1:** Update the headline name, tagline, install snippets (`pnpm add @cascivo/react`,
      `npx @cascivo/cli add button`, `import '@cascivo/themes/light.css'`), badges, and links to
      `cascivo.com` / the renamed repo.
- [ ] **Step 2:** Final brand-copy grep:
      `grep -rni 'cascade-ui\|@cascade-ui\|--cascivo-\|cascade-ui\.dev' . | grep -v node_modules`
      returns only deliberately historical mentions (archived `docs/ROADMAP-V*`, plan files, BRAND.md
      history). Everything user-facing is `cascivo`.
- [ ] **Step 3: Commit** — `docs: README + llms + copy → cascivo (v15-t7)`

---

### Task 4: Execute the external go-live checklist

**Files:**

- Modify: `docs/specs/cascivo-launch-checklist.md` (check items off / record status)

> These are human/infra steps — the agent flags status and verifies what it can.

- [ ] **Step 1:** npm org `cascivo` created; `@cascivo/*` packages publishable (dry-run `pnpm publish
--dry-run` per public package to confirm names/exports/files are correct — do not actually
      publish unless instructed).
- [ ] **Step 2:** `cascivo.com` DNS + hosting + TLS live (or flagged pending); docs subdomain if used.
- [ ] **Step 3:** GitHub repo renamed `urbanisierung/cascade-ui → urbanisierung/cascivo`
      (redirect-safe); local `git remote set-url` updated; the regenerated registry raw URLs resolve.
- [ ] **Step 4:** End-to-end smoke (once infra is live): `npx @cascivo/cli add button` fetches a real
      component from the new registry URLs. Record the result in the checklist.
- [ ] **Step 5: Commit** — `chore(v15-t7): launch checklist status`

---

### Task 5: Final DoD gate (ROADMAP-V15)

- [ ] **Step 1:** Walk every box in `docs/ROADMAP-V15.md` "Definition of Done"; confirm each is met
      (or, for infra-dependent items, that the code prerequisite is done and the human step is flagged
      in the checklist).
- [ ] **Step 2:** Full CLAUDE.md gate: `vp check` → `pnpm build` → `vp run -r check` → `pnpm test` →
      `pnpm regen` → `vp check --fix` → `git diff --exit-code`. All exit 0.
- [ ] **Step 3:** Re-run the T5/T6 mobile harnesses once more on the final build — zero overflow at
      320/375/390/414.
- [ ] **Step 4: Commit** — `chore(v15-t7): final gate + DoD walkthrough`

---

## Tranche exit criteria

1. A brand page (name story, logo, palette + contrast) and a mobile showcase exist; "Why cascivo"
   states claims 25–27, each linked to a receipt.
2. OG/meta/favicons/web-manifest/titles across both apps carry `cascivo` + the brand visuals.
3. README(s)/llms/docs copy are `cascivo`; only historical/archival mentions of the old name remain.
4. `docs/specs/cascivo-launch-checklist.md` reflects the executed/flagged status of npm org, DNS,
   and repo rename; the CLI install path is smoke-verified (or flagged pending infra).
5. Every `ROADMAP-V15.md` DoD box is met or its human step explicitly flagged; full gate exits 0;
   mobile harnesses prove zero overflow on the final build.
   </content>
