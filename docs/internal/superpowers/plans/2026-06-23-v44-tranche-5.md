# v44 — Tranche 5: Stories + Docs Page + App Wiring

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Surface `@cascivo/editor` in the two consumer apps the way `@cascivo/charts` is surfaced: hand-written
**Storybook stories** (`stories/editor/*`), a curated **docs showcase page** (`EditorPage.tsx` + route + nav),
and the **source aliases** that let docs/landing/storybook build the package without a prior full build.

**Architecture:** Stories and the docs page are **hand-written** (the cascivo convention for standalone packages
— charts are not auto-routed from the registry; the auto-generation in T6 covers `registry.json`/`context.json`/
`llms.txt`/MCP, not the human showcase). Because `@cascivo/editor`'s `exports["."]` resolves to `./dist/`, and
docs/landing/storybook build **without** a prior `pnpm build`, each app needs a **source alias** mapping
`@cascivo/editor` → `packages/editor/src/index.ts` (per CLAUDE.md "Workspace package aliases — keep in sync").
Don't forget `@cascivo/editor/styles.css` — the docs/storybook need the editor CSS imported.

**Tech Stack:** Storybook (CSF stories), the docs app (Preact + cascivo), vite aliases. Mirror `ChartsPage.tsx`
and `stories/chart/*`.

---

## Files

- Create: `apps/storybook/stories/editor/code-editor.stories.tsx`, `apps/storybook/stories/editor/highlight.stories.tsx`
- Create: `apps/docs/src/pages/EditorPage.tsx`
- Modify: `apps/docs/src/App.tsx` (add `Route path="/editor"`)
- Modify: the docs nav (where `ChartsPage`'s nav entry lives — `Layout.tsx` / nav tree)
- Modify: `apps/docs/vite.config.ts`, `apps/landing/vite.config.ts`, `apps/storybook/.storybook/main.ts` (add `@cascivo/editor` source alias)

---

### Task 1: Study the charts wiring in docs + storybook

**Files:** `apps/docs/src/pages/ChartsPage.tsx`, `apps/docs/src/App.tsx`, the docs nav file, `apps/storybook/stories/chart/*`, `apps/storybook/.storybook/main.ts`, `apps/docs/vite.config.ts`, `apps/landing/vite.config.ts`

- [ ] **Step 1:** Read `ChartsPage.tsx` for the showcase structure (section headings, live demos, code samples,
      how it imports from `@cascivo/charts` and the `styles.css`). Read the `Route path="/charts"` line in
      `App.tsx` and the nav entry that links to it.
- [ ] **Step 2:** Read a `stories/chart/*.stories.tsx` for the CSF format (meta `title`, `args`, per-variant
      stories) and how it imports the package + styles.
- [ ] **Step 3:** Read the three alias blocks (`apps/docs/vite.config.ts`, `apps/landing/vite.config.ts`,
      `apps/storybook/.storybook/main.ts`) — confirm the exact `@cascivo/charts` alias line to mirror for
      `@cascivo/editor`.

### Task 2: Add the source aliases (all three apps)

**Files:** `apps/docs/vite.config.ts`, `apps/landing/vite.config.ts`, `apps/storybook/.storybook/main.ts`

- [ ] **Step 1:** Add `'@cascivo/editor': resolve(root, 'packages/editor/src/index.ts')` to each app's alias
      block (matching the existing `@cascivo/charts` line's style/path helper in each file).
- [ ] **Step 2:** Verify each app resolves the package: `pnpm exec vp run @cascivo/docs#build
      @cascivo/landing#build @cascivo/storybook#build` (per CLAUDE.md — these build without a prior full build).

### Task 3: Storybook stories

**Files:** `apps/storybook/stories/editor/code-editor.stories.tsx`, `highlight.stories.tsx`

- [ ] **Step 1:** `code-editor.stories.tsx`: import `CodeEditor` + `@cascivo/editor/styles.css`; stories for
      Basic, WithLineNumbers, ReadOnly, Languages (js/ts/json/css/html/markdown/bash), Wrap, and a Themed story
      (render inside `data-theme="dark"`/`"warm"` wrappers). Use `args` for the toggles.
- [ ] **Step 2:** `highlight.stories.tsx`: import `Highlight`; stories showing read-only highlighting per
      language and with/without line numbers.
- [ ] **Step 3:** `pnpm exec vp run @cascivo/storybook#build` succeeds; stories render (smoke check).

### Task 4: Docs showcase page + route + nav

**Files:** `apps/docs/src/pages/EditorPage.tsx`, `apps/docs/src/App.tsx`, docs nav file

- [ ] **Step 1:** Create `EditorPage.tsx` (the `ChartsPage` pattern): an intro, a live editable `CodeEditor`
      demo, a language gallery, a read-only `Highlight` example, a line-numbers/read-only/wrap matrix, and a
      "themeable" note (it follows `data-theme`). Import `@cascivo/editor` + `styles.css`. Keep the page
      signal-correct for the React/Preact app (per CLAUDE.md — Preact docs app is natively reactive).
- [ ] **Step 2:** Add `<Route path="/editor" component={EditorPage} />` to `App.tsx` and a nav entry next to
      Charts in the docs nav tree.
- [ ] **Step 3:** `pnpm exec vp run @cascivo/docs#build` succeeds; the `/editor` route renders.

### Task 5: Verify

- [ ] **Step 1:** `pnpm exec vp run @cascivo/docs#build @cascivo/landing#build @cascivo/storybook#build` all
      green (the alias-sync requirement). `pnpm exec vp check` clean.
- [ ] **Step 2:** No off-scale breakpoint literals introduced in the new pages (`pnpm breakpoint:check`).

### Task 6: Commit T5

- [ ] **Step 1: Stage and commit**

  ```bash
  git add apps/docs apps/landing apps/storybook
  git commit -m "$(cat <<'EOF'
  docs(editor): stories + docs showcase + app aliases (v44 T5)

  Hand-written Storybook stories (stories/editor/*) and a curated docs
  showcase (EditorPage + /editor route + nav), mirroring the charts
  pattern. Adds the @cascivo/editor source alias to docs, landing, and
  storybook vite configs so each builds without a prior full build.

  Co-Authored-By: Claude <noreply@anthropic.com>
  EOF
  )"
  ```
</content>
