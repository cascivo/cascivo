# Settings — Profile

Two-section settings form: personal info fields + preference toggles.

## Install

Copy-paste the source (you own and can edit it):

```bash
npx cascivo add block/settings-profile
```

_Copy-paste only — `Settings — Profile` is not exported from `@cascivo/react`. Run the command above to own the source, or compose it from the exported primitives (`Flex`, `Grid`, `Heading`, …)._

## Category

`shell`

## Props

_No props._

## Dependencies

- `@cascivo/react`

## Tags

settings, profile, form, toggles, preferences

---

_Generated from registry v0.15.0 on 2026-08-03. Docs track `main`; compare with https://cascivo.com/registry.json `.version`._
