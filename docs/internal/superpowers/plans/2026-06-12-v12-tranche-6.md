# v12 Tranche 6 — Receipts, Positioning, Launch Gates

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - T1–T5 merged: audit/drift/action, CSS contract + theming docs + `theme create`,
>   studio/axes/gallery, size + RSC receipts, traveling contract tests + composition
>   matrix.
> - v11-T7's "why cascade" page exists with claims 1–7 (`apps/docs/src/pages/WhyPage.tsx`
>   or wherever it landed — find it); README/llms structure from v11-T7 known.

**Goal:** v12's six claims (8–13) join the why-page with reproducible receipts, the
README/llms surfaces describe the lifecycle story, and the roadmap's Definition of Done is
walked item by item.

**Architecture:** Content + verification tranche; no new machinery.

---

### Task 1: Why-page claims 8–13

**Files:**

- Modify: the why-cascade page (+ landing teaser if v11-T7 added one)

- [ ] **Step 1:** Add the six claims, each with its receipt linked, not asserted:
  - **8 Advisories reach vendored code** → `cascade audit` output sample (real, from the
    dogfood advisory of T1) + the action's PR screenshot/link.
  - **9 Customizations are first-class** → `doctor --drift` sample output.
  - **10 Customization is a contract** → link the CI contract-test run + the rule list;
    "zero `!important` in the entire system" stated as the checkable fact it now is.
  - **11 Distinct by construction** → T3's screenshot pair + studio link.
  - **12 Performance receipts** → Size page + RSC example links; copy-paste-no-bundle
    note repeated here.
  - **13 The keyboard contract travels** → snippet of a copied contract test + the
    broken-dropdown audit demo transcript.
    Honesty bar unchanged: every number reproducible from the repo; no claim for
    anything T1–T5 honestly deferred.
- [ ] **Step 2:** Re-verify claims 1–7 receipts still hold (links unbroken, numbers
      current after v12's changes — especially sizes).
- [ ] **Step 3: Commit** — `feat(docs): why-cascade claims 8–13 with receipts`

---

### Task 2: README + llms refresh

**Files:**

- Modify: `README.md` (hand-written sections only), llms intro in
  `scripts/llms/generate.ts`

- [ ] **Step 1:** README: lifecycle section (audit, drift, update automation, traveling
      tests) + studio/gallery pointers. Surgical.
- [ ] **Step 2:** llms intro: agents operating cascade projects need to know `audit`,
      `doctor --drift`, `--no-tests`, the contract-test convention (they'll encounter
      `*.contract.test.tsx` in user projects and must not delete or "fix" them as
      noise), and the studio share-URL format. State each in one line.
- [ ] **Step 3:** Skills check: do `skills/cascade-add` / `cascade-extend` need updates
      for contract tests + audit flows? Update the skill docs if their instructions now
      misdescribe `add` behavior (tests copied by default).
- [ ] **Step 4: Commit** — `docs: README, llms, skills — lifecycle story`

---

### Task 3: Definition-of-Done walkthrough + close-out

- [ ] **Step 1:** Walk `docs/ROADMAP-V12.md` DoD checkbox by checkbox, running each
      verifying action (advisory round-trip incl. community fixture; drift matrix;
      action PR; contract-gate fixture violation; theming docs spot-checks; `theme
create` parity; studio round-trip + screenshot pair; size-budget fixture breach;
      RSC page hydration-warning check; broken-dropdown audit scenario; composition
      matrix). Tick verified boxes; anything failing is fixed or honestly moved to
      Deferred with rationale.
- [ ] **Step 2:** Full CLAUDE.md gate:

```bash
pnpm exec vp check && pnpm build && pnpm exec vp run -r check && pnpm test
pnpm registry:generate && pnpm readme:generate && pnpm llms:generate
pnpm exec vp check --fix && git diff --exit-code
```

      (plus the v12 additions now in regen: size measurement, RSC matrix artifacts,
      theming-docs data, gallery data.)

- [ ] **Step 3:** Update `docs/ROADMAP-V12.md` status to ✅ Done — only if true.
- [ ] **Step 4: Commit** — `chore(v12-t6): DoD walkthrough, roadmap close-out`

---

## Tranche exit criteria

1. Why-page carries claims 8–13 with live receipts; claims 1–7 re-verified.
2. README/llms/skills accurately describe the lifecycle tooling (including the
   don't-delete-contract-tests instruction for agents).
3. Every ROADMAP-V12 DoD item verified and ticked (or moved to Deferred with rationale);
   status updated truthfully.
4. Root gate + drift check exit 0.
