# v33 — Tranche 8: Gate

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Run the full CI gate locally and confirm every check passes after T1–T7. No feature work — this tranche only verifies and, if drift is detected, regenerates and commits artifacts.

**Architecture:** Mirrors the six CI steps documented in `CLAUDE.md` ("Gate Before Committing"). All must exit 0.

**Tech Stack:** vite+ (`vp`), pnpm.

---

## Files

- Verify: `registry.json` (regenerated via `pnpm regen`, no manual edits)
- Verify: all files touched in T1–T7

---

### Task 1: Format + lint

- [ ] **Step 1:** `pnpm exec vp check` → exit 0. Fix any format/lint issues with `pnpm exec vp check --fix` and re-run.

### Task 2: Build

- [ ] **Step 1:** `pnpm build` → exit 0.
- [ ] **Step 2:** Confirm the apps that build without a prior full build still resolve workspace aliases (docs, landing, storybook). No new `@cascivo/*` package was added in v33, so no alias changes are expected — but if a build fails on a `dist/` resolution, cross-check the alias rule in `CLAUDE.md`.

### Task 3: Type check

- [ ] **Step 1:** `pnpm exec vp run -r check` → exit 0.

### Task 4: Tests

- [ ] **Step 1:** `pnpm test` → exit 0.
- [ ] **Step 2:** Pay attention to the Blocks page tests (`apps/landing/src/pages/blocks/BlocksPage.test.tsx`, `BlockDetailPage.test.tsx`) — the T3 chrome wrapping and iframe-card change may require test updates (e.g. queries that expected an `<img>` now find an `<iframe>`, or assertions about page structure). Update tests to match the new markup; do not delete coverage.

### Task 5: Drift check

- [ ] **Step 1:** `pnpm regen` then `pnpm exec vp check --fix` then `git diff --exit-code` → exit 0. Commit any regenerated artifacts if they changed.

### Task 6: Breakpoint literal check

- [ ] **Step 1:** `pnpm breakpoint:check` → exit 0. Any new `@media`/`@container` width literal (carousel fade, scorecard grid, segmented control spacing) must come from the canonical scale (`sm 30rem`, `md 40rem`, `lg 64rem`, `xl 80rem`).

---

### Task 7: Final verification sweep

- [ ] **Step 1:** Manually re-check each defect against the roadmap Definition of Done:
  - Re-render demo: one form, two live counters, typewriter, reduced-motion respected.
  - Carousel: cross-fades on every switch; instant under reduced motion.
  - Demo apps: back-to-cascivo link present and targets `/`.
  - Blocks: header/footer present; cards show live previews, not green.
  - Accessibility: scorecard with PASS/PASS/FAIL(label).
  - Perf matrix: SegmentedControl + % deltas (omitted at 0%).
  - Latency table: best/worst marked.
  - Re-renders chart: bars (if T7 produced data) or honest "no data" message.

- [ ] **Step 2:** Mobile-overflow + touch-target sweep at 320/360/390/414 across all touched sections.

---

### Task 8: Commit any gate fixups

- [ ] **Step 1: Stage and commit** (only if the gate produced changes — drift regen, test updates, format fixes)

  ```bash
  git add -A
  git commit -m "$(cat <<'EOF'
  chore(v33): gate fixups — drift regen, test updates, format

  Co-Authored-By: Claude <noreply@anthropic.com>
  EOF
  )"
  ```

  </content>
