# v23 Tranche 1 — Rename + rebrand `deploy` → `edge`

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.
>
> Start here. This tranche is a **mechanical rename**: it renames the `deploy` demo to `edge` (Cascade
> Edge) across the app and every integration surface, with **no content/behaviour change**. The DORA/
> pipeline/environment content is still present after T1 — it is replaced in T2+. Isolating the churny
> rename here keeps later tranches focused on the rebuild.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - `apps/examples/deploy/` is package `@cascivo/example-deploy`, slug `deploy`, with `base: './'` in
>   `vite.config.ts`, i18n namespace `deploy.*` (`deployMsg`), and `persistedSignal('deploy.theme', …)`.
> - Integration refs to `deploy`/`example-deploy` exist in: `apps/landing/src/pages/examples/data.ts`,
>   `apps/landing/src/route-head.ts`, `apps/landing/public/sitemap.xml`, `scripts/assemble-demos.mjs`,
>   `scripts/checks/demo-storage-keys.test.ts`, `apps/examples/coverage.test.ts`,
>   `.github/workflows/cf-pages.yml`, and `apps/landing/public/screenshots/deploy/`.
> - The kit's `createMockApi`/`Pipeline`/`Environment`/`Metrics` are shared by `pay`/`flow` — leave them.

**Goal:** The demo is named `edge` everywhere. `pnpm exec vp run @cascivo/example-edge#build` builds; the
landing builds; `pnpm build:landing-demos` serves the live demo at `/demos/edge/`; the marketing route
is `/examples/edge`; and a repo-wide grep finds no stray `deploy`/`example-deploy` reference for this
demo. Gate green.

**Architecture:** `git mv apps/examples/deploy apps/examples/edge`, then rename package/i18n/storage/URL
identifiers inside the app, then update every external reference. No content rebuild.

**Tech stack:** unchanged.

---

### Task 1: Move the app directory + rename the package

**Files:** `apps/examples/deploy/` → `apps/examples/edge/`; `apps/examples/edge/package.json`

- [ ] **Step 1:** `git mv apps/examples/deploy apps/examples/edge` (preserve history).
- [ ] **Step 2:** In `package.json`, set `"name": "@cascivo/example-edge"`. Leave scripts/deps unchanged.
- [ ] **Step 3:** `pnpm install` to refresh the workspace lockfile/links. _Verify: `pnpm -r list 2>/dev/null`
      (or `pnpm why`) resolves `@cascivo/example-edge` and no longer lists `@cascivo/example-deploy`._

### Task 2: Rename in-app identifiers (i18n, storage, URL comment, README)

**Files:** `apps/examples/edge/src/i18n.ts`, `src/App.tsx`, `vite.config.ts`, `readme.body.md`

- [ ] **Step 1:** `src/i18n.ts` — change the `defineMessages` namespace `deploy.*` → `edge.*`, rename the
      exported `deployMsg` → `edgeMsg`, and set `appTitle` to "Cascade Edge". Update all importers
      (`App.tsx`, the section files) to `edgeMsg`.
- [ ] **Step 2:** `src/App.tsx` — change `persistedSignal('deploy.theme', …)` → `persistedSignal('edge.theme', …)`.
- [ ] **Step 3:** `vite.config.ts` — update the `base: './'` comment that references `/demos/deploy/` →
      `/demos/edge/`. (The `../kit/src/index.ts` alias path is unchanged by the move — confirm it still
      resolves.)
- [ ] **Step 4:** `readme.body.md` — rename "Cascade Deploy" → "Cascade Edge", `@cascivo/example-deploy`
      → `@cascivo/example-edge`, and the live URL `/demos/deploy/` → `/demos/edge/`. Then regenerate:
      `pnpm exec vp run scripts/readme/generate.ts` (or the documented readme regen command) so
      `README.md` is rewritten — **never hand-edit `README.md`**.

### Task 3: Update landing integration references

**Files:** `apps/landing/src/pages/examples/data.ts`, `route-head.ts`, `public/sitemap.xml`

- [ ] **Step 1:** `data.ts` — in the `DemoSlug` union replace `'deploy'` with `'edge'`. In the first
      `DEMOS` entry set `slug: 'edge'`, `name: 'Cascade Edge'`, `liveHref: '/demos/edge/'`,
      `detailHref: '/examples/edge'`, `screenshots('edge')`. Keep `feelsLike: 'Vercel'`. (Tagline/
      description/coverage copy is rewritten in T6 — this tranche only renames identifiers.)
- [ ] **Step 2:** `route-head.ts` — rename the `ROUTE_HEAD['/examples/deploy']` key → `'/examples/edge'`
      (title/description copy refreshed in T6) and the `PRERENDER_ROUTES` entry `'examples/deploy'` →
      `'examples/edge'`.
- [ ] **Step 3:** `public/sitemap.xml` — `https://cascivo.com/examples/deploy` → `…/examples/edge`.

### Task 4: Update scripts, checks, and CI references

**Files:** `scripts/assemble-demos.mjs`, `scripts/checks/demo-storage-keys.test.ts`,
`apps/examples/coverage.test.ts`, `.github/workflows/cf-pages.yml`, `scripts/gen-demo-screenshots.mjs`

- [ ] **Step 1:** `assemble-demos.mjs` — in the `DEMOS` map replace `deploy: '@cascivo/example-deploy'`
      with `edge: '@cascivo/example-edge'`.
- [ ] **Step 2:** `demo-storage-keys.test.ts` — replace `'deploy'` with `'edge'` in its `DEMOS` array.
- [ ] **Step 3:** `coverage.test.ts` — replace `'deploy'` with `'edge'` in `APPS`.
- [ ] **Step 4:** `cf-pages.yml` — in the `landing` paths-filter, `apps/examples/deploy/**` →
      `apps/examples/edge/**`.
- [ ] **Step 5:** `gen-demo-screenshots.mjs` — if it hardcodes the slug list, replace `deploy` with
      `edge`; if it reads from `data.ts`, no change. Re-verify.

### Task 5: Rename the screenshots directory

**Files:** `apps/landing/public/screenshots/deploy/` → `…/edge/`

- [ ] **Step 1:** `git mv apps/landing/public/screenshots/deploy apps/landing/public/screenshots/edge`
      (the four PNGs move with it). T6 regenerates them deterministically; the renamed placeholders keep
      the landing building meanwhile.

### Task 6: Grep sweep + end-to-end verification

- [ ] **Step 1:** Grep the repo (excluding `node_modules`/`dist`/`.git`) for `example-deploy`,
      `demos/deploy`, `examples/deploy`, `screenshots/deploy`, `deployMsg`, and the storage key
      `deploy.theme`. **Zero matches** for this demo. (Generic English "deploy"/"deployment" words inside
      the app's UI copy and the kit are expected and fine.)
- [ ] **Step 2:** `pnpm exec vp run @cascivo/example-edge#build` then `vp preview` — the app loads at `/`,
      assets resolve, theme toggles and persists under the `edge.theme` key.
- [ ] **Step 3:** `pnpm build:landing-demos` — confirm `apps/landing/dist/demos/edge/index.html` exists
      and serves the demo; `/examples/edge` prerenders.

### Exit criteria

- [ ] App moved to `apps/examples/edge/`, package `@cascivo/example-edge`, slug `edge` everywhere.
- [ ] i18n namespace `edge.*`, storage prefix `edge.*`, live URL `/demos/edge/`, route `/examples/edge`.
- [ ] All landing/script/check/CI/screenshot references updated; grep sweep clean.
- [ ] Kit `createMockApi`/types untouched; `pay`/`flow` unaffected.
- [ ] Gate green (`vp check` → build → `vp run -r check` → test → regen+diff → `breakpoint:check`).
- [ ] Committed.
