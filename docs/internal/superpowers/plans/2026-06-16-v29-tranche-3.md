# v29 — Tranche 3: Dashboard + Marketing Blocks

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement four blocks: `dashboard-overview` (KPI stat cards), `dashboard-table`
(search + sort + paginated table), `marketing-hero` (headline + CTAs + trust badges), and
`marketing-features` (feature grid). All use only `@cascivo/react` + `@cascivo/core` signals.

**Architecture:** Same file structure as T2. Sorting and filtering in `dashboard-table` are
driven by signals — no React state. The marketing blocks are purely presentational (no signals
needed). CSS uses `@container` for responsive layouts throughout.

**Tech Stack:** `@cascivo/core` (useSignal, useSignals, useComputed), `@cascivo/react` (Button,
Input, Badge, Card), Vitest + @testing-library/react.

---

## Files

- Create: `packages/components/src/blocks/dashboard-overview/` (4 files)
- Create: `packages/components/src/blocks/dashboard-table/` (4 files)
- Create: `packages/components/src/blocks/marketing-hero/` (4 files)
- Create: `packages/components/src/blocks/marketing-features/` (4 files)

---

### Task 1: `dashboard-overview` — tests first

**Files:**

- Create: `packages/components/src/blocks/dashboard-overview/dashboard-overview.test.tsx`

- [ ] **Step 1: Write the test file**

  Create `packages/components/src/blocks/dashboard-overview/dashboard-overview.test.tsx`:

  ```tsx
  import { render, screen } from '@testing-library/react'
  import { describe, expect, it } from 'vitest'
  import { DashboardOverview } from './dashboard-overview'

  describe('DashboardOverview', () => {
    it('renders welcome heading', () => {
      render(<DashboardOverview />)
      expect(screen.getByRole('heading', { level: 1 })).toBeInTheDocument()
    })

    it('renders four stat cards', () => {
      render(<DashboardOverview />)
      // Each stat card has a data-testid="stat-card"
      expect(screen.getAllByTestId('stat-card')).toHaveLength(4)
    })

    it('renders Revenue, Active Users, Orders, Conversion Rate labels', () => {
      render(<DashboardOverview />)
      expect(screen.getByText(/revenue/i)).toBeInTheDocument()
      expect(screen.getByText(/active users/i)).toBeInTheDocument()
      expect(screen.getByText(/orders/i)).toBeInTheDocument()
      expect(screen.getByText(/conversion rate/i)).toBeInTheDocument()
    })
  })
  ```

- [ ] **Step 2: Run — confirm FAIL**

  Run: `pnpm exec vp run @cascivo/components#test -- dashboard-overview.test.tsx`
  Expected: FAIL.

---

### Task 2: `dashboard-overview` — implement

**Files:**

- Create: `packages/components/src/blocks/dashboard-overview/dashboard-overview.tsx`
- Create: `packages/components/src/blocks/dashboard-overview/dashboard-overview.module.css`

- [ ] **Step 1: Create CSS module**

  Create `packages/components/src/blocks/dashboard-overview/dashboard-overview.module.css`:

  ```css
  @layer cascade.blocks.dashboard-overview {
    .root {
      padding: var(--cascivo-space-8);
      background: var(--cascivo-color-surface);
      min-block-size: 100vh;
    }

    .header {
      margin-block-end: var(--cascivo-space-8);
    }

    .heading {
      font-size: var(--cascivo-text-3xl);
      font-weight: var(--cascivo-font-bold);
      margin-block-end: var(--cascivo-space-1);
    }

    .date {
      font-size: var(--cascivo-text-sm);
      color: var(--cascivo-color-foreground-muted);
    }

    .grid {
      container-type: inline-size;
      display: grid;
      grid-template-columns: 1fr;
      gap: var(--cascivo-space-4);
    }

    @container (min-inline-size: 40rem) {
      .grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @container (min-inline-size: 64rem) {
      .grid {
        grid-template-columns: repeat(4, 1fr);
      }
    }

    .stat-card {
      padding: var(--cascivo-space-6);
    }

    .stat-icon {
      display: flex;
      align-items: center;
      justify-content: center;
      inline-size: 2.5rem;
      block-size: 2.5rem;
      border-radius: var(--cascivo-radius-md);
      background: var(--cascivo-color-accent-subtle);
      color: var(--cascivo-color-accent);
      margin-block-end: var(--cascivo-space-4);
    }

    .stat-label {
      font-size: var(--cascivo-text-sm);
      color: var(--cascivo-color-foreground-muted);
      margin-block-end: var(--cascivo-space-1);
    }

    .stat-value {
      font-size: var(--cascivo-text-2xl);
      font-weight: var(--cascivo-font-bold);
      margin-block-end: var(--cascivo-space-2);
    }

    .stat-delta {
      display: flex;
      align-items: center;
      gap: var(--cascivo-space-1-5);
      font-size: var(--cascivo-text-sm);
    }

    .stat-trend {
      color: var(--cascivo-color-foreground-muted);
    }
  }
  ```

- [ ] **Step 2: Create block component**

  Create `packages/components/src/blocks/dashboard-overview/dashboard-overview.tsx`:

  ```tsx
  import { useSignals } from '@cascivo/core'
  import { Badge } from '@cascivo/react/badge'
  import { Card } from '@cascivo/react/card'
  import styles from './dashboard-overview.module.css'

  type Stat = {
    label: string
    value: string
    delta: string
    trend: 'up' | 'down'
    icon: string
  }

  const STATS: Stat[] = [
    { label: 'Revenue', value: '$48,295', delta: '+12.5%', trend: 'up', icon: '💰' },
    { label: 'Active Users', value: '2,847', delta: '+8.1%', trend: 'up', icon: '👥' },
    { label: 'Orders', value: '1,204', delta: '-3.2%', trend: 'down', icon: '📦' },
    { label: 'Conversion Rate', value: '3.6%', delta: '+0.4%', trend: 'up', icon: '📈' },
  ]

  export function DashboardOverview() {
    useSignals()
    const today = new Date().toLocaleDateString('en-US', {
      weekday: 'long',
      year: 'numeric',
      month: 'long',
      day: 'numeric',
    })

    return (
      <div className={styles.root}>
        <header className={styles.header}>
          <h1 className={styles.heading}>Good morning</h1>
          <p className={styles.date}>{today}</p>
        </header>

        <div className={styles.grid}>
          {STATS.map((stat) => (
            <Card key={stat.label} data-testid="stat-card" className={styles.statCard}>
              <div className={styles.statIcon} aria-hidden="true">
                {stat.icon}
              </div>
              <p className={styles.statLabel}>{stat.label}</p>
              <p className={styles.statValue}>{stat.value}</p>
              <div className={styles.statDelta}>
                <Badge variant={stat.trend === 'up' ? 'success' : 'destructive'}>
                  {stat.delta}
                </Badge>
                <span className={styles.statTrend}>vs last month</span>
              </div>
            </Card>
          ))}
        </div>
      </div>
    )
  }
  ```

- [ ] **Step 3: Run tests — expect PASS**

  Run: `pnpm exec vp run @cascivo/components#test -- dashboard-overview.test.tsx`
  Expected: all 3 tests PASS.

- [ ] **Step 4: Grep for banned hooks**

  Run: `grep -n 'useState\|useEffect\|useContext' packages/components/src/blocks/dashboard-overview/dashboard-overview.tsx`
  Expected: no output.

- [ ] **Step 5: Create meta file**

  Create `packages/components/src/blocks/dashboard-overview/dashboard-overview.meta.ts`:

  ```ts
  import type { BlockMeta } from '../types'

  export const meta: BlockMeta = {
    name: 'dashboard-overview',
    displayName: 'Dashboard Overview',
    description: 'Welcome header with four KPI stat cards (revenue, users, orders, conversion).',
    category: 'dashboard',
    tags: ['dashboard', 'stats', 'kpi', 'cards', 'overview'],
    screenshot: {
      light: '/blocks/screenshots/dashboard-overview-light.png',
      dark: '/blocks/screenshots/dashboard-overview-dark.png',
    },
  }
  ```

- [ ] **Step 6: Commit**

  ```bash
  git add packages/components/src/blocks/dashboard-overview/
  git commit -m "feat(blocks): add dashboard-overview block"
  ```

---

### Task 3: `dashboard-table` — tests first

**Files:**

- Create: `packages/components/src/blocks/dashboard-table/dashboard-table.test.tsx`

- [ ] **Step 1: Write the test file**

  Create `packages/components/src/blocks/dashboard-table/dashboard-table.test.tsx`:

  ```tsx
  import { render, screen } from '@testing-library/react'
  import userEvent from '@testing-library/user-event'
  import { describe, expect, it } from 'vitest'
  import { DashboardTable } from './dashboard-table'

  describe('DashboardTable', () => {
    it('renders table with data rows', () => {
      render(<DashboardTable />)
      expect(screen.getByRole('table')).toBeInTheDocument()
      expect(screen.getAllByRole('row').length).toBeGreaterThan(1)
    })

    it('renders search input', () => {
      render(<DashboardTable />)
      expect(screen.getByRole('searchbox')).toBeInTheDocument()
    })

    it('filters rows when search input changes', async () => {
      const user = userEvent.setup()
      render(<DashboardTable />)
      const search = screen.getByRole('searchbox')
      // First row is Alice — search "alice" should leave only her row
      await user.type(search, 'alice')
      const dataRows = screen.getAllByRole('row').filter((r) => r.closest('tbody') !== null)
      expect(dataRows.length).toBe(1)
      expect(dataRows[0]).toHaveTextContent(/alice/i)
    })

    it('renders pagination buttons', () => {
      render(<DashboardTable />)
      expect(screen.getByRole('button', { name: /previous/i })).toBeInTheDocument()
      expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument()
    })

    it('renders export button', () => {
      render(<DashboardTable />)
      expect(screen.getByRole('button', { name: /export/i })).toBeInTheDocument()
    })
  })
  ```

- [ ] **Step 2: Run — confirm FAIL**

  Run: `pnpm exec vp run @cascivo/components#test -- dashboard-table.test.tsx`
  Expected: FAIL.

---

### Task 4: `dashboard-table` — implement

**Files:**

- Create: `packages/components/src/blocks/dashboard-table/dashboard-table.tsx`
- Create: `packages/components/src/blocks/dashboard-table/dashboard-table.module.css`

- [ ] **Step 1: Create CSS module**

  Create `packages/components/src/blocks/dashboard-table/dashboard-table.module.css`:

  ```css
  @layer cascade.blocks.dashboard-table {
    .root {
      padding: var(--cascivo-space-8);
      background: var(--cascivo-color-surface);
      min-block-size: 100vh;
    }

    .toolbar {
      display: flex;
      align-items: center;
      gap: var(--cascivo-space-3);
      margin-block-end: var(--cascivo-space-6);
      flex-wrap: wrap;
    }

    .toolbar-title {
      font-size: var(--cascivo-text-xl);
      font-weight: var(--cascivo-font-semibold);
      flex: 1;
    }

    .table-wrapper {
      overflow-x: auto;
      border: 1px solid var(--cascivo-color-border);
      border-radius: var(--cascivo-radius-md);
    }

    .table {
      inline-size: 100%;
      border-collapse: collapse;
      font-size: var(--cascivo-text-sm);
    }

    .table thead {
      background: var(--cascivo-color-surface-raised);
    }

    .table th {
      padding: var(--cascivo-space-3) var(--cascivo-space-4);
      text-align: start;
      font-weight: var(--cascivo-font-medium);
      color: var(--cascivo-color-foreground-muted);
      cursor: pointer;
      user-select: none;
      white-space: nowrap;
    }

    .table th:hover {
      color: var(--cascivo-color-foreground);
    }

    .table td {
      padding: var(--cascivo-space-3) var(--cascivo-space-4);
      border-block-start: 1px solid var(--cascivo-color-border);
    }

    .table tbody tr:hover {
      background: var(--cascivo-color-surface-raised);
    }

    .pagination {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-block-start: var(--cascivo-space-4);
      font-size: var(--cascivo-text-sm);
      color: var(--cascivo-color-foreground-muted);
    }

    .pagination-controls {
      display: flex;
      gap: var(--cascivo-space-2);
    }
  }
  ```

- [ ] **Step 2: Create block component**

  Create `packages/components/src/blocks/dashboard-table/dashboard-table.tsx`:

  ```tsx
  import { useComputed, useSignal, useSignals } from '@cascivo/core'
  import { Badge } from '@cascivo/react/badge'
  import { Button } from '@cascivo/react/button'
  import { Input } from '@cascivo/react/input'
  import styles from './dashboard-table.module.css'

  type Row = {
    id: string
    name: string
    email: string
    status: 'active' | 'inactive' | 'pending'
    date: string
    amount: string
  }

  const ALL_ROWS: Row[] = [
    {
      id: '1',
      name: 'Alice Johnson',
      email: 'alice@example.com',
      status: 'active',
      date: '2026-06-01',
      amount: '$240.00',
    },
    {
      id: '2',
      name: 'Bob Smith',
      email: 'bob@example.com',
      status: 'pending',
      date: '2026-06-03',
      amount: '$120.50',
    },
    {
      id: '3',
      name: 'Carol White',
      email: 'carol@example.com',
      status: 'active',
      date: '2026-06-05',
      amount: '$895.00',
    },
    {
      id: '4',
      name: 'David Brown',
      email: 'david@example.com',
      status: 'inactive',
      date: '2026-06-07',
      amount: '$60.00',
    },
    {
      id: '5',
      name: 'Eve Davis',
      email: 'eve@example.com',
      status: 'active',
      date: '2026-06-09',
      amount: '$340.00',
    },
    {
      id: '6',
      name: 'Frank Miller',
      email: 'frank@example.com',
      status: 'pending',
      date: '2026-06-11',
      amount: '$180.75',
    },
    {
      id: '7',
      name: 'Grace Wilson',
      email: 'grace@example.com',
      status: 'active',
      date: '2026-06-13',
      amount: '$520.00',
    },
  ]

  const PAGE_SIZE = 5

  const STATUS_VARIANT: Record<Row['status'], 'success' | 'warning' | 'neutral'> = {
    active: 'success',
    pending: 'warning',
    inactive: 'neutral',
  }

  export function DashboardTable() {
    useSignals()
    const query = useSignal('')
    const page = useSignal(0)
    const sortCol = useSignal<keyof Row>('date')
    const sortAsc = useSignal(true)

    const filtered = useComputed(() => {
      const q = query.value.toLowerCase()
      return ALL_ROWS.filter(
        (r) =>
          !q ||
          r.name.toLowerCase().includes(q) ||
          r.email.toLowerCase().includes(q) ||
          r.status.includes(q),
      )
    })

    const sorted = useComputed(() => {
      const col = sortCol.value
      const asc = sortAsc.value
      return [...filtered.value].sort((a, b) => {
        const cmp = a[col] < b[col] ? -1 : a[col] > b[col] ? 1 : 0
        return asc ? cmp : -cmp
      })
    })

    const pageCount = useComputed(() => Math.ceil(sorted.value.length / PAGE_SIZE))
    const rows = useComputed(() =>
      sorted.value.slice(page.value * PAGE_SIZE, (page.value + 1) * PAGE_SIZE),
    )

    function toggleSort(col: keyof Row) {
      if (sortCol.value === col) {
        sortAsc.value = !sortAsc.value
      } else {
        sortCol.value = col
        sortAsc.value = true
      }
      page.value = 0
    }

    function handleSearch(e: React.ChangeEvent<HTMLInputElement>) {
      query.value = e.target.value
      page.value = 0
    }

    const colLabel = (col: keyof Row, label: string) =>
      `${label}${sortCol.value === col ? (sortAsc.value ? ' ↑' : ' ↓') : ''}`

    return (
      <div className={styles.root}>
        <div className={styles.toolbar}>
          <h1 className={styles.toolbarTitle}>Customers</h1>
          <Input
            role="searchbox"
            type="search"
            placeholder="Search customers…"
            value={query.value}
            onChange={handleSearch}
          />
          <Button variant="ghost" onClick={() => {}}>
            Export
          </Button>
        </div>

        <div className={styles.tableWrapper}>
          <table className={styles.table}>
            <thead>
              <tr>
                <th onClick={() => toggleSort('name')}>{colLabel('name', 'Name')}</th>
                <th onClick={() => toggleSort('status')}>{colLabel('status', 'Status')}</th>
                <th onClick={() => toggleSort('email')}>{colLabel('email', 'Email')}</th>
                <th onClick={() => toggleSort('date')}>{colLabel('date', 'Date')}</th>
                <th onClick={() => toggleSort('amount')}>{colLabel('amount', 'Amount')}</th>
              </tr>
            </thead>
            <tbody>
              {rows.value.map((row) => (
                <tr key={row.id}>
                  <td>{row.name}</td>
                  <td>
                    <Badge variant={STATUS_VARIANT[row.status]}>{row.status}</Badge>
                  </td>
                  <td>{row.email}</td>
                  <td>{row.date}</td>
                  <td>{row.amount}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>

        <div className={styles.pagination}>
          <span>
            Page {page.value + 1} of {pageCount.value || 1}
          </span>
          <div className={styles.paginationControls}>
            <Button
              variant="ghost"
              size="sm"
              disabled={page.value === 0}
              onClick={() => {
                page.value -= 1
              }}
            >
              Previous
            </Button>
            <Button
              variant="ghost"
              size="sm"
              disabled={page.value >= pageCount.value - 1}
              onClick={() => {
                page.value += 1
              }}
            >
              Next
            </Button>
          </div>
        </div>
      </div>
    )
  }
  ```

- [ ] **Step 3: Run tests — expect PASS**

  Run: `pnpm exec vp run @cascivo/components#test -- dashboard-table.test.tsx`
  Expected: all 5 tests PASS.

- [ ] **Step 4: Create meta file**

  Create `packages/components/src/blocks/dashboard-table/dashboard-table.meta.ts`:

  ```ts
  import type { BlockMeta } from '../types'

  export const meta: BlockMeta = {
    name: 'dashboard-table',
    displayName: 'Dashboard Table',
    description: 'Searchable, sortable, paginated data table with export button.',
    category: 'dashboard',
    tags: ['dashboard', 'table', 'data', 'search', 'sort', 'pagination'],
    screenshot: {
      light: '/blocks/screenshots/dashboard-table-light.png',
      dark: '/blocks/screenshots/dashboard-table-dark.png',
    },
  }
  ```

- [ ] **Step 5: Grep for banned hooks**

  Run: `grep -n 'useState\|useEffect\|useContext' packages/components/src/blocks/dashboard-table/dashboard-table.tsx`
  Expected: no output.

- [ ] **Step 6: Commit**

  ```bash
  git add packages/components/src/blocks/dashboard-table/
  git commit -m "feat(blocks): add dashboard-table block"
  ```

---

### Task 5: `marketing-hero` — tests first

**Files:**

- Create: `packages/components/src/blocks/marketing-hero/marketing-hero.test.tsx`

- [ ] **Step 1: Write the test file**

  Create `packages/components/src/blocks/marketing-hero/marketing-hero.test.tsx`:

  ```tsx
  import { render, screen } from '@testing-library/react'
  import { describe, expect, it } from 'vitest'
  import { MarketingHero } from './marketing-hero'

  describe('MarketingHero', () => {
    it('renders a primary heading', () => {
      render(<MarketingHero />)
      expect(screen.getByRole('heading', { level: 1 })).toBeInTheDocument()
    })

    it('renders primary and ghost CTA buttons', () => {
      render(<MarketingHero />)
      const buttons = screen.getAllByRole('button')
      expect(buttons.length).toBeGreaterThanOrEqual(2)
    })

    it('renders three trust badges', () => {
      render(<MarketingHero />)
      expect(screen.getAllByTestId('trust-badge')).toHaveLength(3)
    })
  })
  ```

- [ ] **Step 2: Run — confirm FAIL**

  Run: `pnpm exec vp run @cascivo/components#test -- marketing-hero.test.tsx`
  Expected: FAIL.

---

### Task 6: `marketing-hero` — implement

**Files:**

- Create: `packages/components/src/blocks/marketing-hero/marketing-hero.tsx`
- Create: `packages/components/src/blocks/marketing-hero/marketing-hero.module.css`

- [ ] **Step 1: Create CSS module**

  Create `packages/components/src/blocks/marketing-hero/marketing-hero.module.css`:

  ```css
  @layer cascade.blocks.marketing-hero {
    .root {
      padding-block: var(--cascivo-space-24);
      padding-inline: var(--cascivo-space-6);
      text-align: center;
      background: var(--cascivo-color-surface);
    }

    .eyebrow {
      display: inline-flex;
      align-items: center;
      gap: var(--cascivo-space-2);
      margin-block-end: var(--cascivo-space-6);
    }

    .heading {
      font-size: var(--cascivo-text-5xl);
      font-weight: var(--cascivo-font-extrabold);
      line-height: var(--cascivo-leading-tight);
      letter-spacing: var(--cascivo-tracking-tight);
      max-inline-size: 40rem;
      margin-inline: auto;
      margin-block-end: var(--cascivo-space-6);
    }

    .subtitle {
      font-size: var(--cascivo-text-xl);
      color: var(--cascivo-color-foreground-muted);
      max-inline-size: 32rem;
      margin-inline: auto;
      margin-block-end: var(--cascivo-space-10);
      line-height: var(--cascivo-leading-relaxed);
    }

    .cta-row {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: var(--cascivo-space-4);
      flex-wrap: wrap;
      margin-block-end: var(--cascivo-space-10);
    }

    .badges-row {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: var(--cascivo-space-3);
      flex-wrap: wrap;
    }
  }
  ```

- [ ] **Step 2: Create block component**

  Create `packages/components/src/blocks/marketing-hero/marketing-hero.tsx`:

  ```tsx
  import { Badge } from '@cascivo/react/badge'
  import { Button } from '@cascivo/react/button'
  import styles from './marketing-hero.module.css'

  const TRUST_BADGES = ['MIT licence', 'Zero dependencies', 'WCAG 2.1 AA']

  export function MarketingHero() {
    return (
      <section className={styles.root}>
        <div className={styles.eyebrow}>
          <Badge variant="accent">New — v29 Blocks</Badge>
        </div>

        <h1 className={styles.heading}>Build beautiful UIs without the boilerplate</h1>

        <p className={styles.subtitle}>
          cascivo is the CSS-native, signal-driven design system for React. Copy production-ready
          components and blocks into your project and own them completely.
        </p>

        <div className={styles.ctaRow}>
          <Button variant="primary" size="lg">
            Get started
          </Button>
          <Button variant="ghost" size="lg">
            View on GitHub
          </Button>
        </div>

        <div className={styles.badgesRow}>
          {TRUST_BADGES.map((label) => (
            <Badge key={label} variant="neutral" data-testid="trust-badge">
              {label}
            </Badge>
          ))}
        </div>
      </section>
    )
  }
  ```

- [ ] **Step 3: Run tests — expect PASS**

  Run: `pnpm exec vp run @cascivo/components#test -- marketing-hero.test.tsx`
  Expected: all 3 tests PASS.

- [ ] **Step 4: Create meta file**

  Create `packages/components/src/blocks/marketing-hero/marketing-hero.meta.ts`:

  ```ts
  import type { BlockMeta } from '../types'

  export const meta: BlockMeta = {
    name: 'marketing-hero',
    displayName: 'Hero Section',
    description: 'Full-width hero with headline, subtitle, CTA pair, and trust badges.',
    category: 'marketing',
    tags: ['marketing', 'hero', 'landing', 'cta'],
    screenshot: {
      light: '/blocks/screenshots/marketing-hero-light.png',
      dark: '/blocks/screenshots/marketing-hero-dark.png',
    },
  }
  ```

- [ ] **Step 5: Commit**

  ```bash
  git add packages/components/src/blocks/marketing-hero/
  git commit -m "feat(blocks): add marketing-hero block"
  ```

---

### Task 7: `marketing-features` — tests first

**Files:**

- Create: `packages/components/src/blocks/marketing-features/marketing-features.test.tsx`

- [ ] **Step 1: Write the test file**

  Create `packages/components/src/blocks/marketing-features/marketing-features.test.tsx`:

  ```tsx
  import { render, screen } from '@testing-library/react'
  import { describe, expect, it } from 'vitest'
  import { MarketingFeatures } from './marketing-features'

  describe('MarketingFeatures', () => {
    it('renders section heading', () => {
      render(<MarketingFeatures />)
      expect(screen.getByRole('heading', { level: 2 })).toBeInTheDocument()
    })

    it('renders six feature cells', () => {
      render(<MarketingFeatures />)
      expect(screen.getAllByTestId('feature-cell')).toHaveLength(6)
    })

    it('each feature cell has a title and description', () => {
      render(<MarketingFeatures />)
      const cells = screen.getAllByTestId('feature-cell')
      cells.forEach((cell) => {
        expect(cell.querySelector('h3')).not.toBeNull()
        expect(cell.querySelector('p')).not.toBeNull()
      })
    })
  })
  ```

- [ ] **Step 2: Run — confirm FAIL**

  Run: `pnpm exec vp run @cascivo/components#test -- marketing-features.test.tsx`
  Expected: FAIL.

---

### Task 8: `marketing-features` — implement

**Files:**

- Create: `packages/components/src/blocks/marketing-features/marketing-features.tsx`
- Create: `packages/components/src/blocks/marketing-features/marketing-features.module.css`

- [ ] **Step 1: Create CSS module**

  Create `packages/components/src/blocks/marketing-features/marketing-features.module.css`:

  ```css
  @layer cascade.blocks.marketing-features {
    .root {
      padding-block: var(--cascivo-space-20);
      padding-inline: var(--cascivo-space-6);
      background: var(--cascivo-color-surface-raised);
    }

    .header {
      text-align: center;
      margin-block-end: var(--cascivo-space-12);
    }

    .heading {
      font-size: var(--cascivo-text-3xl);
      font-weight: var(--cascivo-font-bold);
      margin-block-end: var(--cascivo-space-3);
    }

    .subtitle {
      font-size: var(--cascivo-text-lg);
      color: var(--cascivo-color-foreground-muted);
      max-inline-size: 36rem;
      margin-inline: auto;
    }

    .grid {
      container-type: inline-size;
      display: grid;
      grid-template-columns: 1fr;
      gap: var(--cascivo-space-8);
      max-inline-size: 72rem;
      margin-inline: auto;
    }

    @container (min-inline-size: 40rem) {
      .grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @container (min-inline-size: 64rem) {
      .grid {
        grid-template-columns: repeat(3, 1fr);
      }
    }

    .cell {
      display: flex;
      flex-direction: column;
      gap: var(--cascivo-space-3);
    }

    .cell-icon {
      display: flex;
      align-items: center;
      justify-content: center;
      inline-size: 3rem;
      block-size: 3rem;
      border-radius: var(--cascivo-radius-md);
      background: var(--cascivo-color-accent-subtle);
      font-size: 1.5rem;
    }

    .cell-title {
      font-size: var(--cascivo-text-lg);
      font-weight: var(--cascivo-font-semibold);
    }

    .cell-description {
      font-size: var(--cascivo-text-sm);
      color: var(--cascivo-color-foreground-muted);
      line-height: var(--cascivo-leading-relaxed);
    }
  }
  ```

- [ ] **Step 2: Create block component**

  Create `packages/components/src/blocks/marketing-features/marketing-features.tsx`:

  ```tsx
  import styles from './marketing-features.module.css'

  type Feature = { icon: string; title: string; description: string }

  const FEATURES: Feature[] = [
    {
      icon: '⚡',
      title: 'Signal-driven',
      description:
        'Preact Signals power every component. No useState, no re-renders, zero unnecessary DOM updates.',
    },
    {
      icon: '🎨',
      title: 'CSS-native',
      description:
        'Modern CSS only — @layer, @container, :has(), custom properties. No Tailwind, no CSS-in-JS.',
    },
    {
      icon: '♿',
      title: 'WCAG 2.1 AA',
      description:
        'Every component ships with correct ARIA roles, keyboard navigation, and focus management.',
    },
    {
      icon: '📋',
      title: 'Copy-paste model',
      description:
        'Components live in your project. You own the code. No vendor lock-in, no version surprises.',
    },
    {
      icon: '🤖',
      title: 'AI-first',
      description:
        'Machine-readable component manifests, MCP server, and Claude Code skills included out of the box.',
    },
    {
      icon: '🌗',
      title: 'Beautiful themes',
      description:
        'Light, dark, and warm themes ship as pure CSS. Scope any theme to any container with data-theme.',
    },
  ]

  export function MarketingFeatures() {
    return (
      <section className={styles.root}>
        <div className={styles.header}>
          <h2 className={styles.heading}>Everything you need</h2>
          <p className={styles.subtitle}>
            cascivo is designed for developers who care about quality, performance, and
            maintainability.
          </p>
        </div>

        <div className={styles.grid}>
          {FEATURES.map((f) => (
            <article key={f.title} className={styles.cell} data-testid="feature-cell">
              <div className={styles.cellIcon} aria-hidden="true">
                {f.icon}
              </div>
              <h3 className={styles.cellTitle}>{f.title}</h3>
              <p className={styles.cellDescription}>{f.description}</p>
            </article>
          ))}
        </div>
      </section>
    )
  }
  ```

- [ ] **Step 3: Run tests — expect PASS**

  Run: `pnpm exec vp run @cascivo/components#test -- marketing-features.test.tsx`
  Expected: all 3 tests PASS.

- [ ] **Step 4: Create meta file**

  Create `packages/components/src/blocks/marketing-features/marketing-features.meta.ts`:

  ```ts
  import type { BlockMeta } from '../types'

  export const meta: BlockMeta = {
    name: 'marketing-features',
    displayName: 'Feature Grid',
    description: 'Three-column responsive feature grid with icon, title, and description per cell.',
    category: 'marketing',
    tags: ['marketing', 'features', 'grid', 'icons'],
    screenshot: {
      light: '/blocks/screenshots/marketing-features-light.png',
      dark: '/blocks/screenshots/marketing-features-dark.png',
    },
  }
  ```

- [ ] **Step 5: Run full test suite**

  Run: `pnpm exec vp run @cascivo/components#test`
  Expected: exits 0.

- [ ] **Step 6: Run vp check**

  Run: `pnpm exec vp check`
  Expected: exits 0.

- [ ] **Step 7: Commit T3**

  ```bash
  git add packages/components/src/blocks/marketing-features/
  git commit -m "feat(blocks): add marketing-features block"
  ```
