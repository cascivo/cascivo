// Hand-maintained: the subset of @cascivo/react components <CascadeView /> can render
// by name. Add an import + a `componentMap` entry to expose a new component to the
// render engine. (No generator writes this file.)
import type React from 'react'
import {
  Accordion,
  Alert,
  AutoGrid,
  Avatar,
  Badge,
  Breadcrumb,
  Button,
  Card,
  Center,
  Checkbox,
  Columns,
  Combobox,
  CommandMenu,
  DataTable,
  DatePicker,
  Dropdown,
  EmptyState,
  FileUploader,
  Flex,
  Form,
  Grid,
  GridItem,
  Header,
  HeaderPanel,
  Input,
  InputGroup,
  Kbd,
  Link,
  Modal,
  NumberInput,
  OverflowMenu,
  Pagination,
  ProgressBar,
  ProgressIndicator,
  Radio,
  Search,
  Select,
  Separator,
  ShellHeader,
  SideNav,
  Skeleton,
  Slider,
  Spacer,
  Spinner,
  Switcher,
  RadioCard,
  RadioCardGroup,
  CheckboxCard,
  Tabs,
  Tag,
  Textarea,
  TimePicker,
  ToastProvider,
  Toggle,
  Tooltip,
} from '@cascivo/react'

type AnyComponent = React.ComponentType<Record<string, unknown>>
const cast = (c: unknown) => c as AnyComponent

export const componentMap: Record<string, AnyComponent> = {
  Accordion: cast(Accordion),
  Alert: cast(Alert),
  AutoGrid: cast(AutoGrid),
  Avatar: cast(Avatar),
  Badge: cast(Badge),
  Breadcrumb: cast(Breadcrumb),
  Button: cast(Button),
  Card: cast(Card),
  Center: cast(Center),
  Checkbox: cast(Checkbox),
  Columns: cast(Columns),
  Combobox: cast(Combobox),
  CommandMenu: cast(CommandMenu),
  DataTable: cast(DataTable),
  DatePicker: cast(DatePicker),
  Dropdown: cast(Dropdown),
  EmptyState: cast(EmptyState),
  FileUploader: cast(FileUploader),
  Flex: cast(Flex),
  Form: cast(Form),
  Grid: cast(Grid),
  GridItem: cast(GridItem),
  Header: cast(Header),
  HeaderPanel: cast(HeaderPanel),
  Input: cast(Input),
  InputGroup: cast(InputGroup),
  Kbd: cast(Kbd),
  Link: cast(Link),
  Modal: cast(Modal),
  NumberInput: cast(NumberInput),
  OverflowMenu: cast(OverflowMenu),
  Pagination: cast(Pagination),
  ProgressBar: cast(ProgressBar),
  ProgressIndicator: cast(ProgressIndicator),
  Radio: cast(Radio),
  Search: cast(Search),
  Select: cast(Select),
  Separator: cast(Separator),
  ShellHeader: cast(ShellHeader),
  SideNav: cast(SideNav),
  Skeleton: cast(Skeleton),
  Slider: cast(Slider),
  Spacer: cast(Spacer),
  Spinner: cast(Spinner),
  Switcher: cast(Switcher),
  RadioCard: cast(RadioCard),
  RadioCardGroup: cast(RadioCardGroup),
  CheckboxCard: cast(CheckboxCard),
  Tabs: cast(Tabs),
  Tag: cast(Tag),
  Textarea: cast(Textarea),
  TimePicker: cast(TimePicker),
  Toast: cast(ToastProvider),
  Toggle: cast(Toggle),
  Tooltip: cast(Tooltip),
}
