# v18 Tranche 7 — Coverage page, claims, and Definition of Done

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. ALSO use compound-engineering:frontend-design for the coverage page visual
> quality + screenshot verification at mobile breakpoints. Steps use checkbox (`- [ ]`) syntax.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - `apps/docs/public/parity.json` is generated (T1) and reflects everything shipped in T5/T6 (derived
>   coverage — not hand-typed).
> - `apps/docs/src/pages/WhyCascadePage.tsx` holds the numbered claims with receipts. Claims 34–36 land
>   here. Re-confirm the `Claim` shape (`number`, `title`, `substance`, `receipt`).
> - Landing-subpage / docs-page patterns from v16/v17 (e.g. `AccessibilityPage`, `PerformancePage`) are
>   the template if the coverage page goes on the landing; otherwise mirror an existing docs page.
> - The mobile overflow harness (`apps/landing/test/mobile.spec.ts` or equivalent) gates new landing
>   pages at 320/375/390/414 — wire the page in if it's a landing page.

**Goal:** A coverage page that renders cascivo vs shadcn/ui vs Carbon from `parity.json` (so it can't
overstate), claims 34–36 on `/why` with receipts, refreshed README/llms, and a fully-passing CLAUDE.md
gate including the new parity drift check. Close the roadmap.

**Architecture:** New coverage page in `apps/docs` (or `apps/landing` if it should be public-facing —
decide and note). Reads `apps/docs/public/parity.json`. Claims in `WhyCascadePage.tsx`. README/llms via
generators. `ROADMAP-V18.md` DoD checked.

**Tech stack:** unchanged. If the page reads a signal, `useSignals()` first; mobile-first CSS.

---

### Task 1: Coverage page

**Files:** Create the page (e.g. `apps/docs/src/pages/ParityPage.tsx` or
`apps/landing/src/pages/ParityPage.tsx`) + route + nav entry; CSS.

- [ ] **Step 1:** Decide placement (docs vs landing) — recommend landing if it's a marketing receipt,
      docs if it's reference. Note the decision. Register the route + nav link (mirror the v17 `/guides`
      wiring if landing).
- [ ] **Step 2:** Fetch/import `parity.json`; render two coverage tables (shadcn, Carbon) with
      covered/partial/gap status per row and the cascivo component (or note). Show the derived summary
      counts at the top ("cascivo covers N/58 shadcn components; M Carbon components"). Gaps link to their
      `factory-backlog.json` status where useful.
- [ ] **Step 3:** Mobile-first CSS: tables stack/scroll cleanly < 48rem; zero horizontal overflow at
      320–414px. If on landing, add the page to the mobile overflow harness and run it.
- [ ] **Step 4:** If the page reads any signal, `useSignals()` is the first statement; no
      `useState`/`useEffect`.

---

### Task 2: Claims 34–36 on /why

**Files:** Modify: `apps/docs/src/pages/WhyCascadePage.tsx`.

- [ ] **Step 1:** Add claim 34 ("Primitives, not just components") — substance: `@cascivo/core` ships
      `Slot`/`asChild`, `useControllableSignal`, `DismissableLayer`, `RovingFocus`, `Presence`,
      signal-driven. Receipt → the core package docs / coverage page.
- [ ] **Step 2:** Add claim 35 ("Measured parity, not implied") — substance: generated matrix scores
      cascivo vs shadcn (58) + Carbon; gaps are queued specs. Receipt → the coverage page.
- [ ] **Step 3:** Add claim 36 ("The catalog keeps closing the gap") — substance: every gap is a real
      backlog entry; the factory builds them on the v18 primitives. Receipt → the coverage page / backlog.
- [ ] **Step 4:** Confirm the receipts link to live, real targets (the coverage page route, not a stub).

---

### Task 3: Refresh generated surfaces

**Files:** via generators: README, `apps/docs/public/llms.txt`, `registry.json`, `parity.json`.

- [ ] **Step 1:** `pnpm regen` — confirm README component count, `llms.txt`, `registry.json`, and
      `parity.json` all reflect the new primitives + components. The primitives appear in `@cascivo/core`
      docs/exports; the components in the registry.
- [ ] **Step 2:** Confirm `git diff --exit-code` is clean after regen (the parity drift gate from T1 is
      now part of the pipeline).

---

### Task 4: Full gate + roadmap close

**Files:** Modify: `docs/ROADMAP-V18.md`.

- [ ] **Step 1:** Run the full CLAUDE.md gate end to end:
      `vp check` → `pnpm build` → `vp run -r check` → `pnpm test` → `pnpm regen` → `vp check --fix` →
      `git diff --exit-code`. All exit 0.
- [ ] **Step 2:** If the page is on landing, run the Playwright mobile harness at 320/375/390/414 — zero
      overflow including the coverage page.
- [ ] **Step 3:** Check every DoD box in `ROADMAP-V18.md`; for the components DoD, state honestly how many
      Tier-1/Tier-2 shipped vs deferred (the coverage page is the receipt). Add a Close-out section dated
      2026-06-14 (or actual date), set status → ✅ Shipped. Move any unfinished components to the Deferred
      list with their backlog status — do not silently drop them.
- [ ] **Step 4: Commit** — `docs(roadmap): v18 coverage page + claims 34–36 + close-out (v18-t7)`

---

## Tranche exit criteria

1. A coverage page renders cascivo vs shadcn vs Carbon from `parity.json` (derived, can't overstate),
   with zero mobile overflow if on landing.
2. Claims 34–36 are live on `/why` with receipts linking to the coverage page.
3. README/llms/registry/parity regenerated; `git diff --exit-code` clean (parity drift gate included).
4. Full CLAUDE.md gate exits 0; mobile harness passes if applicable.
5. `ROADMAP-V18.md` DoD boxes resolved (shipped/deferred stated honestly); status → ✅ Shipped with a
   close-out note.
   </content>
