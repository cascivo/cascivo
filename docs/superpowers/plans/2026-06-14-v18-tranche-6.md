# v18 Tranche 6 — Tier-2 parity components (via the factory)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. DRIVES THE DARK FACTORY — does not hand-write component source. Steps use
> checkbox (`- [ ]`) syntax.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - T1 queued these Tier-2 specs into `factory-backlog.json`: `navigation-menu`, `menubar`, `menu-button`,
>   `toggletip`, `resizable` (a.k.a. `splitter`), `drawer`, `tree-view`, `tile`, `structured-list`,
>   `contained-list`, `carousel`, `calendar`, `date-range-picker`, `color-picker`, `timeline`,
>   `data-list`, `native-select`, `item`, `code-snippet` (a.k.a. `code-block`). RE-READ specs + the
>   pending/review status of the pre-existing ones.
> - T2–T5 shipped: all primitives + the Tier-1 components (several Tier-2 reuse them — e.g.
>   `date-range-picker` builds on `calendar` + `popover`; menus reuse `RovingFocus` + `DismissableLayer` +
>   anchor positioning).
> - Same factory loop + component checklist as T5.

**Goal:** Generate and verify the Tier-2 parity components on the v18 primitives, exported from
`@cascivo/react`, with the matrix/registry regenerated. Any not completed stay honest gaps.

**Architecture:** identical to T5 — factory → `packages/components/src/<name>/` → `@cascivo/react` →
`pnpm regen`.

**Tech stack:** unchanged; components consume `@cascivo/core` primitives.

---

### Task 1: Menus + navigation (anchor + dismissable + roving)

**Files:** Factory → `packages/components/src/{navigation-menu,menubar,menu-button,toggletip}/`; export.

- [ ] **Step 1:** `Menu Button` — a button that opens a menu (uses `DismissableLayer` + anchor
      positioning + the existing `menu`). Verify checklist; export.
- [ ] **Step 2:** `Toggletip` — click-triggered popover-tooltip (`DismissableLayer` + anchor). Distinct
      from the hover `tooltip`. Verify; export.
- [ ] **Step 3:** `Menubar` — horizontal menu bar (`RovingFocus` across triggers + `DismissableLayer` +
      anchor per menu). Verify; export.
- [ ] **Step 4:** `Navigation Menu` — site-nav with flyout panels (`RovingFocus` + `DismissableLayer` +
      anchor/`Presence`). Verify; export.

---

### Task 2: Structural lists + panes

**Files:** Factory → `packages/components/src/{tree-view,structured-list,contained-list,data-list,resizable,scroll-area-deps?}/`; export.

- [ ] **Step 1:** `Tree View` — expandable/selectable tree (`RovingFocus` + `useControllableSignal`).
      Verify; export.
- [ ] **Step 2:** `Structured List` — Carbon-style row list, optional selectable (`RovingFocus`). Verify;
      export.
- [ ] **Step 3:** `Contained List` + `Data List` — list display surfaces; `Slot` where composition helps.
      Verify; export.
- [ ] **Step 4:** `Resizable` (splitter) — draggable split panes (`useControllableSignal` + pointer
      handling; no `useEffect` — pointer listeners via `useSignalEffect`). Verify; export.

---

### Task 3: Rich inputs + media

**Files:** Factory → `packages/components/src/{calendar,date-range-picker,color-picker,carousel,drawer,native-select,tile,timeline,code-snippet,item}/`; export.

- [ ] **Step 1:** `Calendar` — month grid (`RovingFocus` for date navigation + `useControllableSignal`).
      Verify; export. (Unblocks date-range.)
- [ ] **Step 2:** `Date Range Picker` — `Calendar` (range mode) inside a `Popover`. Verify; export.
- [ ] **Step 3:** `Color Picker` — hue/alpha/saturation controls (`useControllableSignal` + pointer
      handling via `useSignalEffect`). Verify; export.
- [ ] **Step 4:** `Carousel` — slides with `RovingFocus` + `useMediaQuery` for responsive count. Verify;
      export.
- [ ] **Step 5:** `Drawer` — edge-anchored panel using `Presence` + `DismissableLayer` + `useScrollLock`
      (no swipe physics — deferred per roadmap; this complements `sheet`). Verify; export.
- [ ] **Step 6:** `Native Select` (native `<select>` styled), `Tile` (selectable/expandable; `Slot` +
      `useControllableSignal`), `Timeline`, `Item` (generic item primitive; `Slot`), `Code Snippet`
      (inline/multiline/terminal; `useClipboard`). Verify each; export.

---

### Task 4: Regenerate + gate

- [ ] **Step 1:** `pnpm regen`; confirm `registry.json` + `parity.json` reflect every shipped Tier-2
      component as `covered` (derived). Components not completed remain `gap`/`partial` — do not overstate.
- [ ] **Step 2:** Full sub-gate: `vp check`, `pnpm build`, `vp run -r check`, `pnpm test`,
      `git diff --exit-code`. Fix drift.
- [ ] **Step 3:** Record the final shipped vs deferred list (feeds T7's honest coverage page).
- [ ] **Step 4: Commit** — `feat(components): tier-2 parity components on v18 primitives (v18-t6)`

---

## Tranche exit criteria

1. Tier-2 components are generated, pass the CLAUDE.md component checklist, and are exported from
   `@cascivo/react` — or are honestly left as gaps in the matrix (none marked covered without being
   built + exported).
2. Each consumes the intended v18 primitive(s); `Drawer` complements `sheet` (no duplicate); `Toggletip`
   is distinct from `tooltip`; `Date Range Picker` builds on `Calendar`.
3. `pnpm regen` ran; `registry.json` + `parity.json` updated; `git diff --exit-code` clean.
4. Full sub-gate (check, build, type check, test) green. The shipped/deferred list is recorded for T7.
</content>
