# App Shell

Two-column layout with collapsible sidebar, nav links, and content area.

## Install

Copy-paste the source (you own and can edit it):

```bash
npx cascivo add block/app-shell
```

_Copy-paste only — `App Shell` is not exported from `@cascivo/react`. Run the command above to own the source, or compose it from the exported primitives (`Flex`, `Grid`, `Heading`, …)._

## Category

`shell`

## Props

_No props._

## Dependencies

- `@cascivo/react`

## Tags

shell, layout, sidebar, navigation, responsive

---

_Generated from registry v0.14.0 on 2026-07-31. Docs track `main`; compare with https://cascivo.com/registry.json `.version`._
