# v38 — Tranche 5: Docs, Proposals & Final Gate

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Document the new theme in the canonical theme-proposals reference, regenerate any derived
artifacts, and run the **full** CI gate (parity, chart-CVD, fallback, breakpoint, drift, brand) so v38
ships clean. This is the closing tranche — it only describes/validates what T1–T4 produced.

**Architecture:** `docs/THEME-PROPOSALS.md` is the human reference describing each theme's
radius/shadow/border/accent character and per-theme caveats (it currently documents ten). The new theme is
added there with its palette + structure + animation notes and the font caveat. `pnpm regen` regenerates
any theme-derived listings; the drift gate must show no uncommitted diff afterward.

**Tech Stack:** Markdown + `pnpm regen` + the full `pnpm ready` gate.

---

## Files

- Modify: `docs/THEME-PROPOSALS.md`
- Modify (if `regen` touches them): generated theme listings / README sections / registry surfaces
- Verify: full gate

---

### Task 1: Document `cyberpunk` in THEME-PROPOSALS

**Files:** `docs/THEME-PROPOSALS.md`

- [ ] **Step 1:** Add a `cyberpunk` row to the theme comparison table (radius `0px`, shadow `hard offset +
      neon glow`, border `thick neon`, accent `neon magenta / cyan`, mood `brutalism × cyberpunk`).
- [ ] **Step 2:** Add a prose section (mirroring the `brutalist` / `terminal` sections) describing the palette
      (near-black base, magenta primary, cyan secondary, glow-tinted hard shadows), the brutalist structure it
      borrows, and the **opt-in CSS animations** (hard-snap motion character + scanline/glitch/flicker,
      reduced-motion-safe).
- [ ] **Step 3:** Update any "ten themes" count/wording in the doc to eleven; add `cyberpunk` to the font-caveat
      note (it inherits `--cascivo-font-sans`, same as `brutalist`/`terminal`).

### Task 2: Regenerate derived artifacts

- [ ] **Step 1:** `pnpm regen`. If any generated file (theme listing, package README section, registry surface)
      changes to include `cyberpunk`, that is expected — stage it.
- [ ] **Step 2:** Drift gate: `pnpm regen && pnpm exec vp check --fix && git diff --exit-code` → clean (commit
      anything `regen`/`--fix` modified).

### Task 3: Full gate

- [ ] **Step 1:** `pnpm exec vp check` (format + lint) → 0.
- [ ] **Step 2:** `pnpm build` → 0.
- [ ] **Step 3:** `pnpm exec vp run -r check` (type check) → 0.
- [ ] **Step 4:** `pnpm test` → 0, with `packages/themes` `parity.test.ts` and `chart-palette.test.ts` green for
      the new theme.
- [ ] **Step 5:** `pnpm breakpoint:check`, `pnpm fallback:check`, `pnpm brand:check` → all 0.
- [ ] **Step 6 (CI parity, optional but recommended for the animation/CSS changes):** `pnpm ready:ci` (cold
      cache, sequential build) → 0.

### Task 4: Final coverage sweep

- [ ] **Step 1:** `grep -rln "cyberpunk" packages apps docs` shows the theme file, both test lists, the themes
      `package.json`/README/`all.css`, CLI, Storybook, docs app (theme.ts/app.css/charts), **all five** landing
      surfaces, and the proposals doc. Anything theme-enumerating that lacks a hit is a missed registration —
      fix before closing.
- [ ] **Step 2:** Confirm no stale "ten themes" / 10-only enumerations remain:
      `grep -rni "ten themes" packages apps docs` → empty.

### Task 5: Commit T5

- [ ] **Step 1: Stage and commit**

  ```bash
  git add docs packages apps
  git commit -m "$(cat <<'EOF'
  docs(themes): document cyberpunk theme + regen + final v38 gate (v38 T5)

  Document the cyberpunk theme in THEME-PROPOSALS.md (palette, brutalist
  structure, opt-in CSS animations, font caveat) and bump the theme count to
  eleven. Regenerate derived theme listings; drift gate clean. Full gate
  green: parity + chart-CVD, fallback:check, breakpoint:check, brand:check,
  build, type check, tests. Coverage sweep confirms cyberpunk is registered
  on every theme-enumerating surface.

  Co-Authored-By: Claude <noreply@anthropic.com>
  EOF
  )"
  ```

### Task 6: Push the branch

- [ ] **Step 1:** `git push -u origin claude/dazzling-hamilton-5hb51g` (retry with exponential backoff on
      network failure). Do **not** open a PR unless explicitly asked.
