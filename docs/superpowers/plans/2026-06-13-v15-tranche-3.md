# v15 Tranche 3 — Package namespace rename (`@cascivo/*` → `@cascivo/*`)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.
>
> This is the **largest mechanical tranche**. It renames the npm scope and every import. The
> **CLAUDE.md vite-alias rule is mandatory here** — miss it and the apps fail to build. Token prefix
> is already `--cascivo-*` (T2). CLI/registry/MCP/external are still old (that's T4). Repo must end
> fully green.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - 24 `package.json` reference `@cascade-ui` (root is `@cascivo/monorepo`). 15 package dirs:
>   `ai, charts, cli, components, core, i18n, icons, layouts, mcp, react, registry, render, storage,
themes, tokens`. Confirm: `grep -rl '@cascade-ui' --include=package.json . | wc -l`.
> - 72 component `*.meta.ts` set `dependencies: ['@cascivo/core', …]`.
> - Vite alias blocks for dist-resolving packages (`core, storage, i18n, ai, render, icons`) live in
>   `apps/docs/vite.config.ts`, `apps/landing/vite.config.ts`, `apps/storybook/.storybook/main.ts`
>   (CLAUDE.md "Workspace package aliases" section). RE-READ that section before starting.
> - `pnpm-workspace.yaml` + tsconfig `paths` may reference the scope.

**Goal:** The npm scope is `@cascivo/*` everywhere: every `package.json` name + dependency, every
`import`/`from '@cascivo/…'`, every tsconfig `paths` entry, all three vite alias blocks, and 72
`meta.dependencies`. Zero `@cascivo/` references remain in source. Build, type check, and tests
are green. (`packages/cli` package-name and registry URLs are renamed in T4, but its imports of
other packages ARE renamed here.)

**Architecture:** Repo-wide scope find-replace `@cascivo/` → `@cascivo/`, plus the three vite
alias blocks and tsconfig paths, guarded by a grep gate and the three app builds.

**Tech stack:** unchanged.

---

### Task 1: Rename package identities

**Files:**

- Modify: every `packages/*/package.json` `name` field; root `package.json`
  (`@cascivo/monorepo` → `@cascivo/monorepo`)

- [ ] **Step 1:** Rename each publishable package `name`: `@cascivo/<x>` → `@cascivo/<x>` for
      `ai, charts, core, i18n, icons, layouts, mcp, react, render, storage, themes, tokens` (and
      `cli` — note its package name is `cascade`, handled in T4; here only its **dependencies** on
      other packages are renamed). `components`/`registry` aren't published but update their names
      for consistency.
- [ ] **Step 2:** Rename the root `@cascivo/monorepo` → `@cascivo/monorepo`.
- [ ] **Step 3:** In every `package.json`, rename `dependencies`/`peerDependencies`/`devDependencies`
      entries `@cascivo/*` → `@cascivo/*`. Keep version ranges and `workspace:*` specifiers as-is.

---

### Task 2: Rename all imports + meta dependencies + tsconfig paths

**Files:**

- Modify: all `.ts/.tsx/.mjs/.js` importing `@cascivo/*`; 72 `*.meta.ts`; tsconfigs;
  `pnpm-workspace.yaml` if it references the scope

- [ ] **Step 1:** Repo-wide replace `@cascivo/` → `@cascivo/` in all source imports
      (`import … from '@cascivo/x'`, dynamic `import('@cascivo/x')`, `require`).
- [ ] **Step 2:** Update the 72 component `meta.dependencies` arrays (`['@cascivo/core', …]`).
- [ ] **Step 3:** Update tsconfig `paths`/`references` and any `compilerOptions.paths` aliasing the
      scope to source.
- [ ] **Step 4: Commit** — `refactor(pkg): rename scope @cascivo/* → @cascivo/* (v15-t3)`

---

### Task 3: Rename the three vite alias blocks (CLAUDE.md rule)

**Files:**

- Modify: `apps/docs/vite.config.ts`, `apps/landing/vite.config.ts`,
  `apps/storybook/.storybook/main.ts`

- [ ] **Step 1:** In each, rename every `'@cascivo/<x>': resolve(root, 'packages/<x>/src/...')`
      alias key to `'@cascivo/<x>'`. The resolve **paths are unchanged** (still `packages/<x>/src`);
      only the alias key (the package name) changes. Cover all dist-resolving packages currently
      aliased (`core, storage, i18n, ai, render, icons`) plus any others present.
- [ ] **Step 2:** Verify each app builds **without a prior `pnpm build`** (this is exactly what the
      alias rule protects): `pnpm exec vp run @cascivo/docs#build @cascivo/landing#build @cascivo/storybook#build`
      (use the renamed task IDs). All succeed.
- [ ] **Step 3: Commit** — `fix(apps): rename vite source aliases to @cascivo/* (v15-t3)`

---

### Task 4: Reinstall + grep gate + full build/test

- [ ] **Step 1:** Reinstall so the workspace re-links under the new names: `pnpm install` (via `vp`).
      The lockfile updates — commit it.
- [ ] **Step 2: GATE —** `grep -rn '@cascade-ui' . | grep -v node_modules` returns **nothing**.
      (CLI package name `cascade` and registry URLs are still old — those are T4, not `@cascade-ui`,
      so they won't appear here.)
- [ ] **Step 3:** Full CLAUDE.md gate: `vp check` → `pnpm build` → `vp run -r check` → `pnpm test` →
      `pnpm regen` → `vp check --fix` → `git diff --exit-code`. All exit 0.
- [ ] **Step 4: Commit** — `chore(v15-t3): package-rename gate sweep + lockfile`

---

## Tranche exit criteria

1. Zero `@cascivo/` references remain in source (grep gate); all 24 `package.json` and the root
   are `@cascivo/*`; root is `@cascivo/monorepo`.
2. All imports, 72 `meta.dependencies`, and tsconfig paths use `@cascivo/*`.
3. All three vite alias blocks are renamed; `apps/{docs,landing,storybook}` each build **without** a
   prior full build.
4. `pnpm install` re-links the workspace; the lockfile is committed; build + type check + all tests
   are green.
5. CLI package name (`cascade`) and registry/MCP URLs are intentionally still old (T4); root gate +
   drift exit 0.
   </content>
