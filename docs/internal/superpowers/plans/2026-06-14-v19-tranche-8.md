# v19 Tranche 8 — Final gate, re-verify, DoD, roadmap close

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Steps use checkbox
> (`- [ ]`) syntax.
>
> T1–T7 are done: numbers fixed, per-route head + prerender, OG + favicons, 404, semantic links,
> a11y parity + drawer focus trap, code-splitting, expanded tests. This tranche proves the whole thing
> passes the full CLAUDE.md gate, re-verifies on a cold checkout, walks the DoD, and closes the
> roadmap. No new content — only verification and close-out.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - All eight workstreams (A–H per the roadmap) have landed across T1–T7.
> - `public/og.png` and the favicon/manifest set exist and are committed.
> - Prerender emits per-route head into `dist/<route>/index.html`; `dist/404.html` exists.

**Goal:** All five CLAUDE.md gate commands exit 0, the e2e harness passes (mobile all routes, axe all
routes, links, smoke, motion), a cold-clone build produces the OG/favicon/robots/sitemap/prerendered
HTML, every DoD box in `ROADMAP-V19.md` is verified and checked, and status flips to ✅ Shipped.

**Architecture:** Verification only. The sole content edit is checking DoD boxes + status in
`ROADMAP-V19.md`. Any failure found here is fixed in the tranche that owns it (commit the fix), then
re-gate.

**Tech stack:** unchanged.

---

### Task 1: Full CLAUDE.md gate

- [ ] **Step 1:** `pnpm exec vp check` — 0 errors; warnings at/below baseline.
- [ ] **Step 2:** `pnpm build` — all packages/apps build (landing must build without a prior full
      build; the OG/prerender build steps must succeed).
- [ ] **Step 3:** `pnpm exec vp run -r check` — type check passes, 0 errors.
- [ ] **Step 4:** `pnpm test` — all tests pass.
- [ ] **Step 5:** Drift — `pnpm regen` → `pnpm exec vp check --fix` → `git diff --exit-code` exits 0.
      The OG image and any prerendered HTML must regenerate identically (deterministic). Commit any
      regenerated artifact.

### Task 2: e2e re-verify

- [ ] **Step 1:** `pnpm exec playwright test apps/landing/test` — mobile (`/`, `/accessibility`,
      `/performance`, `/guides` at 320/375/390/414), a11y (all four, light+dark), links, smoke, motion
      all green.
- [ ] **Step 2:** Run the `@og` generator if it's part of regen; confirm `public/og.png` is 1200×630.
- [ ] **Step 3:** Manual keyboard spot-check: mobile drawer focus trap + restore; skip-nav on all four
      pages.

### Task 3: Cold-checkout verification

- [ ] **Step 1:** In a clean worktree, build from scratch and confirm `dist/` contains: `og.png`,
      `favicon.ico`/`apple-touch-icon.png`/`site.webmanifest`/icons, `robots.txt`, `sitemap.xml`,
      `404.html`, and per-route `index.html` whose head is route-specific (`curl`/grep title +
      description). This is the "works on a cold deploy" proof.

### Task 4: DoD walkthrough

- [ ] **Step 1:** Walk each `ROADMAP-V19.md` DoD line and confirm the evidence:
  - Numbers correct + drift guard (T1).
  - OG + social/twitter tags + favicon set on cold clone (T1/T2).
  - Per-route prerendered head viewable without JS (T3).
  - robots/sitemap/canonical/JSON-LD (T1/T3).
  - Real `NotFound` + `404.html` (T3).
  - Semantic anchors + secure external links (T4).
  - Skip-nav on all four + drawer focus trap (T5).
  - Code-splitting + smaller home bundle (T6).
  - Mobile/axe/links coverage (T7).
- [ ] **Step 2:** Confirm zero `useState`/`useEffect`/`useContext`/`useReducer`/`useLayoutEffect` were
      introduced across all touched landing files (grep), except the pre-existing **sanctioned**
      `useState` twin in `SignalsDemo.tsx` (untouched). Confirm no hardcoded colors added.
- [ ] **Step 3:** Confirm `#axe` was NOT modified and still resolves (it was a false positive).

### Task 5: Close the roadmap

**Files:** Modify `docs/ROADMAP-V19.md`

- [ ] **Step 1:** Check every DoD box (`[ ]` → `[x]`) with a one-line evidence note per box (mirror the
      v17 roadmap's filled-in DoD style).
- [ ] **Step 2:** Update the header: `Status:` → `✅ Shipped`; `Last updated:` → the close date.
- [ ] **Step 3: Commit** — `docs(roadmap): close v19 — landing audit & hardening shipped (v19-t8)`
- [ ] **Step 4:** Push the branch: `git push -u origin claude/epic-hawking-qas7zh` (retry on network
      error with exponential backoff). Do NOT open a PR unless explicitly asked.

---

## Tranche exit criteria

1. All five CLAUDE.md gate commands exit 0 (`vp check`, build, `vp run -r check`, test, regen+diff).
2. The full `apps/landing/test` suite is green; cold-checkout build emits OG/favicons/robots/sitemap/
   404/per-route HTML.
3. Every `ROADMAP-V19.md` DoD box is checked with an evidence note; status is ✅ Shipped.
4. Zero banned hooks and zero hardcoded colors introduced across touched files; `#axe` untouched.
5. The branch is pushed; no PR opened (unless explicitly requested).
