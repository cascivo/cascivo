# v14 Tranche 6 — Receipts + Launch (Chart Gallery, Multi-Lens Perf, A11y Page, Claims 20–24)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - T1–T5 landed: per-theme chart palettes + verification test; chart-tooltip primitive on all
>   chart types; multi-lens perf table + methodology doc; WCAG 2.2 AA + APG + media-feature
>   checks; AT matrix + accessibility statement.
> - `apps/docs` (Preact, signals) has a charts page (`ChartsPage.tsx`) and a theme switcher
>   (`data-theme`); `apps/landing` has the perf + a11y pages and a "Why cascade" surface.
> - React-app rule: `useSignals()` where signals are read.

**Goal:** Make the three earned-quality wins visible and prove them, then close the version:
a themed chart gallery, a keyboard-tooltip demo, the multi-lens perf page (from T3), the
accessibility conformance page (from T4/T5), "Why cascade" claims 20–24 with receipts, and a
DoD walkthrough.

**Architecture:** Docs/landing surfaces reading the artifacts the prior tranches produced. No
new logic — presentation + receipts.

**Tech stack:** unchanged. Docs app: Preact + signals + house rules.

---

### Task 1: Themed chart gallery

**Files:**

- Modify/Create: `apps/docs/src/pages/ChartsPage.tsx` (or a new `ChartThemesPage.tsx`) + nav

- [ ] **Step 1:** Render one representative chart (e.g. a multi-series bar or line using all 8
      series colors) repeated across all ten themes — each instance wrapped in a
      `data-theme="<theme>"` container so the per-theme palette (T1) shows side by side. The
      receipt for claim 20: the same data, visibly tuned per theme, no clashing.
- [ ] **Step 2:** Add a short note linking `docs/specs/chart-palette.md` (lineage + CVD
      rationale) and the redundant-encoding rule.
- [ ] **Step 3: Commit** — `feat(docs): themed chart gallery (palette across all themes)`

---

### Task 2: Keyboard tooltip demo

**Files:**

- Modify: the charts docs page

- [ ] **Step 1:** A focusable chart with an instruction ("Tab to focus, Arrow keys to
      traverse, Escape to clear") demonstrating the T2 tooltip via keyboard + the `aria-live`
      announcement. Receipt for claim 21.
- [ ] **Step 2: Commit** — `feat(docs): keyboard-accessible chart tooltip demo`

---

### Task 3: Verify the multi-lens perf page (from T3)

**Files:**

- Verify: `apps/landing/src/pages/PerformancePage.tsx`

- [ ] **Step 1:** Confirm the "What one component costs" section renders standalone /
      incremental / amortized lenses with explained zeros and the lens-vs-architecture copy
      (shipped in T3). If any gap remains (e.g. a `0` without a note), fix here. Receipt for
      claim 22 — link `docs/specs/perf-methodology.md`.
- [ ] **Step 2: Commit** — `chore(landing): verify multi-lens perf page completeness`

---

### Task 4: Accessibility conformance page (from T4/T5)

**Files:**

- Verify/extend: the landing a11y section + `AccessibilityStatement.tsx`

- [ ] **Step 1:** Confirm the a11y surface shows: versioned WCAG (2.2-AA) per component, the
      APG pattern column, the AT matrix link, the axe-ceiling statement, and the legal mapping
      (T5). Add a one-line summary banner ("WCAG 2.2 AA · APG-conformant · AT-tested ·
      EAA/EN 301 549/508-mapped"). Receipts for claims 23 + 24.
- [ ] **Step 2: Commit** — `chore(landing): accessibility conformance page completeness`

---

### Task 5: "Why cascade" claims 20–24 + README/llms refresh

**Files:**

- Modify: the "Why cascade" page
- Modify: README source / `scripts/readme` inputs; run `readme:generate` + `llms:generate`

- [ ] **Step 1:** Add claims 20–24 (from `ROADMAP-V14.md`), each linking its receipt:
      20 → themed chart gallery; 21 → keyboard tooltip demo; 22 → multi-lens perf page +
      methodology doc; 23 → a11y page (WCAG 2.2 + APG check); 24 → AT matrix + accessibility
      statement.
- [ ] **Step 2:** Refresh README (themed charts, accessible tooltips, honest multi-lens perf,
      WCAG 2.2 AA + APG + AT testing) and regenerate `llms.txt`. Drift gate.
- [ ] **Step 3: Commit** — `docs: Why-cascade claims 20–24 + README/llms refresh`

---

### Task 6: DoD walkthrough + roadmap close-out

**Files:**

- Modify: `docs/ROADMAP-V14.md` (status → shipped; check the DoD boxes that are met)

- [ ] **Step 1:** Walk every box in the `ROADMAP-V14.md` Definition of Done against reality;
      check the met ones with evidence; record any gap honestly (don't check unverified boxes).
- [ ] **Step 2:** Full CLAUDE.md gate end to end: `vp check` → build → `vp run -r check` →
      test → full regen → `vp check --fix` → `git diff --exit-code`. All exit 0.
- [ ] **Step 3: Commit** — `chore(v14): roadmap close-out + final gate sweep`

---

## Tranche exit criteria

1. A themed chart gallery shows the same chart across all ten themes with the per-theme
   palettes; a keyboard-tooltip demo proves hover + keyboard + announcement.
2. The multi-lens perf page and the accessibility conformance page are complete (no
   unexplained zeros; versioned WCAG + APG + AT matrix + legal mapping all present).
3. The "Why cascade" page states claims 20–24, each linked to a reproducible receipt; README
   + `llms.txt` reflect the earned-quality wins; drift gate clean.
4. The `ROADMAP-V14.md` DoD is walked; met boxes checked with evidence, gaps recorded.
5. Root gate + drift check exit 0.
