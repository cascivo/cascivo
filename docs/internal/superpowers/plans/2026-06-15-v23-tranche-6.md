# v23 Tranche 6 — Screenshots + landing copy + CI + full gate + close

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.
>
> Final tranche. The app is functionally complete (T2–T5); this tranche regenerates the deterministic
> `edge` screenshots, finalises the landing marketing copy/coverage chips, verifies CI + the assembly
> pipeline end-to-end, regenerates the README, runs the full CLAUDE.md gate, and flips the roadmap DoD.
>
> Current-state assumptions — RE-VERIFY before starting (post-T5):
>
> - `apps/examples/edge` is the complete Vercel dashboard: home grid, project detail (tabs), deployment
>   inspector, Usage/Speed Insights. Slug/package/i18n/storage all `edge.*` (T1).
> - Landing `data.ts` first entry is `edge` with placeholder T1 copy; `route-head.ts` has `/examples/edge`
>   with placeholder copy; `screenshots/edge/` holds renamed placeholders from T1.
> - Screenshot generator: `scripts/gen-demo-screenshots.mjs` (re-verify how it picks slugs + whether it
>   needs a Chromium-capable host).

**Goal:** Cascade Edge is presented accurately on the landing (hub card + `/examples/edge` detail page)
with real (or deterministic-placeholder) screenshots, faithful coverage chips, and refreshed copy; the
CI paths-filter + assembly deploy the demo correctly; the README reflects the rebuild; the full gate is
green; and `ROADMAP-V23.md` is flipped to ✅ Shipped.

**Architecture:** Edits in `apps/landing/src/pages/examples/data.ts`, `route-head.ts`, the screenshots
dir, `apps/examples/edge/readme.body.md`, and `docs/ROADMAP-V23.md`. CI is verified (already renamed in
T1). No app behaviour changes.

**Tech stack:** unchanged.

---

### Task 1: Deterministic screenshots

**Files:** `apps/landing/public/screenshots/edge/*.png`; `scripts/gen-demo-screenshots.mjs` (if needed)

- [ ] **Step 1:** Pick the canonical capture state: the **projects grid home** (most recognisable). Pause
      any simulation before capture (the inspector/usage sims must not tick mid-shot) so the capture is
      reproducible. Seed-fixed data guarantees identical frames.
- [ ] **Step 2:** Regenerate light + dark, desktop (1280×800) + mobile (390×844): run the demo's
      screenshot capture (`pnpm screenshots:generate --capture` or the documented command) for `edge`.
      Commit the four PNGs at `screenshots/edge/{light,dark}-{desktop,mobile}.png`.
- [ ] **Step 3:** If the build host has no Chromium, fall back to committing the deterministic placeholder
      wireframes (as v22 did) and **note in the roadmap DoD** that real captures need a browser-capable
      host — re-running the capture there must be a no-op diff.

### Task 2: Finalise landing marketing copy + coverage chips

**Files:** `apps/landing/src/pages/examples/data.ts`, `apps/landing/src/route-head.ts`

- [ ] **Step 1:** In the `edge` `DEMOS` entry, write accurate copy: `name: 'Cascade Edge'`, `feelsLike:
'Vercel'`, a `tagline` (e.g. "Projects, deployments, and a live build-log inspector."), a
      `description` (the faithful-dashboard pitch), a `proves` paragraph (full app shell + scope switcher +
      Cmd+K + DataTable + live build-log simulation + charts), and a `coverage` chip list reflecting what
      Edge **actually** renders (AppShell, DataTable, Status, Badge, CommandMenu, ProgressBar, Toast,
      Skeleton, EmptyState, AreaChart, BarChart). Keep chips truthful — they're verified against the app.
- [ ] **Step 2:** `route-head.ts` — write the `/examples/edge` title + description + ogTitle to match the
      new copy.
- [ ] **Step 3:** Spot-check the rendered surfaces: homepage `ExamplesGallery` card, `/examples` hub, and
      `/examples/edge` detail page show the new name, thumbnail, and chips; the "Open live demo" CTA →
      `/demos/edge/`.

### Task 3: README + assembly + CI verification

**Files:** `apps/examples/edge/readme.body.md` → `README.md`; verify `cf-pages.yml`, `assemble-demos.mjs`

- [ ] **Step 1:** Update `readme.body.md` "What it demos" to the real component/chart list and the
      faithful-Vercel description; regenerate `README.md` via the readme generator (never hand-edit).
- [ ] **Step 2:** `pnpm build:landing-demos` — confirm `apps/landing/dist/demos/edge/index.html` exists
      and the live demo drives end-to-end from one static server (home → project → deployment inspector
      streams → usage). Confirm a deep hard-reload of `/demos/edge/` serves the demo, not `404.html`.
- [ ] **Step 3:** Confirm `cf-pages.yml`'s `landing` paths-filter includes `apps/examples/edge/**` (T1)
      and no stray `deploy-deploy`/`example-deploy` job or var remains. The demo ships inside the single
      `deploy-landing` job (v22) — no per-demo CF job is added.

### Task 4: Full gate + close

- [ ] **Step 1:** Run the full CLAUDE.md gate, all six exit 0:
      `pnpm exec vp check` → `pnpm build` → `pnpm exec vp run -r check` → `pnpm test` →
      `pnpm regen && pnpm exec vp check --fix && git diff --exit-code` → `pnpm breakpoint:check`.
- [ ] **Step 2:** Final grep: no `example-deploy` / `demos/deploy` / `examples/deploy` /
      `screenshots/deploy` / `deployMsg` / `deploy.theme` anywhere (excluding `node_modules`/`dist`/`.git`
      and generic English UI words). Coverage test green.
- [ ] **Step 3:** Flip `docs/ROADMAP-V23.md`: check all DoD boxes, set **Status → ✅ Shipped**. Commit.

### Exit criteria

- [ ] Deterministic `edge` screenshots committed (or placeholders + documented note); consumed by the hub + detail page; re-running the capture is a no-op diff.
- [ ] Landing copy + coverage chips + route head are accurate and truthful; CTAs point to `/demos/edge/`.
- [ ] README regenerated; `pnpm build:landing-demos` serves the live demo; CI filter correct; no stray
      `deploy` references.
- [ ] Full CLAUDE.md gate exits 0; coverage test green.
- [ ] `ROADMAP-V23.md` DoD all checked, status ✅ Shipped. Committed.
