# v27 — Tranche 7: Gate

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Run the full CI gate across all v27 changes (T1–T6). Every command must exit 0 before
the branch is considered mergeable.

**Architecture:** No file changes. Gate commands only.

**Prerequisite:** T1 through T6 all committed on the current branch.

---

## No files to create or modify

This tranche is verification only.

---

### Task 1: Pre-gate summary — confirm all prior commits landed

- [ ] **Step 1: Review git log**

  ```sh
  git log --oneline -10
  ```

  Expected commits (in any order after v26 tip):
  - `feat(landing): add SPA client-side router with view transitions`
  - `feat(landing): sticky header, scroll progress bar, GitHub icon`
  - `feat(landing): replace theme dots with compact cycle button`
  - `feat: add @cascivo/search package with CMD+K dialog on landing`
  - `feat(landing): add modern CSS tech deep dive section`
  - `fix(landing): replace Cascade brand name with cascivo; add A11yMatrix pagination`

  If any are missing, return to the relevant tranche and complete it first.

---

### Task 2: Format + lint + type-check

- [ ] **Step 1: Run the combined check**

  ```sh
  pnpm exec vp check
  ```

  Expected: exit 0. Fix any reported issue and re-run before continuing.

  Common failures and fixes:
  - Unused imports (from T1/T2/T3 refactors): remove them.
  - Missing `useSignals()` in a component that reads `.value`: add it as the first statement.
  - Type error from `startViewTransition` cast: add a `declare global` stub or the correct TS
    lib flag.
  - Oxlint rule about `no-unsafe-optional-chaining`: use `?.closest('a')` with explicit null
    guard.

---

### Task 3: Build all packages

- [ ] **Step 1: Full build**

  ```sh
  pnpm build
  ```

  Expected: exit 0. The landing build must succeed with the new `@cascivo/search` workspace
  alias and the `TechDeepDive` section included.

  Common failures:
  - Missing alias in `vite.config.ts` for `@cascivo/search` or `@cascivo/search/SearchDialog`.
  - CSS import path for `search.css` that Vite cannot resolve — adjust to a direct relative
    path if the package alias doesn't cover CSS assets.

---

### Task 4: Type-check all packages

- [ ] **Step 1: Run recursive type-check**

  ```sh
  pnpm exec vp run -r check
  ```

  Expected: exit 0. This runs tsc across all workspace packages including the new `packages/search`.

  If `packages/search` is missing a `tsconfig.json`, create a minimal one:

  ```json
  {
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
      "outDir": "dist",
      "rootDir": "src"
    },
    "include": ["src"]
  }
  ```

  And ensure `packages/search` is listed in the workspace's `pnpm-workspace.yaml` (check that
  `packages/*` glob covers it — it should).

---

### Task 5: Tests

- [ ] **Step 1: Run all tests**

  ```sh
  pnpm test
  ```

  Expected: exit 0. No existing tests cover the new router or search code directly; this step
  confirms no regressions in existing test suites.

  If any test fails, diagnose the root cause — do not skip or modify tests to pass.

---

### Task 6: Drift check

- [ ] **Step 1: Regenerate and confirm no uncommitted diff**

  ```sh
  pnpm regen
  pnpm exec vp check --fix
  git diff --exit-code
  ```

  Expected: exit 0. If `pnpm regen` produces changes to `registry.json` or generated files,
  stage and commit them first, then re-run.

---

### Task 7: Breakpoint literal check

- [ ] **Step 1: Run the breakpoint checker**

  ```sh
  pnpm breakpoint:check
  ```

  Expected: exit 0. All `@media` and `@container` width literals in CSS must use canonical
  rem values (30rem, 40rem, 64rem, 80rem). Check `landing.css` new blocks added in T2, T3,
  T4, and T5 for any raw pixel values that slipped through.

---

### Task 8: Manual smoke test

- [ ] **Step 1: Build and preview the landing**

  ```sh
  pnpm exec vp run @cascivo/landing#build
  pnpm exec vp run @cascivo/landing#preview
  ```

  Open `http://localhost:4173/`.

- [ ] **Step 2: SPA navigation**

  Click each nav link. Confirm no full page reload (Network tab: no document requests after
  the first load). View transitions animate between pages.

- [ ] **Step 3: Sticky header**

  Scroll down on the home page. Header stays pinned. Scroll progress bar fills.

- [ ] **Step 4: Theme cycle**

  Click the theme button. Cycles light → dark → warm. Icon changes. Tooltip shows name.

- [ ] **Step 5: Search**

  CMD+K opens the dialog. Type "button" → results appear. Press Enter → navigates. Escape closes.

- [ ] **Step 6: Tech deep dive**

  The "Modern CSS changes the rules" section renders with three comparison blocks. At 640 px+
  the code pairs are side by side.

- [ ] **Step 7: A11y page**

  Navigate to `/accessibility`. The "Legal framework mapping" table header reads "cascivo".
  The "Every entry documents its contract" DataTable shows 10 rows with pagination controls.

---

### Task 9: Final commit and branch push

- [ ] **Step 1: Confirm clean working tree**

  ```sh
  git status
  ```

  Expected: nothing to commit, working tree clean.

- [ ] **Step 2: Push branch and open PR**

  Follow the project's PR process. Reference this roadmap doc (`docs/ROADMAP-V27.md`) in
  the PR description.

  PR title format: `feat(landing): v27 — SPA routing, navbar redesign, search, tech deep dive`
