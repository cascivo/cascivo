# v23 Tranche 4 — Deployment inspector (live build logs)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. For the visual surface, also use compound-engineering:frontend-design +
> screenshot-verify at 320/360/390/414. Steps use checkbox (`- [ ]`) syntax.
>
> This tranche builds the **deployment inspector** (reached via `openDeployment` from T3): the live
> build-log stream — the demo's centrepiece. Logs advance line-by-line via the kit simulation engine
> (Queued → Building → Ready), deterministically and pausably.
>
> Current-state assumptions — RE-VERIFY before starting (post-T3):
>
> - `src/state.ts` exposes `view` (`…'deployment'`), `activeDeploymentId`, and `openDeployment(id)`.
> - The App view-switch renders a `deployment` placeholder for `view === 'deployment'` — replace it here.
> - `@cascivo/example-kit` exports `createSimulation`/`useSimulation` (used by `pulse`/`flow`) — re-verify
>   the API and copy the pattern. The simulation must be **seeded + pausable**, no wall-clock dependence
>   in tests.

**Goal:** Opening a deployment shows an inspector: a summary header (status, domains, source commit,
created-by, duration) and a **live build-log stream** that advances Queued → Building → Ready via the
simulation engine, with a ProgressBar, a copy-logs button, and a Toast on completion. The stream is
deterministic (seeded), pausable, and resets correctly when switching deployments.

**Architecture:** `src/sections/DeploymentInspector.tsx` reads `activeDeploymentId`; a
`src/sim/build.ts` builds a seeded log-line script per deployment and drives it with kit
`createSimulation`. Sub-components: `BuildLog.tsx` (the streaming log view), `DeploymentSummary.tsx`.

**Tech stack:** unchanged.

---

### Task 1: Seeded build-log simulation

**Files:** `apps/examples/edge/src/sim/build.ts` (new), `build.test.ts`

- [ ] **Step 1:** For a given deployment seed, generate a deterministic **script** of build-log lines
      (e.g. "Cloning github.com/…", "Installing dependencies…", "Running `build`…", "Compiled
      successfully", "Uploading build outputs…", "Deployment ready") with per-line delays from
      `seededRandom`. A "ready" deployment plays to completion; an "error" one stops at a failing line.
- [ ] **Step 2:** Drive progression with kit `createSimulation`/`useSimulation` — each tick appends the
      next line and advances the status (`queued` → `building` → `ready`/`error`). Expose `paused`,
      `progress` (0–1), the visible-lines signal, and a `reset(deploymentId)`.
- [ ] **Step 3:** `build.test.ts` — assert determinism (same seed → identical line sequence + final
      status) and that pausing halts progression. **No real timers in assertions** — drive ticks
      manually (the kit simulation pattern from `pulse`/`flow`).

### Task 2: Build-log stream view

**Files:** `apps/examples/edge/src/sections/BuildLog.tsx`, CSS module

- [ ] **Step 1:** Render the visible log lines in a monospace, scrollable region (ScrollArea), each line
      with a timestamp + text. Auto-scroll to the latest line via `useSignalEffect` on the visible-lines
      signal (DOM side effect — never `useEffect`).
- [ ] **Step 2:** A **ProgressBar** bound to the simulation `progress`; a status chip (Status/Badge:
      Queued/Building/Ready/Error). A **pause/resume** IconButton toggling `paused` (needed for
      deterministic screenshots in T6). A **copy-logs** CopyButton that copies the full visible log; a
      Toast confirms the copy.
- [ ] **Step 3:** On completion, fire a **Toast** ("Deployment ready" / "Build failed"). Use the existing
      Toast surface so coverage holds.

### Task 3: Deployment summary header

**Files:** `apps/examples/edge/src/sections/DeploymentSummary.tsx`, CSS module

- [ ] **Step 1:** Resolve the deployment from `activeDeploymentId` via `useComputed`. Header shows: status
      (Status), environment (Badge), the deployment URL + production/preview domains (CopyButton/Code),
      source commit (hash + message + branch + author Avatar), created-by + relative time, and build
      duration (Stat). A breadcrumb back to the project (`openProject(projectId)`).
- [ ] **Step 2:** A "Visit" Button (→ deployment URL) and a "Back to project" affordance. EmptyState if
      the id resolves to nothing.

### Task 4: Wire navigation + lifecycle

**Files:** `apps/examples/edge/src/sections/DeploymentInspector.tsx`, `App.tsx`

- [ ] **Step 1:** `DeploymentInspector` composes Summary + BuildLog. Replace the App `deployment`
      placeholder with it.
- [ ] **Step 2:** When `activeDeploymentId` changes, **reset** the simulation to that deployment's seed
      (via `useSignalEffect` watching the id). Switching deployments must not bleed log lines between
      them. A deployment already `ready` shows the full completed log immediately (no replay needed) or
      replays from a paused-complete state — pick one and keep it deterministic.

### Task 5: Verify

- [ ] **Step 1:** Smoke spec: open a project → open a deployment → assert the inspector renders, logs are
      present, and (with the sim ticked) status reaches Ready. Drive ticks deterministically.
- [ ] **Step 2:** Mobile sweep 320/360/390/414 — the log region scrolls, controls stay ≥44px, summary
      stacks without overflow. `pnpm breakpoint:check` clean.

### Exit criteria

- [ ] A deployment inspector streams build logs line-by-line via a seeded, pausable simulation (Queued →
      Building → Ready/Error), with ProgressBar, copy-logs, and a completion Toast.
- [ ] A deployment summary shows status, domains, source commit, created-by, and duration.
- [ ] Switching deployments resets the stream deterministically; tests drive ticks without wall-clock.
- [ ] Coverage test green (ProgressBar/Toast/ScrollArea/CopyButton/Status exercised).
- [ ] Gate green (`vp check` → build → `vp run -r check` → test → regen+diff → `breakpoint:check`).
- [ ] Committed.
