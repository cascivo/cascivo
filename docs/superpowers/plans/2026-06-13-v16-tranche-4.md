# v16 Tranche 4 — Ecosystem section

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.
>
> T3 (AgentLayer deepening) is done. Now build the Ecosystem section — a brief, scannable inventory
> of the full @cascivo/\* package family beyond core components. `@cascivo/charts`, `@cascivo/layouts`,
> `@cascivo/i18n`, and `@cascivo/icons` are entirely invisible on the landing today. A single
> well-crafted section fixes that without burying the page in demos.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - The current section order ends: …ThemeDemo → ChartShowcase (T2) → QuickStart → CtaBand → Footer.
>   The Ecosystem section inserts between ChartShowcase and QuickStart.
> - Package names and descriptions to use as ground truth:
>   - `@cascivo/charts` — "17 chart types, CVD-safe palettes, keyboard tooltips"
>   - `@cascivo/layouts` — AppShell with off-canvas nav + grid breakpoint utilities
>   - `@cascivo/i18n` — signal-driven locale store, built-in per-component catalog, typed catalogs
>   - `@cascivo/icons` — optional SVG icon components
>   - `@cascivo/mcp` — MCP server (already visible in AgentLayer; link there rather than repeat)
>     Re-verify package descriptions by reading their `package.json` `description` fields before writing copy.
> - Docs routes for each package (re-verify from `apps/docs/src/App.tsx`):
>   - Charts: `/docs/charts` or `/charts`
>   - Layouts: `/docs/layouts` or `/layouts`
>   - i18n: `/docs/i18n` or `/i18n`
>   - Icons: `/docs/icons` or `/icons`

**Goal:** A new `Ecosystem` section on the landing that:

1. Cards the 4 "batteries included" packages: charts, layouts, i18n, icons.
2. Each card has: package name, one-line value prop, `@cascivo/<name>` install hint, and a link to docs.
3. A brief section intro: "cascivo ships more than components."
4. Mobile-first, no interactive demos — pure discoverability.
5. Zero overflow at 320–414px.

**Architecture:** New `apps/landing/src/sections/Ecosystem.tsx` + CSS in `landing.css`.
Position: after `ChartShowcase`, before `QuickStart`. Wire in `App.tsx`.

**Tech stack:** unchanged. No signals needed — static cards. No `useSignals()` required.

---

### Task 1: Research and draft card content

**Files:**

- Read: `packages/charts/package.json`, `packages/layouts/package.json`,
  `packages/i18n/package.json`, `packages/icons/package.json`
- Read: `apps/docs/src/App.tsx` (routing to find correct docs paths)

- [ ] **Step 1:** Read each package's `package.json` — record `name`, `description`, and
      `version`. These are the ground-truth one-liners for the cards.
- [ ] **Step 2:** Find the correct docs routes for charts, layouts, i18n, icons in
      `apps/docs/src/App.tsx`. If a page doesn't exist yet, link to the GitHub package directory
      instead (`github.com/urbanisierung/cascivo/tree/main/packages/<name>`).
- [ ] **Step 3:** Draft the 4 card contents:

  ```
  Card: @cascivo/charts
  Value prop: <description from package.json or short form>
  Install: pnpm add @cascivo/charts
  Link: [correct docs route]

  Card: @cascivo/layouts
  Value prop: AppShell, off-canvas nav, responsive grid — the shell that powers these docs.
  Install: pnpm add @cascivo/layouts
  Link: [correct docs route]

  Card: @cascivo/i18n
  Value prop: Built-in string catalog in every component. Override per-instance via `labels`.
  Zero hardcoded English.
  Install: pnpm add @cascivo/i18n
  Link: [correct docs route]

  Card: @cascivo/icons
  Value prop: Optional SVG icon set, tree-shaken, token-colored.
  Install: pnpm add @cascivo/icons
  Link: [correct docs route]
  ```

- [ ] **Step 4:** Draft the section headline and sub:
  - Headline: "Everything you need, nothing you don't"
  - Sub: "cascivo ships more than components. Every package is optional, versioned, and built on
    the same token architecture."

---

### Task 2: Build Ecosystem.tsx

**Files:**

- Create: `apps/landing/src/sections/Ecosystem.tsx`

- [ ] **Step 1:** Build the component. Pure static markup — no signals, no hooks.
      ```tsx
      const PACKAGES = [
      {
      name: '@cascivo/charts',
      value: '...',
      install: 'pnpm add @cascivo/charts',
      href: '/charts', // use correct route from Task 1
      },
      // ... layouts, i18n, icons
      ]

      export function Ecosystem() {
        return (
          <section id="ecosystem" class="ecosystem section">
            <h2>Everything you need, nothing you don't</h2>
            <p class="ecosystem-sub">...</p>
            <div class="ecosystem-grid">
              {PACKAGES.map(pkg => (
                <div class="ecosystem-card" key={pkg.name}>
                  <code class="ecosystem-pkg-name">{pkg.name}</code>
                  <p class="ecosystem-value">{pkg.value}</p>
                  <pre class="ecosystem-install"><code>{pkg.install}</code></pre>
                  <a href={pkg.href} class="ecosystem-link">Docs →</a>
                </div>
              ))}
            </div>
          </section>
        )
      }
      ```

- [ ] **Step 2:** The `@cascivo/layouts` card copy should mention "powers these very docs" — a
      credibility signal that this is dogfooded, not theoretical.
- [ ] **Step 3:** The install snippet in each card should use a `CopyCommand` component if one
      exists on the landing already, or a plain `<pre><code>` if not. Match what `QuickStart.tsx`
      uses for consistency.

---

### Task 3: Style Ecosystem in landing.css

**Files:**

- Modify: `apps/landing/src/landing.css`

- [ ] **Step 1:** Add a `/* ── Ecosystem ── */` block. Mobile-first:
  ```css
  .ecosystem {
    /* section padding matches other sections */
  }
  .ecosystem-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--cascivo-space-6);
  }
  .ecosystem-card {
    border: 1px solid var(--cascivo-border-subtle, currentColor);
    border-radius: var(--cascivo-radius-md);
    padding: var(--cascivo-space-6);
    display: flex;
    flex-direction: column;
    gap: var(--cascivo-space-3);
  }
  .ecosystem-pkg-name {
    font-size: var(--cascivo-text-sm);
  }
  .ecosystem-install {
    overflow-x: auto;
  }
  @media (min-width: 40rem) {
    .ecosystem-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (min-width: 64rem) {
    .ecosystem-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }
  ```
  Use only existing token names from `packages/tokens/src/index.css`. Do not hardcode px or colors.
- [ ] **Step 2:** `.ecosystem-sub` matches the fluid sub-text size pattern from other sections.
- [ ] **Step 3:** The section has a visual separator (either a top border or a distinct background
      token like `--cascivo-color-surface-alt`) to distinguish it from the chart showcase above
      and QuickStart below. Use tokens only.
- [ ] **Step 4: Commit** — `feat(landing): Ecosystem section — charts, layouts, i18n, icons (v16-t4)`

---

### Task 4: Wire into App.tsx + mobile harness

**Files:**

- Modify: `apps/landing/src/App.tsx`
- Modify: `apps/landing/test/mobile.spec.ts`

- [ ] **Step 1:** Import `Ecosystem` and insert it between `ChartShowcase` and `QuickStart`.
- [ ] **Step 2:** Add `#ecosystem` to the mobile overflow harness — screenshot at 320/375/390/414
      and assert zero horizontal overflow.
- [ ] **Step 3:** Run `pnpm exec playwright test apps/landing/test/mobile.spec.ts` — all assertions
      pass.
- [ ] **Step 4: Commit** — `feat(landing): wire Ecosystem section + mobile harness (v16-t4)`

---

## Tranche exit criteria

1. An `Ecosystem` section exists between `ChartShowcase` and `QuickStart`.
2. Cards for `@cascivo/charts`, `@cascivo/layouts`, `@cascivo/i18n`, `@cascivo/icons` — each with value prop, install command, and docs link.
3. `@cascivo/layouts` card notes it powers the cascivo docs (dogfood credibility signal).
4. Mobile: single column at base; 2-col at 40rem; 4-col at 64rem.
5. Zero overflow at 320–414px (Playwright).
6. All token-based styling — no hardcoded colors or sizes.
