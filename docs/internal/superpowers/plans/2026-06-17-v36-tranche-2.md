# v36 — Tranche 2: Minimal Home Composition

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** With every relocating section now living on a dedicated page (T1), trim `HomePage` to the
minimal target set: Hero, a thin proof strip, Principles, a single showcase slot, condensed Quick Start,
closing CTA, Footer. Remove the relocated sections and their now-dead lazy imports. The hero, showcase,
and disclosure redesigns happen in T3–T5; T2 establishes the **structure**.

**Architecture:** Edits the `HomePage` composition in `apps/landing/src/App.tsx` and condenses
`StatsBand` into a thin proof strip. The showcase slot in T2 temporarily renders the existing
`ThemeDemo` (it becomes the full `Tabs` showcase in T4). No section internals change here beyond
`StatsBand`'s condensation.

**Tech Stack:** React 18 island, `@cascivo/core` signals, vite+ (`vp`).

---

## Files

- Modify: `apps/landing/src/App.tsx` — `HomePage` → minimal section set; drop relocated lazy imports
- Modify: `apps/landing/src/sections/StatsBand.tsx` — condense to a single quiet proof strip
- Verify: no dead imports, no orphaned lazy chunks, no broken in-page anchors

---

### Task 1: Rebuild the `HomePage` composition

**Files:** `apps/landing/src/App.tsx`

- [ ] **Step 1:** Edit `HomePage` to render exactly, in order: `Header`, then inside `main`: `Hero`,
      `StatsBand` (condensed), `Principles`, `ThemeDemo` (showcase slot — replaced by `Showcase` in T4),
      `QuickStart`, `CtaBand`; then `Footer`.
- [ ] **Step 2:** Remove from `HomePage` (and their `lazy(...)` declarations if unused elsewhere):
      `TechDeepDive`, `RelayConsole`, `SignalsDemo`, `ProofTeasers`, `AgentLayer`, `ChartShowcase`,
      `ExamplesGallery`, `Ecosystem`. Keep each component file on disk — they are imported by their new
      pages (T1).
- [ ] **Step 3:** Remove now-unused imports/`lazy` bindings and any `SectionFallback` heights tied only
      to removed sections. `vp check` must report no unused imports.

### Task 2: Condense `StatsBand` into a proof strip

**Files:** `apps/landing/src/sections/StatsBand.tsx`

- [ ] **Step 1:** Reduce `StatsBand` to a single compact metric row (3–4 numbers max) with a "see the
      numbers" link to `/performance`. Keep it eager (cheap, above-the-fold-adjacent). Do not delete the
      metrics — surplus metrics live on `/performance` (T1 moved the proof there).
- [ ] **Step 2:** Verify the strip passes the mobile sweep (no overflow at 320–414).

### Task 3: Anchor + link integrity

- [ ] **Step 1:** Search the landing app for in-page hash links into removed home sections (e.g.
      `href="/#agent-layer"`, `#charts`, `#signals`). Re-point them to the relocated page URL (`/ai`,
      `/examples`, `/performance`). No anchor may 404 or scroll to nothing.
- [ ] **Step 2:** Confirm the search index entries (T1) and the nav still resolve.

### Task 4: Verify + gate

- [ ] **Step 1:** `pnpm build` → exit 0; `pnpm exec vp check` → exit 0 (zero unused imports/warnings).
- [ ] **Step 2:** `pnpm test` for the landing app → pass (update any home-composition test snapshot/assertion
      that counted sections).
- [ ] **Step 3:** Drift gate: `pnpm regen && pnpm exec vp check --fix && git diff --exit-code`.

### Task 5: Commit T2

- [ ] **Step 1: Stage and commit**

  ```bash
  git add apps/landing
  git commit -m "$(cat <<'EOF'
  feat(landing): trim home to the minimal section set (v36 T2)

  Cut HomePage from 14 sections to the minimal set (Hero, proof strip,
  Principles, showcase slot, Quick Start, CTA, Footer). The eight relocated
  sections now live on their dedicated pages from T1; this removes them from the
  home composition and drops the dead lazy imports. Condense StatsBand to a thin
  proof strip linking to /performance. Re-point in-page anchors to relocated URLs.

  Co-Authored-By: Claude <noreply@anthropic.com>
  EOF
  )"
  ```
