# v16 Tranche 3 — AgentLayer deepening (audit + llms.txt)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.
>
> T2 (charts showcase) is done. Now expand the AgentLayer section to tell the complete AI story:
> not just how to connect an agent to cascivo, but how cascivo audits what the agent produces.
> The audit tool (`cascivo audit --ai`) is the most compelling differentiator in the AI layer and
> is currently invisible on the landing.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - `AgentLayer.tsx` has 4 artifact cards in a grid: (1) The Manifest, (2) Connect the MCP Server,
>   (3) Then Just Ask, (4) Agents Don't Screenshot. Re-read the file before implementing to verify
>   current card count and the grid CSS class.
> - `cascivo audit --ai` is a real shipped command in `packages/cli/src/commands/` — re-verify the
>   exact command syntax by reading that file before writing copy.
> - `llms.txt` is generated at build time by `scripts/llms/` (re-verify path). The landing footer
>   links to it. The file format follows the llms.txt community convention.
> - The AgentLayer grid CSS is in `apps/landing/src/landing.css` — find the grid rule for the
>   artifact cards before editing so you don't break the layout.

**Goal:** The `AgentLayer` section gains two new artifact cards:

1. **`cascivo audit --ai`** — a before/after code block: agent-generated code with a hard-coded
   color value, then the audit command and its output catching the violation and suggesting the
   correct token. Makes the "agents make mistakes; cascivo catches them" story tangible.

2. **`llms.txt`** — a brief explanation: what it is (a generated file that consolidates component
   manifests, intent blocks, token catalog, and design specs into a single LLM-readable document),
   how it's generated, and why a developer would point an agent at it instead of the raw docs.

**Architecture:** Modify `apps/landing/src/sections/AgentLayer.tsx` + `landing.css`. No new files
beyond what the new cards need inline. Mobile-first on any new grid rules.

**Tech stack:** unchanged.

---

### Task 1: Research audit command syntax + llms.txt format

**Files:**

- Read: `packages/cli/src/commands/` (find the audit command file)
- Read: `scripts/llms/` (find the llms.txt generator)
- Read: `llms.txt` (root of repo, if generated)

- [ ] **Step 1:** Find and read the CLI audit command implementation. Record the exact invocation
      syntax (`cascivo audit --ai <paths>` or similar) and one example of its output format (the
      violation message that flags a hard-coded color). This becomes the copy for Card 5.
- [ ] **Step 2:** Find and read the llms.txt generator. Record what the file contains (manifest
      sections, token catalog, specs) and how it's generated (at build time via `pnpm regen` or
      similar). Record the URL where it's served (`/llms.txt` on the domain). This becomes the
      copy for Card 6.
- [ ] **Step 3:** Read the current `AgentLayer.tsx` in full. Note:
  - The CSS class on the 4-card grid container
  - How each card is structured (heading + body + optional code block + caption)
  - Whether the grid is already 2-column or 2×2
  - The section `id` attribute (for scroll targets)

---

### Task 2: Add Card 5 — cascivo audit --ai

**Files:**

- Modify: `apps/landing/src/sections/AgentLayer.tsx`

- [ ] **Step 1:** Add a 5th artifact card immediately after card 4 ("Agents Don't Screenshot"):
  ```
  Title: "Audit the agent's output"
  Body:
    A code block showing bad agent-generated code:
      color: #3b82f6;  /* ← hard-coded */
    Then the audit invocation:
      cascivo audit --ai src/components/
    Then the audit output (one representative line):
      ⚠  src/components/Dashboard.tsx:14  hard-coded color '#3b82f6'
         → use var(--cascivo-color-accent) or a component token
    Caption:
      "cascivo audit --ai scans agent-generated code for hard-coded values, invented props,
       missing required props, and raw strings where i18n is expected."
  ```
- [ ] **Step 2:** The code blocks use `<pre><code>` with appropriate CSS class. Match the existing
      card markup pattern exactly (same `class`, same `<figure>`/`<figcaption>` wrapper if used).
- [ ] **Step 3:** The before/after should be visually distinct — the "bad" block can use a subtle
      red/error surface token (`--cascivo-color-danger-subtle` or similar), the "good" output a
      neutral code surface. Use token values only.

---

### Task 3: Add Card 6 — llms.txt

**Files:**

- Modify: `apps/landing/src/sections/AgentLayer.tsx`

- [ ] **Step 1:** Add a 6th artifact card after card 5:
  ```
  Title: "One file. Everything an agent needs."
  Body:
    A brief snippet from llms.txt showing the structure header (component name + intent block +
    token list) — 6–8 lines is enough to show the format.
  Caption:
    "llms.txt consolidates every component's manifest, intent block, token catalog, and design
     specs into a single LLM-readable document — generated at build time from the same source that
     drives the MCP server and the docs. Point an agent at /llms.txt before it writes."
  Link: <a href="/llms.txt">View llms.txt →</a>
  ```
- [ ] **Step 2:** Match the card markup pattern from existing cards. The snippet should be in a
      `<pre><code>` block; the caption in `<figcaption>` or equivalent.
- [ ] **Step 3:** Update the section subheadline (if present) to reflect the expanded scope:
      "Your agent already knows cascivo — and it can audit what it writes."
      (Or a similarly expanded version of the current subheadline; keep it a single sentence.)
- [ ] **Step 4: Commit** — `feat(landing): AgentLayer — audit + llms.txt artifact cards (v16-t3)`

---

### Task 4: CSS updates for 6-card grid

**Files:**

- Modify: `apps/landing/src/landing.css`

- [ ] **Step 1:** Find the AgentLayer grid rule. If it's a strict 2×2 grid (`grid-template-columns:
repeat(2, 1fr)` or similar), it needs to accommodate 6 cards. Options:
  - Change to `repeat(auto-fill, minmax(280px, 1fr))` — the grid fills naturally at any card count.
  - Or use `repeat(3, 1fr)` at `md` and stack 1-col at mobile.
    Choose the option that preserves visual quality at all widths. Single column at base is mandatory.
- [ ] **Step 2:** Verify the new cards render without breaking existing 4-card layout at all
      breakpoints. Run the build and visually spot-check at 320px, 768px, 1280px.
- [ ] **Step 3:** Run `apps/landing/test/mobile.spec.ts` — zero overflow at 320/375/390/414.
- [ ] **Step 4: Commit** — `feat(landing): AgentLayer 6-card grid layout (v16-t3)`

---

## Tranche exit criteria

1. AgentLayer has 6 artifact cards; the 4 existing cards are untouched.
2. Card 5 shows a before/after audit example with the exact CLI command and a representative output line.
3. Card 6 shows a llms.txt snippet with caption and a link to `/llms.txt`.
4. Section subheadline reflects the audit + llms.txt additions.
5. Grid accommodates 6 cards with single-column mobile layout; zero overflow at 320–414px.
6. No hardcoded color values in new markup (token-based only).
