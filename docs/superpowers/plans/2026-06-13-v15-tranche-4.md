# v15 Tranche 4 — CLI, registry, MCP, skills & external references

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.
>
> This tranche finishes the rename: the CLI package + bin, the registry raw URLs + base-URL config,
> the MCP server name + base URLs (+ test fixtures), the `cascade:*` skills, and the remaining brand
> copy. It also writes the **external go-live checklist** (npm org, DNS, GH repo rename) — the steps
> the human performs. Tokens (T2) and packages (T3) are already renamed; this is the last code layer.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - `packages/cli/package.json`: `"name": "cascade"`, `"bin": { "cascade": "./bin/cascade.mjs" }`.
> - Registry raw URLs hardcoded in `scripts/registry/generate.ts:22`
>   (`https://raw.githubusercontent.com/urbanisierung/cascade-ui/main`),
>   `packages/cli/src/utils/config.ts:25`, and across `registry.json`. `resolve.ts:94` builds from
>   `owner`/`repo`.
> - MCP base URLs `https://cascade-ui.dev` in `packages/mcp/src/context.ts:51`, `tokens.ts:23`;
>   server name `@cascivo/mcp` in `server.ts:49`; test fixtures in `*.test.ts` assert those URLs.
> - Skills `cascade:add`, `cascade:design-page`, `cascade:create-theme`, `cascade:extend` in
>   `skills/`.
> - Decision: GH repo will be renamed `urbanisierung/cascade-ui` → `cascivo/cascivo`
>   (redirect-safe). Domain `cascivo.com`.

**Goal:** No `cascade-ui.dev`, `urbanisierung/cascade-ui`, `cascade` (as the CLI/skill brand), or
other stale brand string remains in source. The CLI is `@cascivo/cli` (bin `cascivo`); registry URLs
point at the renamed repo and are parameterized via `REGISTRY_BASE_URL`; MCP uses `cascivo.com`;
skills are `cascivo:*`. `pnpm regen` produces no drift. The external checklist is documented.

**Architecture:** Final brand-string rename + URL parameterization + registry regeneration. The
GH-repo and npm-org actions are out-of-code steps the checklist captures; the code is written so it
works the instant those flip.

**Tech stack:** unchanged.

---

### Task 1: CLI package + bin rename

**Files:**

- Modify: `packages/cli/package.json`, `packages/cli/bin/*`, CLI source referencing its own name,
  any docs invoking `npx cascade`

- [ ] **Step 1:** `packages/cli/package.json`: `"name": "cascade"` → `"@cascivo/cli"`;
      `"bin": { "cascade": "./bin/cascade.mjs" }` → `"bin": { "cascivo": "./bin/cascivo.mjs" }`.
      Rename the bin file `bin/cascade.mjs` → `bin/cascivo.mjs` (and any shebang/help text inside).
- [ ] **Step 2:** Update CLI help/usage strings and any self-references from `cascade` to `cascivo`
      (e.g. `cascivo init / add / list / update`). Update the package `description`.
- [ ] **Step 3:** Update every doc/README/skill that says `npx cascade …` → `npx @cascivo/cli …`
      (note the scoped invocation; the unscoped `npx cascivo` shorthand is deferred per the master
      plan).
- [ ] **Step 4: Commit** — `refactor(cli): rename to @cascivo/cli, bin cascivo (v15-t4)`

---

### Task 2: Registry URLs + base-URL parameterization

**Files:**

- Modify: `scripts/registry/generate.ts`, `packages/cli/src/utils/config.ts`,
  `packages/cli/src/utils/resolve.ts`, `registry.json` (regenerated)

- [ ] **Step 1:** Replace the hardcoded host/repo
      `https://raw.githubusercontent.com/urbanisierung/cascade-ui/main` →
      `https://raw.githubusercontent.com/cascivo/cascivo/main` in `generate.ts` and
      `config.ts`. Keep `REGISTRY_BASE_URL` as the override (`generate.ts:22`); confirm `resolve.ts`
      `owner`/`repo` defaults track the new repo.
- [ ] **Step 2:** Regenerate the registry: `pnpm regen` — `registry.json` rewrites every raw URL to
      the new repo path. Commit the regenerated file.
- [ ] **Step 3:** Note in a comment / the external checklist that these URLs resolve only once the GH
      repo is renamed (GitHub auto-redirects the old path, so they don't 404 in the interim).
- [ ] **Step 4: Commit** — `refactor(registry): point raw URLs at cascivo/cascivo (v15-t4)`

---

### Task 3: MCP server name + base URLs + fixtures

**Files:**

- Modify: `packages/mcp/src/server.ts`, `context.ts`, `tokens.ts`, and `*.test.ts` fixtures

- [ ] **Step 1:** `server.ts:49` name `@cascivo/mcp` → `@cascivo/mcp`.
- [ ] **Step 2:** `context.ts:51` + `tokens.ts:23` base URL `https://cascade-ui.dev` →
      `https://cascivo.com` (or the chosen docs subdomain — pin one in the external checklist and use
      it consistently).
- [ ] **Step 3:** Update MCP test fixtures (`context.test.ts`, `multi-registry.test.ts`, etc.) that
      assert the old URLs; run `vp run @cascivo/mcp#test` — green.
- [ ] **Step 4: Commit** — `refactor(mcp): rename server + base URLs to cascivo (v15-t4)`

---

### Task 4: Skills + remaining brand copy

**Files:**

- Modify: `skills/*` (skill names/manifests), `scripts/factory/*`, `factory-backlog.json`,
  README(s), `llms.txt`/`llms-full.txt` if present, any remaining brand strings

- [ ] **Step 1:** Rename skills `cascade:add → cascivo:add`, `cascade:design-page → cascivo:design-page`,
      `cascade:create-theme → cascivo:create-theme`, `cascade:extend → cascivo:extend`; update their
      manifests and any cross-references.
- [ ] **Step 2:** Sweep remaining brand copy: `grep -rni 'cascade-ui\|cascade ' . | grep -v node_modules`
      and convert the remaining brand-name occurrences to `cascivo` (skip the `--cascivo-`/`@cascivo`
      hits already done; skip historical references in older `docs/ROADMAP-V*.md`/`docs/superpowers/plans/`
      and `docs/BRAND.md` "how we got here" — those are deliberately historical. Use judgement:
      brand-facing strings → rename; historical/archival → leave).
- [ ] **Step 3:** Update README/llms files headline name, install snippets (`@cascivo/*`,
      `npx @cascivo/cli`), and links to `cascivo.com`.
- [ ] **Step 4: Commit** — `refactor(brand): rename skills + remaining brand copy to cascivo (v15-t4)`

---

### Task 5: External go-live checklist

**Files:**

- Create: `docs/specs/cascivo-launch-checklist.md`

- [ ] **Step 1:** Document the out-of-code steps (owner performs):
  - Create npm org **`cascivo`**; publish/transfer the 15 `@cascivo/*` packages (and `@cascivo/cli`).
  - Point **`cascivo.com`** DNS at the landing host; set up the docs subdomain if used; provision TLS.
  - Rename the GitHub repo **`urbanisierung/cascade-ui` → `cascivo/cascivo`** (GitHub keeps a
    redirect from the old path, so the regenerated raw URLs keep working both before and after).
  - Update repo `git remote` locally after the rename.
  - Verify `npx @cascivo/cli add button` resolves a real component from the new registry URLs.
- [ ] **Step 2:** Note which code prerequisites are already done (this tranche) vs. what waits on the
      external flip; mark verification steps for T7.

---

### Task 6: Grep gate + full gate

- [ ] **Step 1: GATE —** `grep -rni 'cascade-ui\.dev\|urbanisierung/cascade-ui\|cascade:add\|"name": "cascade"' . | grep -v node_modules`
      returns nothing (allowing deliberately historical mentions in archived roadmap/plan docs).
- [ ] **Step 2:** Full CLAUDE.md gate: `vp check` → build → `vp run -r check` → test → `pnpm regen`
      → `vp check --fix` → `git diff --exit-code`. All exit 0 (registry regen must be drift-clean).
- [ ] **Step 3: Commit** — `chore(v15-t4): external-rename gate sweep + launch checklist`

---

## Tranche exit criteria

1. CLI is `@cascivo/cli` with bin `cascivo`; all `npx cascade` references are `npx @cascivo/cli`.
2. Registry raw URLs + `REGISTRY_BASE_URL` default point at `cascivo/cascivo`; `registry.json`
   is regenerated and drift-clean.
3. MCP server is `@cascivo/mcp`, base URLs are `cascivo.com`, fixtures updated, MCP tests green.
4. Skills are `cascivo:*`; README/llms/brand copy carry the new name; only deliberately historical
   doc mentions of the old name remain.
5. `docs/specs/cascivo-launch-checklist.md` documents the npm-org / DNS / repo-rename steps.
6. Root gate + drift exit 0 — the rename (T1–T4) is complete and green.
   </content>
