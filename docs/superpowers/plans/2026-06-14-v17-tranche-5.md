# v17 Tranche 5 — FAQ (collapsible, on the shipped Accordion) + closing CTA

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. ALSO use compound-engineering:frontend-design for visual quality +
> screenshot verification. Steps use checkbox (`- [ ]`) syntax.
>
> T1–T4 are done (scaffold, migration, customization, use cases + boundaries). Close the page with the
> two things every adoption-stage visitor expects: a collapsible FAQ and a final CTA. The FAQ MUST be
> built on the shipped `Accordion` component — dogfooding, and it's the research-expected pattern
> (tap-to-expand). Every answer ends in a concrete next step.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - `Accordion` ships in `packages/components/src/accordion/`. RE-READ its source + `component.meta.ts`
>   for the real prop API (item shape, single vs multi-expand, controlled vs uncontrolled, heading
>   level). Build the FAQ to THAT API — do not assume.
> - Other landing subpages import components like `@cascivo/components/skip-nav`. Confirm the same
>   subpath-import pattern resolves `@cascivo/components/accordion` in `apps/landing` (check
>   `apps/landing/vite.config.ts` aliases + the package `exports` map). If a source alias is needed per
>   the CLAUDE.md vite-alias rule, add it.
> - If the Accordion reads signals internally, a landing React component rendering it still must follow
>   the landing signal rule: `useSignals()` first if THIS component reads `signal.value`. The
>   Accordion's own internal signals are its concern, not the page's.
> - `guides/data.ts` already exists from T1–T4. The FAQ entries extend it.

**Goal:** Two closing sections on `/guides`:

1. `GuidesFaq` (anchor `#faq`) — a collapsible FAQ built on the shipped `Accordion`, answering ≥6 real
   adoption questions; every answer ends in a next-step link (docs route, skill, or another guide
   anchor).
2. `GuidesCta` — a final call-to-action band (install command + primary links) closing the page.

Both render with zero horizontal overflow at 320–414px.

**Architecture:** New `apps/landing/src/pages/guides/GuidesFaq.tsx` and `guides/GuidesCta.tsx`, content
in `guides/data.ts`, styles in `landing.css`. No package changes (unless a vite alias is required to
resolve `@cascivo/components/accordion` — add per the CLAUDE.md rule if so).

**Tech stack:** unchanged. Mobile-first CSS. `useSignals()` only if a signal is read.

---

### Task 1: Verify the Accordion API + draft FAQ entries

**Files:**

- Read: `packages/components/src/accordion/` (component source + `component.meta.ts`),
  `apps/landing/vite.config.ts` (alias check).

- [ ] **Step 1:** Record the real `Accordion` prop API: how items are passed (children vs `items`
      prop), single vs multiple open, default-open, the heading semantics. Note the i18n `labels`
      prop if present (CLAUDE.md component rule 6). Build to this exact API.
- [ ] **Step 2:** Confirm `@cascivo/components/accordion` resolves in `apps/landing` (the
      `skip-nav` subpath import already works on other subpages, so the pattern exists). If it needs a
      vite source alias, add it to `apps/landing/vite.config.ts` per the CLAUDE.md vite-alias rule and
      note it.
- [ ] **Step 3:** Draft ≥6 real adoption questions, each answer ending in a next-step link. Candidates:
      - "Is cascivo free / what's the license?" → MIT → next: GitHub.
      - "Do I have to use all of it, or can I add one component?" → copy-own, add what you need →
        next: QuickStart.
      - "Can I use it with Next.js / RSC?" → RSC-compatible, example app exists → next: the react-next
        example.
      - "Do I need Tailwind?" → no; modern CSS + tokens → next: `#customize`.
      - "How is this different from shadcn?" → shared copy-own model + signals/tokens/AI layer →
        next: `#migrate`.
      - "Will agents generate correct cascivo code?" → manifests + MCP + `audit --ai` → next: `/context`.
      - "How do I change a component's behavior, not just its color?" → `cascivo-extend` skill →
        next: the extend skill / docs.
      - "What browsers are supported?" → last-2-versions (needs `:has()`/`@container`) → next: `#when-not`.
      Keep answers tight (2–4 sentences) and benefit-oriented.

---

### Task 2: Add FAQ + CTA content to data.ts

**Files:**

- Modify: `apps/landing/src/pages/guides/data.ts`

- [ ] **Step 1:** Add `FAQ: { q, a, next: {label, href} }[]` and `GUIDES_CTA: { title, sub, primary,
      secondary }` exports. Answers are plain strings; the next-step link is structured so the
      component can render it consistently after each answer.

---

### Task 3: Build GuidesFaq.tsx + GuidesCta.tsx

**Files:**

- Create: `apps/landing/src/pages/guides/GuidesFaq.tsx`
- Create: `apps/landing/src/pages/guides/GuidesCta.tsx`

- [ ] **Step 1:** `GuidesFaq` — `id="faq"`, `<h2>`, render the `FAQ` array through the shipped
      `Accordion` (each item: question = trigger, answer + next-step link = panel content). Use the
      Accordion's real API from Task 1. Add `useSignals()` first only if this component reads a signal.
- [ ] **Step 2:** `GuidesCta` — a closing band reusing the existing `CtaBand` styling/pattern from the
      home page if practical (check `apps/landing/src/sections/CtaBand.tsx`); otherwise a simple
      band with the install command + "Browse components →" + "Read the why →" links.
- [ ] **Step 3:** Wire both into `GuidesPage.tsx` as the last two sections (FAQ then CTA), after
      `WhenNotToUse`.

---

### Task 4: Style

**Files:**

- Modify: `apps/landing/src/landing.css`

- [ ] **Step 1:** Add `/* ── Guides: FAQ ── */` and `/* ── Guides: CTA ── */` CSS. The Accordion
      carries its own component styles; only add the section-level wrapper (max-width, spacing) and the
      next-step link styling inside panels. Mobile-first; token-based colors only.
- [ ] **Step 2:** Verify keyboard + screen-reader behavior of the FAQ is whatever the shipped Accordion
      already provides (do not re-implement; just confirm it works in the section context).
- [ ] **Step 3:** Run the mobile spec for `/guides` — zero overflow with FAQ + CTA present.
- [ ] **Step 4: Commit** — `feat(landing): FAQ accordion + closing CTA on /guides (v17-t5)`

---

## Tranche exit criteria

1. `/guides#faq` renders a collapsible FAQ built on the shipped `@cascivo/components` `Accordion` (not a
   hand-rolled accordion), answering ≥6 real questions.
2. Every FAQ answer ends in a concrete next-step link (docs route, skill, or in-page anchor).
3. A closing CTA band ends the page with the install command + primary links.
4. Mobile harness passes at 320/375/390/414 — zero overflow with all `/guides` sections present.
5. No `useState`/`useEffect`/`useContext`; `useSignals()` only if a signal is read; no hardcoded colors.
