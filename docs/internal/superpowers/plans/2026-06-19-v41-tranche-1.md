# v41 — Tranche 1: `image` Component

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship the clearest component gap the HeroUI study found — a dedicated **`image`** component. cascivo
has `aspect-ratio` (a layout box) and `skeleton` (a placeholder) but nothing that owns image **load
behaviour**. Build it the cascivo way: a signal-driven `loading → loaded → error` FSM modelled exactly on
`avatar/avatar.tsx`, a blur-up/`skeleton` placeholder shown during `loading`, a graceful `fallbackSrc` on
error, `width`/`height`/`radius`, and an optional reduced-motion-safe hover `zoom`. Ship the full kit —
`image.tsx` + `image.module.css` + `image.meta.ts` + `image.test.tsx` — export it from `@cascivo/react`, add
it to `_all-metas.ts`, and let `pnpm regen` add it to `registry.json`.

**Architecture:** Follow `packages/components/src/avatar/avatar.tsx` for the load FSM
(`createMachine`/`useMachine`, `useSignals()`, `onLoad`/`onError` → `LOADED`/`ERROR`) and
`packages/components/src/skeleton/*` for the placeholder. Per CLAUDE.md: no banned hooks; visual states via
CSS; DOM side effects (none needed beyond native `<img>` load events) never via `useEffect`.

**Tech Stack:** signal-driven TSX + CSS Modules; `@cascivo/core` (`createMachine`, `useMachine`, `useSignals`,
`cn`); `@cascivo/i18n` if any string surfaces; Vitest + Testing Library.

---

## Files

- Create: `packages/components/src/image/image.tsx`
- Create: `packages/components/src/image/image.module.css`
- Create: `packages/components/src/image/image.meta.ts`
- Create: `packages/components/src/image/image.test.tsx`
- Modify: `packages/react/src/index.ts` (export `image`)
- Modify: `packages/components/src/_all-metas.ts` (add `imageMeta`)
- Modify: `packages/i18n/src/builtin.ts` (only if Image surfaces a string)
- Regenerate: `registry.json` (`pnpm regen`)

---

### Task 1: Study the avatar load FSM and the skeleton placeholder

**Files:** `packages/components/src/avatar/*`, `packages/components/src/skeleton/*`, `packages/components/src/aspect-ratio/*`

- [ ] **Step 1:** Read `avatar/avatar.tsx` end to end: the `createMachine({ initial: 'loading', states: { loading: { on: { LOADED, ERROR } }, loaded, error } })` shape, `useSignals()` first, `showImage` derivation, `onLoad`/`onError` → `send(...)`, and how `data-*` attributes drive CSS.
- [ ] **Step 2:** Read `skeleton/*` (how the placeholder is styled/animated, reduced-motion handling) and `aspect-ratio/*` (how a width/height ratio box is expressed) — decide whether `image` consumes them or just mirrors their CSS.
- [ ] **Step 3:** Read `avatar/avatar.meta.ts` for the `ComponentMeta` shape and `avatar/avatar.test.tsx` for the load/error test pattern.

### Task 2: Implement `image.tsx` (load FSM, signal-driven)

**Files:** `packages/components/src/image/image.tsx`

- [ ] **Step 1:** Props extending `ImgHTMLAttributes<HTMLImageElement>` (omit conflicting): `src`, `alt`
      (required for non-decorative), `fallbackSrc?`, `width?`/`height?`, `radius?: 'none'|'sm'|'md'|'lg'|'full'`,
      `zoom?: boolean`, `removeWrapper?: boolean` (HeroUI parity — render bare `<img>` when true),
      `isBlurred?`/`blur` placeholder toggle, `className`/`...rest`.
- [ ] **Step 2:** `useSignals()` first. `const [state, send] = useMachine(machine)` with the
      `loading/loaded/error` machine (copy avatar's). On `error`, if `fallbackSrc` is set, render it; else show a
      neutral fallback box. Render `<img onLoad={() => send('LOADED')} onError={() => send('ERROR')}>` inside a
      wrapper `<span>`/`<div>` carrying `data-state={state.value}`, `data-radius`, `data-zoom`.
- [ ] **Step 3:** Placeholder: while `state.value === 'loading'`, show the skeleton/blur layer (CSS, sized to
      `width`/`height` or aspect-ratio). Swap to the loaded image via CSS opacity transition (reduced-motion-safe).
- [ ] **Step 4:** No `useEffect`. The only side effect is the native `<img>` load lifecycle, handled by event
      props → `send`. Guard `removeWrapper` (no placeholder/zoom when bare).

### Task 3: CSS, manifest & (optional) i18n

**Files:** `packages/components/src/image/image.module.css`, `image.meta.ts`, (maybe) `packages/i18n/src/builtin.ts`

- [ ] **Step 1:** `image.module.css`: `@layer cascade.component`; consume `var(--cascivo-*)` tokens only (no
      literals); radius via `--cascivo-radius-*`; blur-up + opacity swap + optional `:hover` `scale()` zoom each
      with a **static fallback** preceding any progressive declaration; **disable** blur/zoom transitions under
      `@media (prefers-reduced-motion: reduce)`; logical properties; no off-scale breakpoint literals.
- [ ] **Step 2:** `image.meta.ts`: complete `ComponentMeta` — `name: 'Image'`, `category: 'display'`, `states:
      ['loading','loaded','error']`, props (from the TS types), `tokens` read, accessibility (`role: 'img'`,
      `wcag: 'AA'`), examples (basic, with fallback, blurred, zoom), `dependencies: ['@cascivo/core']`, tags
      (`['image','media','display','loading']`).
- [ ] **Step 3:** Only if Image surfaces a user-visible string (e.g. a default error/loading `aria-label`), add a
      `image` namespace to `@cascivo/i18n` `builtin.ts`; otherwise rely on the caller's `alt`. No hardcoded English.

### Task 4: Tests

**Files:** `packages/components/src/image/image.test.tsx`

- [ ] **Step 1:** Renders an `<img>` with `src`/`alt`; wrapper carries `data-state="loading"` initially.
- [ ] **Step 2:** Firing `load` on the img transitions to `data-state="loaded"` (the placeholder is hidden).
- [ ] **Step 3:** Firing `error` transitions to `data-state="error"`; with `fallbackSrc`, the fallback `src` is
      used; without it, a fallback box renders (deterministic — fire events, no timers).
- [ ] **Step 4:** `removeWrapper` renders a bare `<img>` (no wrapper/placeholder). A static check confirms no
      `useState`/`useEffect`/`useContext` imported in `image.tsx`.

### Task 5: Export, regenerate, gate

**Files:** `packages/react/src/index.ts`, `packages/components/src/_all-metas.ts`, `registry.json`

- [ ] **Step 1:** Add `export * from '../../components/src/image/image'` to `packages/react/src/index.ts`.
- [ ] **Step 2:** Add `import { meta as imageMeta } from './image/image.meta'` and include it in `_all-metas.ts`'s
      export array (match the existing alphabetical pattern).
- [ ] **Step 3:** `pnpm regen` — `registry.json` gains the `image` entry (category `display`, files, meta); drift
      gate clean.
- [ ] **Step 4:** `pnpm exec vp run @cascivo/components#test` (image green); `pnpm exec vp check`; type check;
      `pnpm breakpoint:check` + `pnpm fallback:check`.

### Task 6: Commit T1

- [ ] **Step 1: Stage and commit**

  ```bash
  git add packages/components/src/image packages/react/src/index.ts packages/components/src/_all-metas.ts registry.json
  git commit -m "$(cat <<'EOF'
  feat(components): add Image (load FSM, blur-up, fallback, zoom) (v41 T1)

  The clearest component gap the HeroUI study found. Signal-driven
  loading→loaded→error FSM modelled on avatar; blur-up/skeleton
  placeholder, graceful fallbackSrc, radius, and optional
  reduced-motion-safe hover zoom. No useState/useEffect; CSS-only
  transitions with static fallbacks. Ships manifest, react export,
  _all-metas entry, registry entry, and tests.

  Co-Authored-By: Claude <noreply@anthropic.com>
  EOF
  )"
  ```
</content>
