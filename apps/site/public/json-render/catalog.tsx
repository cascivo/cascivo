// Generated by scripts/render/generate-catalogs.ts from cascivo’s manifests — do not edit.
//
// A json-render (https://github.com/vercel-labs/json-render) catalog and registry for
// cascivo components. Copy it into your app; it needs @json-render/core,
// @json-render/react, zod and @cascivo/react. Event props become json-render events:
// `onValueChange` is emitted as `valueChange`, so bind it with `on: { valueChange: … }`.
// Render: <JSONUIProvider registry={registry}><Renderer spec={spec} registry={registry} />
// </JSONUIProvider>. Prompt the model with catalog.prompt().
import { createElement, type ComponentType, type ReactNode } from 'react'
import { defineCatalog } from '@json-render/core'
import { defineRegistry } from '@json-render/react'
import { schema } from '@json-render/react/schema'
import { z } from 'zod'
import {
  Accordion,
  Alert,
  AutoGrid,
  Avatar,
  Badge,
  Breadcrumb,
  Button,
  Card,
  Center,
  Checkbox,
  CheckboxCard,
  Columns,
  Combobox,
  CommandMenu,
  DataTable,
  DatePicker,
  Dropdown,
  EmptyState,
  FileUploader,
  Flex,
  Form,
  Grid,
  GridItem,
  Header,
  HeaderPanel,
  Input,
  InputGroup,
  Kbd,
  LargeTitleHeader,
  Link,
  Modal,
  NumberInput,
  OverflowMenu,
  Pagination,
  ProgressBar,
  ProgressIndicator,
  Radio,
  RadioCard,
  RadioCardGroup,
  Search,
  Select,
  Separator,
  ShellHeader,
  SideNav,
  Skeleton,
  Slider,
  Spacer,
  Spinner,
  Switcher,
  Tabs,
  Tag,
  Textarea,
  TimePicker,
  ToastProvider,
  Toggle,
  Tooltip,
} from '@cascivo/react'

export const catalog = defineCatalog(schema, {
  components: {
    Accordion: {
      description: 'Vertically stacked, collapsible content sections Events: valueChange.',
      props: z.object({
        type: z.enum(['single', 'multiple']).optional(),
        defaultValue: z.unknown().optional(),
        value: z.unknown().optional(),
      }),
      slots: ['default'],
    },
    Alert: {
      description: 'Highlights a short, important message inline Events: dismiss.',
      props: z.object({
        variant: z.enum(['default', 'info', 'success', 'warning', 'destructive']).optional(),
        title: z.string().optional(),
        icon: z.unknown().optional(),
        dismissible: z.boolean().optional(),
        action: z.unknown().optional(),
      }),
      slots: ['default'],
    },
    AutoGrid: {
      description: 'Media-query-free responsive grid — columns auto-fill based on available space.',
      props: z.object({
        min: z.string().optional(),
        gap: z.unknown().optional(),
      }),
      slots: ['default'],
    },
    Avatar: {
      description: 'Displays a user image with initials fallback',
      props: z.object({
        src: z.string().optional(),
        alt: z.string().optional(),
        name: z.string().optional(),
        fallback: z.string().optional(),
        size: z.enum(['xs', 'sm', 'md', 'lg', 'xl']).optional(),
        status: z.enum(['online', 'offline', 'busy']).optional(),
      }),
      slots: ['default'],
    },
    Badge: {
      description: 'Small status label or category indicator',
      props: z.object({
        variant: z
          .enum(['default', 'secondary', 'success', 'warning', 'destructive', 'outline'])
          .optional(),
        size: z.enum(['sm', 'md']).optional(),
      }),
      slots: ['default'],
    },
    Breadcrumb: {
      description: 'Shows the current page location within a navigation hierarchy',
      props: z.object({
        items: z.unknown(),
        maxVisible: z.number().optional(),
        className: z.string().optional(),
        label: z.string().optional(),
        ariaLabel: z.string().optional(),
      }),
      slots: ['default'],
    },
    Button: {
      description: 'Triggers an action or event Events: click.',
      props: z.object({
        variant: z.enum(['primary', 'secondary', 'ghost', 'destructive']).optional(),
        size: z.enum(['sm', 'md', 'lg']).optional(),
        loading: z.boolean().optional(),
        disabled: z.boolean().optional(),
        asChild: z.boolean().optional(),
      }),
      slots: ['default'],
    },
    Card: {
      description: 'Container for grouping related content',
      props: z.object({
        actions: z.unknown().optional(),
        variant: z.enum(['default', 'outlined', 'elevated']).optional(),
        padding: z.enum(['none', 'sm', 'md', 'lg']).optional(),
      }),
      slots: ['default'],
    },
    Center: {
      description: 'Horizontally centered container with a configurable max-width.',
      props: z.object({
        maxWidth: z.string().optional(),
      }),
      slots: ['default'],
    },
    Checkbox: {
      description: 'Binary toggle for forms, with indeterminate support Events: change.',
      props: z.object({
        label: z.string().optional(),
        checked: z.boolean().optional(),
        indeterminate: z.boolean().optional(),
        disabled: z.boolean().optional(),
        ariaLabel: z.string().optional(),
      }),
      slots: ['default'],
    },
    CheckboxCard: {
      description:
        'Multi-selectable card backed by a native checkbox. Use multiple independent CheckboxCards for multi-select scenarios. Events: checkedChange.',
      props: z.object({
        title: z.unknown(),
        description: z.unknown().optional(),
        checked: z.boolean().optional(),
        defaultChecked: z.boolean().optional(),
        disabled: z.boolean().optional(),
      }),
      slots: ['default'],
    },
    Columns: {
      description:
        'Equal-width multi-column layout that collapses to single column on narrow viewports.',
      props: z.object({
        count: z.unknown().optional(),
        gap: z.unknown().optional(),
      }),
      slots: ['default'],
    },
    Combobox: {
      description:
        'Filterable single-select with an animated custom listbox, built on the dropdown open/close machine Events: valueChange, searchChange, create, openChange.',
      props: z.object({
        id: z.string().optional(),
        options: z.unknown(),
        value: z.string().optional(),
        defaultValue: z.string().optional(),
        clearable: z.boolean().optional(),
        searchable: z.boolean().optional(),
        loading: z.boolean().optional(),
        filter: z.unknown().optional(),
        creatable: z.boolean().optional(),
        name: z.string().optional(),
        required: z.boolean().optional(),
        open: z.boolean().optional(),
        defaultOpen: z.boolean().optional(),
        label: z.string().optional(),
        hint: z.string().optional(),
        error: z.string().optional(),
        size: z.enum(['sm', 'md', 'lg']).optional(),
        disabled: z.boolean().optional(),
        labels: z.unknown().optional(),
        className: z.string().optional(),
        'aria-labelledby': z.string().optional(),
        'aria-describedby': z.string().optional(),
        'aria-invalid': z.boolean().optional(),
        ariaLabel: z.string().optional(),
      }),
      slots: ['default'],
    },
    CommandMenu: {
      description:
        'Cmd+K command palette with fuzzy search over grouped commands Events: openChange, queryChange.',
      props: z.object({
        open: z.boolean(),
        groups: z.unknown(),
        placeholder: z.string().optional(),
        emptyLabel: z.string().optional(),
        hotkey: z.boolean().optional(),
        ariaLabel: z.string().optional(),
        label: z.string().optional(),
        loading: z.boolean().optional(),
        scopes: z.unknown().optional(),
        className: z.string().optional(),
      }),
      slots: ['default'],
    },
    DataTable: {
      description:
        'Signal-driven data table with client/server sort, filter, pagination, multi-selection, expandable rows, and CSS content-visibility row containment for large datasets Events: sortChange, filtersChange, columnStateChange, cellEdit.',
      props: z.object({
        virtualized: z.boolean().optional(),
        rowHeight: z.number().optional(),
        windowSize: z.number().optional(),
        overscan: z.number().optional(),
        columns: z.unknown(),
        rows: z.unknown(),
        getRowId: z.unknown().optional(),
        sort: z.unknown().optional(),
        defaultSort: z.unknown().optional(),
        sortMode: z.enum(['client', 'server']).optional(),
        searchable: z.boolean().optional(),
        pagination: z.unknown().optional(),
        selection: z.unknown().optional(),
        batchActions: z.unknown().optional(),
        filters: z.unknown().optional(),
        defaultFilters: z.unknown().optional(),
        noResultsState: z.unknown().optional(),
        toolbar: z.unknown().optional(),
        rowActions: z.unknown().optional(),
        columnState: z.unknown().optional(),
        defaultColumnState: z.unknown().optional(),
        columnSettings: z.unknown().optional(),
        server: z.unknown().optional(),
        multiSort: z.boolean().optional(),
        stateKey: z.string().optional(),
        keyboardNavigation: z.enum(['row', 'grid']).optional(),
        groupBy: z.unknown().optional(),
        totals: z.boolean().optional(),
        pinnedRows: z.unknown().optional(),
        columnGroups: z.unknown().optional(),
        exportable: z.unknown().optional(),
        renderExpandedRow: z.unknown().optional(),
        density: z.enum(['compact', 'normal', 'relaxed']).optional(),
        zebra: z.boolean().optional(),
        stickyHeader: z.boolean().optional(),
        loading: z.boolean().optional(),
        emptyState: z.unknown().optional(),
        ariaLabel: z.string().optional(),
        title: z.string().optional(),
        description: z.string().optional(),
        labels: z.unknown().optional(),
        className: z.string().optional(),
      }),
      slots: ['default'],
    },
    DatePicker: {
      description:
        'An accessible date-picker with a calendar popover. Events: valueChange, openChange.',
      props: z.object({
        id: z.string().optional(),
        value: z.string().optional(),
        defaultValue: z.string().optional(),
        min: z.string().optional(),
        max: z.string().optional(),
        clearable: z.boolean().optional(),
        typeable: z.boolean().optional(),
        disabledDate: z.unknown().optional(),
        format: z.unknown().optional(),
        showToday: z.boolean().optional(),
        name: z.string().optional(),
        required: z.boolean().optional(),
        open: z.boolean().optional(),
        label: z.string().optional(),
        hint: z.string().optional(),
        error: z.string().optional(),
        size: z.enum(['sm', 'md', 'lg']).optional(),
        disabled: z.boolean().optional(),
        labels: z.unknown().optional(),
        'aria-labelledby': z.string().optional(),
        'aria-describedby': z.string().optional(),
        'aria-invalid': z.boolean().optional(),
        ariaLabel: z.string().optional(),
      }),
      slots: ['default'],
    },
    Dropdown: {
      description: 'Menu of actions revealed from a trigger Events: select, openChange.',
      props: z.object({
        trigger: z.unknown(),
        items: z.unknown(),
        placement: z.enum(['bottom-start', 'bottom-end']).optional(),
        open: z.boolean().optional(),
      }),
      slots: ['default'],
    },
    EmptyState: {
      description: 'Placeholder for views that have no data to display',
      props: z.object({
        icon: z.unknown().optional(),
        title: z.string(),
        description: z.string().optional(),
        action: z.unknown().optional(),
        size: z.enum(['md', 'lg']).optional(),
      }),
      slots: ['default'],
    },
    FileUploader: {
      description:
        'Drag-and-drop file upload zone with file list and status indicators. Events: filesAdded, remove, rejected.',
      props: z.object({
        files: z.unknown().optional(),
        multiple: z.boolean().optional(),
        accept: z.string().optional(),
        maxSize: z.number().optional(),
        label: z.string().optional(),
        hint: z.string().optional(),
        labels: z.unknown().optional(),
        disabled: z.boolean().optional(),
        ariaLabel: z.string().optional(),
      }),
      slots: ['default'],
    },
    Flex: {
      description:
        'Flex layout primitive for vertical or horizontal stacking with gap control. ⚠ Unlike CSS `flex-direction`, `direction` defaults to `vertical` — pass `direction="horizontal"` for a row.',
      props: z.object({
        direction: z.enum(['vertical', 'horizontal']).optional(),
        gap: z.unknown().optional(),
        align: z.enum(['start', 'center', 'end', 'stretch']).optional(),
        justify: z.enum(['start', 'center', 'end', 'between']).optional(),
        wrap: z.boolean().optional(),
        size: z.enum(['auto', 'fixed', 'grow', 'shrink']).optional(),
        basis: z.string().optional(),
        truncate: z.boolean().optional(),
      }),
      slots: ['default'],
    },
    Form: {
      description:
        'Typed signal-based form store (createForm/useForm) with sync/async validation and a thin Form element wrapper Events: valid.',
      props: z.object({
        form: z.unknown(),
        className: z.string().optional(),
      }),
      slots: ['default'],
    },
    Grid: {
      description:
        'CSS grid layout primitive with responsive column collapsing. Establishes its own containment, so responsive `cols` adapt to the grid’s own slot width with no wrapper or container ancestor required.',
      props: z.object({
        cols: z.unknown().optional(),
        gap: z.unknown().optional(),
        align: z.enum(['start', 'center', 'end', 'stretch']).optional(),
        justify: z.enum(['start', 'center', 'end', 'stretch']).optional(),
        alignRows: z.boolean().optional(),
        span: z.unknown().optional(),
      }),
      slots: ['default'],
    },
    GridItem: {
      description: 'One cell of a Grid, spanning columns and rows.',
      props: z.record(z.string(), z.unknown()),
      slots: ['default'],
    },
    Header: {
      description: 'App top bar with brand, primary navigation links, and an actions slot',
      props: z.object({
        labels: z.unknown().optional(),
        brand: z.unknown().optional(),
        links: z.unknown().optional(),
        actions: z.unknown().optional(),
        sticky: z.boolean().optional(),
        className: z.string().optional(),
      }),
      slots: ['default'],
    },
    HeaderPanel: {
      description:
        'Non-modal panel anchored below the shell header at the inline-end edge — hosts notifications, app switcher, user settings Events: close.',
      props: z.object({
        open: z.boolean(),
        label: z.string(),
        labels: z.unknown().optional(),
        className: z.string().optional(),
      }),
      slots: ['default'],
    },
    Input: {
      description: 'Text input field with optional label, hint, and error state',
      props: z.object({
        label: z.string().optional(),
        hint: z.string().optional(),
        error: z.string().optional(),
        size: z.enum(['sm', 'md', 'lg']).optional(),
        placeholder: z.string().optional(),
        disabled: z.boolean().optional(),
        ariaLabel: z.string().optional(),
      }),
      slots: ['default'],
    },
    InputGroup: {
      description:
        'Prefix/suffix addon wrapper for Input; InputGroupAddon renders inline icons/units inside the field border; ButtonGroup collapses adjacent button borders',
      props: z.object({
        prefix: z.unknown().optional(),
        suffix: z.unknown().optional(),
      }),
      slots: ['default'],
    },
    Kbd: {
      description: 'Displays a keyboard key or shortcut',
      props: z.object({
        size: z.enum(['sm', 'md']).optional(),
      }),
      slots: ['default'],
    },
    LargeTitleHeader: {
      description:
        'Scrolling region whose large title collapses into a compact sticky bar as it scrolls',
      props: z.object({
        title: z.string(),
        leading: z.unknown().optional(),
        actions: z.unknown().optional(),
        level: z.unknown().optional(),
        collapseDistance: z.number().optional(),
        className: z.string().optional(),
      }),
      slots: ['default'],
    },
    Link: {
      description: 'Styled anchor for navigation, standalone or inline within prose',
      props: z.object({
        variant: z.enum(['standalone', 'inline']).optional(),
        size: z.enum(['sm', 'md', 'lg']).optional(),
        external: z.boolean().optional(),
        href: z.string().optional(),
        asChild: z.boolean().optional(),
      }),
      slots: ['default'],
    },
    Modal: {
      description: 'Accessible dialog overlay using native <dialog> element Events: close.',
      props: z.object({
        open: z.boolean().optional(),
        title: z.string().optional(),
        description: z.string().optional(),
        size: z.enum(['sm', 'md', 'lg']).optional(),
        footer: z.unknown().optional(),
        draggable: z.boolean().optional(),
      }),
      slots: ['default'],
    },
    NumberInput: {
      description:
        'Numeric input with stepper buttons, clamping, precision, and locale formatting Events: valueChange.',
      props: z.object({
        value: z.union([z.number(), z.null()]).optional(),
        defaultValue: z.number().optional(),
        min: z.number().optional(),
        max: z.number().optional(),
        step: z.number().optional(),
        precision: z.number().optional(),
        formatOptions: z.unknown().optional(),
        label: z.string().optional(),
        hint: z.string().optional(),
        error: z.string().optional(),
        size: z.enum(['sm', 'md', 'lg']).optional(),
        disabled: z.boolean().optional(),
        incrementLabel: z.string().optional(),
        decrementLabel: z.string().optional(),
        ariaLabel: z.string().optional(),
      }),
      slots: ['default'],
    },
    OverflowMenu: {
      description: 'Kebab icon button revealing a menu of row-level actions Events: select.',
      props: z.object({
        items: z.unknown(),
        placement: z.enum(['bottom-start', 'bottom-end']).optional(),
        label: z.string().optional(),
        ariaLabel: z.string().optional(),
        size: z.enum(['sm', 'md']).optional(),
        disabled: z.boolean().optional(),
        tabIndex: z.number().optional(),
        className: z.string().optional(),
      }),
      slots: ['default'],
    },
    Pagination: {
      description:
        'Controls for navigating paged data sets, with page size selection Events: pageChange, pageSizeChange.',
      props: z.object({
        page: z.number(),
        pageSize: z.number(),
        totalItems: z.number(),
        pageSizeOptions: z.unknown().optional(),
        labels: z.unknown().optional(),
        className: z.string().optional(),
      }),
      slots: ['default'],
    },
    ProgressBar: {
      description: 'Shows determinate or indeterminate progress of a task',
      props: z.object({
        value: z.number().optional(),
        max: z.number().optional(),
        label: z.string().optional(),
        helperText: z.string().optional(),
        size: z.enum(['sm', 'md']).optional(),
        status: z.enum(['active', 'success', 'error']).optional(),
      }),
      slots: ['default'],
    },
    ProgressIndicator: {
      description: 'Shows progress through the steps of a multi-step flow',
      props: z.object({
        steps: z.unknown(),
        currentIndex: z.number(),
        vertical: z.boolean().optional(),
        className: z.string().optional(),
      }),
      slots: ['default'],
    },
    Radio: {
      description: 'Single choice from a set, grouped with RadioGroup',
      props: z.object({
        label: z.string().optional(),
        value: z.string(),
        disabled: z.boolean().optional(),
        name: z.string().optional(),
        ariaLabel: z.string().optional(),
      }),
      slots: ['default'],
    },
    RadioCard: {
      description:
        'Selectable card backed by a native radio input. Use RadioCardGroup for single-select groups.',
      props: z.object({
        hideIndicator: z.boolean().optional(),
        value: z.string(),
        title: z.unknown(),
        description: z.unknown().optional(),
        disabled: z.boolean().optional(),
      }),
      slots: ['default'],
    },
    RadioCardGroup: {
      description: 'Groups RadioCard options into one single-choice field.',
      props: z.record(z.string(), z.unknown()),
      slots: ['default'],
    },
    Search: {
      description:
        'Search input with debounced search callback and clear button Events: valueChange, search.',
      props: z.object({
        value: z.string().optional(),
        defaultValue: z.string().optional(),
        debounceMs: z.number().optional(),
        placeholder: z.string().optional(),
        size: z.enum(['sm', 'md', 'lg']).optional(),
        label: z.string().optional(),
        disabled: z.boolean().optional(),
        clearLabel: z.string().optional(),
        id: z.string().optional(),
        className: z.string().optional(),
        'aria-labelledby': z.string().optional(),
        'aria-describedby': z.string().optional(),
        'aria-invalid': z.boolean().optional(),
        ariaLabel: z.string().optional(),
      }),
      slots: ['default'],
    },
    Select: {
      description: 'Native select menu styled to match the design system',
      props: z.object({
        label: z.string().optional(),
        hint: z.string().optional(),
        error: z.string().optional(),
        placeholder: z.string().optional(),
        options: z.unknown(),
        size: z.enum(['sm', 'md', 'lg']).optional(),
        disabled: z.boolean().optional(),
        ariaLabel: z.string().optional(),
      }),
      slots: ['default'],
    },
    Separator: {
      description: 'Visual or semantic divider between content',
      props: z.object({
        orientation: z.enum(['horizontal', 'vertical']).optional(),
        decorative: z.boolean().optional(),
      }),
      slots: ['default'],
    },
    ShellHeader: {
      description:
        'Console application header: brand with prefix, dropdown nav menus, global icon actions, hamburger, skip-to-content Events: menuClick.',
      props: z.object({
        brand: z.unknown().optional(),
        nav: z.unknown().optional(),
        center: z.unknown().optional(),
        actions: z.unknown().optional(),
        end: z.unknown().optional(),
        menuExpanded: z.boolean().optional(),
        skipToContentHref: z.union([z.string(), z.boolean()]).optional(),
        labels: z.unknown().optional(),
      }),
      slots: ['default'],
    },
    SideNav: {
      description:
        'Collapsible sidebar navigation with optional icons and one level of grouping Events: collapsedChange.',
      props: z.object({
        groups: z.unknown().optional(),
        showCollapseToggle: z.boolean().optional(),
        items: z.unknown().optional(),
        collapsed: z.boolean().optional(),
        defaultCollapsed: z.boolean().optional(),
        label: z.string().optional(),
        ariaLabel: z.string().optional(),
        collapseLabel: z.string().optional(),
        expandLabel: z.string().optional(),
        expandOnHover: z.boolean().optional(),
        header: z.unknown().optional(),
        footer: z.unknown().optional(),
        className: z.string().optional(),
      }),
      slots: ['default'],
    },
    Skeleton: {
      description: 'Animated loading placeholder that mirrors the shape of pending content',
      props: z.object({
        variant: z.enum(['text', 'circle', 'rect']).optional(),
        width: z.string().optional(),
        height: z.string().optional(),
        lines: z.number().optional(),
      }),
      slots: ['default'],
    },
    Slider: {
      description: 'Range input for selecting a value within bounds',
      props: z.object({
        label: z.string().optional(),
        min: z.number().optional(),
        max: z.number().optional(),
        step: z.number().optional(),
        value: z.number().optional(),
        defaultValue: z.number().optional(),
        disabled: z.boolean().optional(),
        ariaLabel: z.string().optional(),
      }),
      slots: ['default'],
    },
    Spacer: {
      description: 'Fixed-height spacing block using design token steps.',
      props: z.object({
        size: z.unknown().optional(),
      }),
      slots: ['default'],
    },
    Spinner: {
      description: 'Indeterminate loading indicator',
      props: z.object({
        size: z.enum(['sm', 'md', 'lg']).optional(),
        ariaLabel: z.string().optional(),
        label: z.string().optional(),
      }),
      slots: ['default'],
    },
    Switcher: {
      description:
        'Always-visible app/product switcher LIST — every entry is rendered at once, it does not collapse. Lives inside HeaderPanel; renders links with an active indicator and optional dividers. For a collapsed trigger with a menu, use Dropdown.',
      props: z.object({
        items: z.unknown(),
        ariaLabel: z.string().optional(),
        label: z.string().optional(),
        className: z.string().optional(),
      }),
      slots: ['default'],
    },
    Tabs: {
      description: 'Switch between related panels of content Events: valueChange.',
      props: z.object({
        defaultValue: z.string().optional(),
        value: z.string().optional(),
      }),
      slots: ['default'],
    },
    Tag: {
      description: 'Compact chip for labeling, categorizing, or filtering content Events: dismiss.',
      props: z.object({
        variant: z.enum(['default', 'info', 'success', 'warning', 'error']).optional(),
        size: z.enum(['sm', 'md']).optional(),
        dismissLabel: z.string().optional(),
      }),
      slots: ['default'],
    },
    Textarea: {
      description: 'Multi-line text input with optional label, hint, and error state',
      props: z.object({
        label: z.string().optional(),
        hint: z.string().optional(),
        error: z.string().optional(),
        rows: z.number().optional(),
        resize: z.enum(['none', 'vertical', 'both']).optional(),
        autosize: z.boolean().optional(),
        disabled: z.boolean().optional(),
        ariaLabel: z.string().optional(),
      }),
      slots: ['default'],
    },
    TimePicker: {
      description:
        'Native time input wrapper with label, hint, error, and size variants Events: valueChange.',
      props: z.object({
        value: z.string().optional(),
        defaultValue: z.string().optional(),
        min: z.string().optional(),
        max: z.string().optional(),
        step: z.number().optional(),
        label: z.string().optional(),
        hint: z.string().optional(),
        error: z.string().optional(),
        size: z.enum(['sm', 'md', 'lg']).optional(),
        disabled: z.boolean().optional(),
        className: z.string().optional(),
        ariaLabel: z.string().optional(),
      }),
      slots: ['default'],
    },
    Toast: {
      description: 'Transient notification surfaced via the useToast hook',
      props: z.object({
        title: z.string(),
        description: z.string().optional(),
        variant: z.enum(['default', 'success', 'warning', 'destructive']).optional(),
        duration: z.number().optional(),
      }),
      slots: ['default'],
    },
    Toggle: {
      description: 'On/off switch built as an accessible button Events: valueChange.',
      props: z.object({
        checked: z.boolean().optional(),
        defaultChecked: z.boolean().optional(),
        label: z.string().optional(),
        size: z.enum(['sm', 'md']).optional(),
        disabled: z.boolean().optional(),
        ariaLabel: z.string().optional(),
      }),
      slots: ['default'],
    },
    Tooltip: {
      description: 'Contextual label shown on hover or focus',
      props: z.object({
        content: z.unknown(),
        placement: z.enum(['top', 'right', 'bottom', 'left']).optional(),
        delay: z.number().optional(),
      }),
      slots: ['default'],
    },
  },
  actions: {},
})

type Rendered = ComponentType<Record<string, unknown>>

interface RenderArgs {
  props: Record<string, unknown>
  children?: ReactNode
  emit: (event: string) => void
}

function render(component: unknown, events: string[]) {
  const Component = component as Rendered
  return ({ props, children, emit }: RenderArgs) => {
    const handlers: Record<string, () => void> = {}
    for (const prop of events) {
      handlers[prop] = () => emit(prop.charAt(2).toLowerCase() + prop.slice(3))
    }
    return createElement(Component, { ...props, ...handlers }, children)
  }
}

export const { registry } = defineRegistry(catalog, {
  components: {
    Accordion: render(Accordion, ['onValueChange']),
    Alert: render(Alert, ['onDismiss']),
    AutoGrid: render(AutoGrid, []),
    Avatar: render(Avatar, []),
    Badge: render(Badge, []),
    Breadcrumb: render(Breadcrumb, []),
    Button: render(Button, ['onClick']),
    Card: render(Card, []),
    Center: render(Center, []),
    Checkbox: render(Checkbox, ['onChange']),
    CheckboxCard: render(CheckboxCard, ['onCheckedChange']),
    Columns: render(Columns, []),
    Combobox: render(Combobox, ['onValueChange', 'onSearchChange', 'onCreate', 'onOpenChange']),
    CommandMenu: render(CommandMenu, ['onOpenChange', 'onQueryChange']),
    DataTable: render(DataTable, [
      'onSortChange',
      'onFiltersChange',
      'onColumnStateChange',
      'onCellEdit',
    ]),
    DatePicker: render(DatePicker, ['onValueChange', 'onOpenChange']),
    Dropdown: render(Dropdown, ['onSelect', 'onOpenChange']),
    EmptyState: render(EmptyState, []),
    FileUploader: render(FileUploader, ['onFilesAdded', 'onRemove', 'onRejected']),
    Flex: render(Flex, []),
    Form: render(Form, ['onValid']),
    Grid: render(Grid, []),
    GridItem: render(GridItem, []),
    Header: render(Header, []),
    HeaderPanel: render(HeaderPanel, ['onClose']),
    Input: render(Input, []),
    InputGroup: render(InputGroup, []),
    Kbd: render(Kbd, []),
    LargeTitleHeader: render(LargeTitleHeader, []),
    Link: render(Link, []),
    Modal: render(Modal, ['onClose']),
    NumberInput: render(NumberInput, ['onValueChange']),
    OverflowMenu: render(OverflowMenu, ['onSelect']),
    Pagination: render(Pagination, ['onPageChange', 'onPageSizeChange']),
    ProgressBar: render(ProgressBar, []),
    ProgressIndicator: render(ProgressIndicator, []),
    Radio: render(Radio, []),
    RadioCard: render(RadioCard, []),
    RadioCardGroup: render(RadioCardGroup, []),
    Search: render(Search, ['onValueChange', 'onSearch']),
    Select: render(Select, []),
    Separator: render(Separator, []),
    ShellHeader: render(ShellHeader, ['onMenuClick']),
    SideNav: render(SideNav, ['onCollapsedChange']),
    Skeleton: render(Skeleton, []),
    Slider: render(Slider, []),
    Spacer: render(Spacer, []),
    Spinner: render(Spinner, []),
    Switcher: render(Switcher, []),
    Tabs: render(Tabs, ['onValueChange']),
    Tag: render(Tag, ['onDismiss']),
    Textarea: render(Textarea, []),
    TimePicker: render(TimePicker, ['onValueChange']),
    Toast: render(ToastProvider, []),
    Toggle: render(Toggle, ['onValueChange']),
    Tooltip: render(Tooltip, []),
  },
})
