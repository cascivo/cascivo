# v17 Tranche 6 — Final gate, mobile re-verify, DoD, roadmap close

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Steps use checkbox
> (`- [ ]`) syntax.
>
> T1–T5 are done: the `/guides` page is complete (hero, migration, customization, use cases,
> boundaries, FAQ, CTA), claims 31–33 are on `/why`. This tranche proves the whole thing passes the
> full CLAUDE.md gate, re-verifies mobile across the entire page, walks the DoD, and closes the
> roadmap. No new content — only verification and the close-out.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - The full `/guides` section order in `GuidesPage.tsx` is: `GuidesHero` → `MigrationGuide` →
>   `BrandCustomization` → `UseCaseScenarios` → `WhenNotToUse` → `GuidesFaq` → `GuidesCta`.
> - Claims 31, 32, 33 are present in `apps/docs/src/pages/WhyCascadePage.tsx`.
> - `/guides` is registered in the mobile overflow harness (`apps/landing/test/mobile.spec.ts`).

**Goal:** All five CLAUDE.md gate commands exit 0, the Playwright mobile harness passes for the whole
`/guides` page at 320/375/390/414, every DoD box in `ROADMAP-V17.md` is verified and checked, and the
roadmap status flips to ✅ Shipped.

**Architecture:** Verification only. The sole content edit is checking DoD boxes + status in
`ROADMAP-V17.md`. Any failure found here is fixed in the tranche that owns it (commit the fix), then
re-gate.

**Tech stack:** unchanged.

---

### Task 1: Full CLAUDE.md gate

**Files:**

- Run only (fix in the owning tranche if anything fails).

- [ ] **Step 1:** `pnpm exec vp check` — 0 errors; warnings at or below the pre-existing baseline.
- [ ] **Step 2:** `pnpm build` — all packages/apps build (landing must build without a prior full
      build; if a `@cascivo/components` alias was added in T5, confirm the landing builds clean).
- [ ] **Step 3:** `pnpm exec vp run -r check` — type check passes, 0 errors.
- [ ] **Step 4:** `pnpm test` — all tests pass.
- [ ] **Step 5:** Drift check — `pnpm regen` → `pnpm exec vp check --fix` → `git diff --exit-code`
      exits 0 (no uncommitted regenerated artifacts). If `regen` changed anything (e.g. the `/why`
      claims feed a generated artifact), commit it.

---

### Task 2: Playwright mobile re-verify (whole page)

**Files:**

- Run: `apps/landing/test/mobile.spec.ts`

- [ ] **Step 1:** `pnpm exec playwright test apps/landing/test/mobile.spec.ts` — `/guides` passes at
      320/375/390/414 with ALL sections present, zero horizontal overflow.
- [ ] **Step 2:** Manually spot-check the two highest-risk spots at 320px: the migration code snippets
      and the customization code snippets scroll WITHIN their `<pre>` box (do not widen the page); the
      FAQ accordion expands/collapses without overflow.
- [ ] **Step 3:** If any overflow is found, fix in the owning tranche's CSS block (commit), then re-run.

---

### Task 3: DoD walkthrough

**Files:**

- Verify against `docs/ROADMAP-V17.md` Definition of Done.

- [ ] **Step 1:** Walk each DoD line and confirm the evidence exists:
  - `/guides` route + active "Guides" nav link (T1).
  - Migration guide: transfers/changes model + numbered steps + live-bench honest comparison linking
    `/performance` + `/accessibility` (T2). Grep the migration component for hardcoded competitor
    numbers — there must be none.
  - Customization guide: three-tier overrides + real copyable snippets + `data-theme` scoping +
    create-theme skill / MCP tool links (T3).
  - Use-case scenarios (≥4) + "when not to use" with real boundaries + receipts (T4).
  - FAQ on the shipped `Accordion` (≥6 Qs), each answer ending in a next-step link (T5).
  - Claims 31–33 on `/why` with receipts into `/guides` (T2/T3/T4).
- [ ] **Step 2:** Confirm zero `useState`/`useEffect`/`useContext` across all new `guides/` files:
      `grep -rn "useState\|useEffect\|useContext\|useReducer\|useLayoutEffect" apps/landing/src/pages/guides apps/landing/src/pages/GuidesPage.tsx` → 0 results.
- [ ] **Step 3:** Confirm no hardcoded hex/rgb colors in the new CSS blocks (all `var(--cascivo-*)`).

---

### Task 4: Close the roadmap

**Files:**

- Modify: `docs/ROADMAP-V17.md`

- [ ] **Step 1:** Check every DoD box (`[ ]` → `[x]`) with a one-line evidence note per box (mirror the
      v16 roadmap's filled-in DoD style).
- [ ] **Step 2:** Update the header: `Status:` → `✅ Shipped`; `Last updated:` → the close date.
- [ ] **Step 3: Commit** — `docs(roadmap): close v17 — the guides layer shipped (v17-t6)`
- [ ] **Step 4:** Push the branch: `git push -u origin claude/landing-page-roadmap-plan-zr6ssm`
      (retry on network error with exponential backoff per the git instructions). Do NOT open a PR
      unless explicitly asked.

---

## Tranche exit criteria

1. All five CLAUDE.md gate commands exit 0 (`vp check`, build, `vp run -r check`, test, regen+diff).
2. Playwright mobile passes for the full `/guides` page at 320/375/390/414 — zero overflow.
3. Every `ROADMAP-V17.md` DoD box is checked with an evidence note; status is ✅ Shipped.
4. Zero banned hooks and zero hardcoded colors across the new `guides/` files and CSS.
5. The branch is pushed; no PR opened (unless explicitly requested).
