# v17 Tranche 4 — Use-case scenarios + "when not to use cascivo"

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. ALSO use compound-engineering:frontend-design for visual quality +
> screenshot verification. Steps use checkbox (`- [ ]`) syntax.
>
> T1–T3 are done (scaffold, migration, customization). Now frame WHO cascivo is for — and, just as
> importantly, who it isn't for. Research is explicit: a candid "when not to use" section is what
> separates a credible guide from a sales page. Both sections ship in this tranche.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - cascivo's real strengths (for the scenario→strength mapping): AI-first context layer (MCP, manifests,
>   `cascivo audit --ai`), fine-grained signals (fewest re-renders in the bench — see
>   `/performance`), 10 themes + token customization, WCAG 2.2 AA built in (see `/accessibility`),
>   copy-own model, RSC-compatible. Confirm each against the landing it links to.
> - cascivo's real boundaries (for "when not to use"): CSS `@function`/`if()` is Chrome-leading
>   PROGRESSIVE ENHANCEMENT (Safari/Firefox get static fallbacks — NOT a regression; state it
>   honestly, link the fallback evidence); `vp` (vite+) tooling is alpha (accepted risk); React/Preact
>   focus (no Vue/Svelte/Angular); browser targets are last-2-versions (needs `:has()`/`@container`).
>   RE-VERIFY these against `CLAUDE.md` before stating them.
> - `apps/docs/src/pages/WhyCascadePage.tsx` — claim 33 lands here.

**Goal:** Two sections on `/guides`:

1. `UseCaseScenarios` (anchor `#use-cases`) — a bento-style grid of ≥4 scenarios, each mapping a real
   situation to the cascivo strength that wins there, with a link to the proof.
2. `WhenNotToUse` (anchor `#when-not`) — an honest boundaries section stating real limits, each routed
   to its honest receipt.

Both render with zero horizontal overflow at 320–414px.

**Architecture:** New `apps/landing/src/pages/guides/UseCaseScenarios.tsx` and
`guides/WhenNotToUse.tsx`, content in `guides/data.ts`, styles in `landing.css`, claim 33 in
`WhyCascadePage.tsx`. No package changes.

**Tech stack:** unchanged. Mobile-first CSS. `useSignals()` only if a signal is read.

---

### Task 1: Draft the scenarios + boundaries (grounded, not invented)

**Files:**

- Read: `CLAUDE.md` (Core Principles, browser/a11y targets, dependency policy), `/performance` and
  `/accessibility` page sources (to link each scenario/boundary to a real receipt).

- [ ] **Step 1:** Draft ≥4 use-case scenarios as `{ persona, situation, why, receipt }`:
      - "You're building UIs with an AI agent" → manifests + MCP + `audit --ai` keep generated code
        correct → receipt `/context` (or `/ai`).
      - "You're shipping a multi-brand product" → `data-theme` scoping + token overrides → receipt
        `/guides#customize`.
      - "Performance is a hard requirement" → signal-driven, fewest re-renders → receipt `/performance`.
      - "Accessibility is mandatory (gov/enterprise/regulated)" → WCAG 2.2 AA built in, zero axe
        violations in the bench → receipt `/accessibility`.
      - (optional 5th) "You want to own your components, not rent them" → copy-own model → receipt
        QuickStart.
- [ ] **Step 2:** Draft the boundaries as `{ limit, honest_framing, receipt }`:
      - CSS `@function`/`if()` is Chrome-leading progressive enhancement → "works pixel-identically in
        Safari/Firefox via static fallbacks; the CSS-native path is a forward pilot" → receipt: the
        functions.css / fallback-audit evidence (re-verify the link used by claim 18 on `/why`).
      - `vp` (vite+) is alpha → "accepted risk for a greenfield system; pinned + tracked" → receipt:
        CLAUDE.md dependency policy / viteplus.dev.
      - React/Preact only → "if you're on Vue/Svelte/Angular, cascivo isn't your library today."
      - Modern-browser targets → "needs `:has()`/`@container`; last-2-versions Chrome/Firefox/Safari."
      Keep the framing honest and non-defensive — candor is the point.

---

### Task 2: Add content to data.ts

**Files:**

- Modify: `apps/landing/src/pages/guides/data.ts`

- [ ] **Step 1:** Add typed `SCENARIOS: {persona, situation, why, receipt:{label,href}}[]` and
      `BOUNDARIES: {limit, framing, receipt:{label,href}}[]` exports.

---

### Task 3: Build the two sections

**Files:**

- Create: `apps/landing/src/pages/guides/UseCaseScenarios.tsx`
- Create: `apps/landing/src/pages/guides/WhenNotToUse.tsx`

- [ ] **Step 1:** `UseCaseScenarios` — `id="use-cases"`, `<h2>`, a bento/card grid (scannable modular
      blocks per the research). Each card: persona heading, situation line, "why cascivo wins here"
      line, and a "See the proof →" link. Single column on mobile; 2-up then bento on wider screens.
- [ ] **Step 2:** `WhenNotToUse` — `id="when-not"`, `<h2>`, an intro that sets the honest tone
      ("Here's where cascivo is the wrong tool — or only a forward bet."), then a list of boundaries,
      each with its honest framing and a receipt link. Visually quieter than the scenarios grid
      (it's candor, not a pitch) — but not hidden.
- [ ] **Step 3:** Static content only unless a signal is read. Wire both into `GuidesPage.tsx` after
      `BrandCustomization`, in order: `UseCaseScenarios` then `WhenNotToUse`.

---

### Task 4: Style + claim 33

**Files:**

- Modify: `apps/landing/src/landing.css`
- Modify: `apps/docs/src/pages/WhyCascadePage.tsx`

- [ ] **Step 1:** Add `/* ── Guides: use cases ── */` and `/* ── Guides: boundaries ── */` CSS.
      Mobile-first: scenario cards single column < 48rem, grid above; boundaries list single column
      throughout (readability over density). Token-based colors only. The boundaries section uses a
      subtle/muted surface to read as "honest aside," not "warning/error" — no red/destructive tokens.
- [ ] **Step 2:** Add claim 33 to `WhyCascadePage.tsx` `CLAIMS`:
      - title: "Honest about fit"
      - substance: one sentence — use-case scenarios that say where cascivo wins, and a "when not to
        use" section that says where it doesn't.
      - receipt: `{ label: 'Use cases & honest boundaries', href: '/guides#use-cases' }`.
- [ ] **Step 3:** Run the mobile spec for `/guides` — zero overflow across both new sections.
- [ ] **Step 4: Commit** — `feat(landing): use-case scenarios + when-not-to-use on /guides + claim 33 (v17-t4)`

---

## Tranche exit criteria

1. `/guides#use-cases` renders ≥4 scenario cards, each mapping a situation to a cascivo strength with a
   "see the proof" link to the real receipt page.
2. `/guides#when-not` renders an honest boundaries section with ≥3 real limits, each framed candidly and
   linked to its receipt; no destructive/error styling.
3. Claim 33 is live on `/why` with a receipt linking to `/guides#use-cases`.
4. Mobile harness passes at 320/375/390/414 — zero overflow in both new sections.
5. No `useState`/`useEffect`/`useContext`; `useSignals()` only if a signal is read; no hardcoded colors.
