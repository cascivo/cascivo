# v22 Tranche 5 — CI consolidation + READMEs + full gate + close

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans.
>
> Final tranche. Depends on T1–T4. Folds the five standalone demo deploys into the landing's single
> Cloudflare Pages deploy, widens the `landing` paths-filter, removes the now-redundant per-demo jobs and
> CF project vars, updates demo READMEs/specs for the new live URLs, runs the full CLAUDE.md gate, and
> closes the roadmap.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - `.github/workflows/cf-pages.yml` has a `changes` job (dorny/paths-filter) emitting per-app booleans
>   and separate `deploy-{docs,landing,storybook,playground,deploy,pay,flow,track,pulse}` jobs. The
>   `landing` filter watches `apps/landing/**`, `packages/tokens/**`, `packages/themes/**`,
>   `pnpm-lock.yaml`. `deploy-landing` runs `vp run @cascivo/landing#build` then
>   `wrangler pages deploy apps/landing/dist --project-name=$CF_PROJECT_LANDING`.
> - The five `deploy-{deploy,pay,flow,track,pulse}` jobs deploy to `CF_PROJECT_{DEPLOY,PAY,FLOW,TRACK,PULSE}`.
> - `scripts/assemble-demos.mjs` + `build:landing-demos` (T1) build the landing then assemble demos into
>   `apps/landing/dist/demos/<name>/`.
> - Each demo has a README (v21) and a Playwright spec.

**Goal:** One Cloudflare Pages deploy (landing) carries the homepage, the `/examples` marketing pages,
and all five live `/demos/<name>/` apps. The five separate demo deploy jobs and their CF project vars
are gone. The `landing` paths-filter triggers on demo/kit/package changes too. Demo docs reflect the
new same-origin live URLs. The full gate is green and the roadmap is Shipped.

**Architecture:** Edit `.github/workflows/cf-pages.yml` only (CI). Edit five demo READMEs + specs (docs/
URL touch-ups). Flip `docs/ROADMAP-V22.md` status.

**Tech stack:** unchanged.

---

### Task 1: Fold demo deploys into `deploy-landing`

**Files:** `.github/workflows/cf-pages.yml`

- [ ] **Step 1:** In `deploy-landing`, replace the single build step with: `pnpm install --frozen-lockfile`
      → build the five demos + landing + assemble (run `node scripts/assemble-demos.mjs --build` after
      the landing build, or call the `build:landing-demos` script) → `wrangler pages deploy
      apps/landing/dist --project-name=$CF_PROJECT_LANDING`. The deployed `apps/landing/dist` now
      contains `/demos/<name>/`. Confirm each demo build uses its source aliases (no prior full build) —
      they do (per-app `vite.config.ts`), so no extra alias work.
- [ ] **Step 2:** Widen the `landing` paths-filter to also watch `apps/examples/{deploy,pay,flow,track,pulse}/**`,
      `apps/examples/kit/**`, `packages/**`, and `scripts/assemble-demos.mjs`. (A demo change must now
      redeploy the landing, since that's where it ships.)
- [ ] **Step 3:** Remove the five `deploy-{deploy,pay,flow,track,pulse}` jobs and their per-app outputs
      in the `changes` job (the `deploy/pay/flow/track/pulse` filter keys + `out` echoes). Leave docs,
      storybook, playground untouched.

### Task 2: Retire redundant CF project vars

**Files:** `.github/workflows/cf-pages.yml`; note any external var docs

- [ ] **Step 1:** Drop references to `CF_PROJECT_{DEPLOY,PAY,FLOW,TRACK,PULSE}` (no longer used). Note in
      the PR description that those Cloudflare Pages projects/vars can be deleted by the maintainer
      (out-of-repo cleanup — don't assume repo can delete CF projects).
- [ ] **Step 2:** Re-verify no other workflow (ci.yml, directory.yml, perf.yml) references the removed
      demo deploy jobs/vars.

### Task 3: Demo READMEs + specs

**Files:** `apps/examples/{deploy,pay,flow,track,pulse}/README.md` + each demo's Playwright spec

- [ ] **Step 1:** Update each README: the live demo now lives at `https://cascivo.com/demos/<slug>/`
      (same origin as the landing), built with `base: './'`, assembled by `scripts/assemble-demos.mjs`.
      Keep the "mock demo" statement.
- [ ] **Step 2:** Re-verify each Playwright spec still targets the standalone preview at `/` (relative
      base preserves this). If any spec hardcoded an old `.pages.dev` URL, update it. No new flakiness.

### Task 4: Full gate + close

- [ ] **Step 1:** Run the six CLAUDE.md gate commands and fix anything red:
      `pnpm exec vp check` → `pnpm build` → `pnpm exec vp run -r check` → `pnpm test` →
      (`pnpm regen` + `pnpm exec vp check --fix` + `git diff --exit-code`) → `pnpm breakpoint:check`.
- [ ] **Step 2:** Local end-to-end: `pnpm build:landing-demos`, serve `apps/landing/dist`, click from
      homepage `ExamplesGallery` → `/examples` → a detail page → "Open live demo" → `/demos/<slug>/`
      live app. Confirm the whole path is same-origin with no dead links.
- [ ] **Step 3:** Check all eleven DoD boxes in `docs/ROADMAP-V22.md`; flip **Status** to ✅ Shipped.
      Verify each of T1–T5's named files/routes exist as built.

### Exit criteria

- [ ] One CF Pages deploy (landing) ships homepage + `/examples/*` + `/demos/<name>/`×5; the five
      separate demo deploy jobs and `CF_PROJECT_*` vars are removed; the `landing` filter watches the
      demo apps + kit + packages.
- [ ] Demo READMEs + specs reflect the new same-origin live URLs and relative base.
- [ ] Full CLAUDE.md gate exits 0.
- [ ] `ROADMAP-V22.md` DoD complete; status ✅ Shipped.
- [ ] Committed + pushed.
