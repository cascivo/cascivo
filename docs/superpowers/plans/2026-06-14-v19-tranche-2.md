# v19 Tranche 2 — OG image at build time + full favicon/PWA set

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.
>
> Make the social preview and home-screen identity correct on a **cold clone**. Today `public/og.png`
> is only produced by a test that's excluded from CI, and the favicon set is a lone SVG. After this
> tranche, a fresh `git clone` + build yields a working `og:image` and a complete favicon/manifest set.
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - `test/og.spec.ts` renders `/og` at 1200×630 and writes `public/og.png`, tagged `@og`, excluded by
>   `playwright.config.ts` (grep `^(?!.*@og)`). `public/og.png` does NOT exist on a cold clone.
> - `public/` has only `favicon.svg`, `logo.svg`, `logo-mark.svg`. `index.html` links only
>   `favicon.svg` and references `/og.png`.
> - `vite.config.ts` already runs Playwright-style build steps conceptually (it has `closeBundle`
>   plugins). The OG generator (`OgCard` route) is real and renders the card.

**Goal:** `public/og.png` (1200×630) exists after build on a cold clone and is drift-checked; a full
favicon/PWA set ships with correct head tags; `index.html`/T1 og:image tags resolve to a real asset.

**Architecture:** `apps/landing/vite.config.ts` (OG build wiring), `apps/landing/public/` (new
`og.png`, `favicon.ico`, `apple-touch-icon.png`, `site.webmanifest`, PNG icons), `index.html` (head
tags). No package changes.

**Tech stack:** unchanged. The OG generation reuses the existing `/og` render + Playwright; no new
runtime deps.

---

### Task 1: Produce the OG image from the build/regen flow

**Files:** `apps/landing/vite.config.ts` (or `package.json` scripts), `test/og.spec.ts` (keep as
generator), `public/og.png` (output)

- [ ] **Step 1:** Decide the lowest-risk wiring (pick one, document it):
      (a) a `regen`/build script step that boots the preview server and runs the `@og` Playwright
      spec to write `public/og.png`, committed and drift-checked; or
      (b) a `closeBundle` step in `vite.config.ts` that renders `/og` headlessly to `dist/og.png`.
      Prefer (a) — it reuses `og.spec.ts` verbatim and keeps the asset version-controlled so cold
      clones and social crawlers always have it.
- [ ] **Step 2:** Generate `public/og.png` once via the chosen path; confirm it is exactly 1200×630.
- [ ] **Step 3:** Ensure the asset is committed and the CLAUDE.md drift check (`pnpm regen` →
      `git diff --exit-code`) regenerates an identical image (deterministic render — fixed viewport,
      `waitForTimeout`, no animation). If the render is non-deterministic, disable animation/reveal on
      `/og` so the bytes are stable.

### Task 2: Build the favicon / PWA asset set

**Files:** `apps/landing/public/` (new assets)

- [ ] **Step 1:** Generate from the existing `favicon.svg` brand mark: `favicon.ico` (multi-size
      16/32/48), `apple-touch-icon.png` (180×180), `icon-192.png`, `icon-512.png`. Use the brand
      gradient/`--cascivo-brand-primary` look so they match `favicon.svg`. Keep 1:1 aspect, square.
- [ ] **Step 2:** Create `public/site.webmanifest`: `name`/`short_name` "cascivo", `icons` (192 + 512
      PNG, `type: image/png`, `purpose: any`), `theme_color` = brand primary hex, `background_color`,
      `display: standalone`, `start_url: /`.

### Task 3: Wire the head tags

**Files:** `index.html`

- [ ] **Step 1:** Add head links: `apple-touch-icon` (180), `icon` 32×32 PNG, `icon` 16×16 PNG (or rely
      on `favicon.ico` for legacy), and `<link rel="manifest" href="/site.webmanifest" />`. Keep the
      existing `favicon.svg` link first (modern browsers prefer it).
- [ ] **Step 2:** Confirm `og:image`/`twitter:image` (from T1) point to `/og.png` and that the asset
      now exists. Add nothing duplicate.

### Task 4: Verify on a cold checkout + commit

- [ ] **Step 1:** In a clean worktree (or after `git stash`), run the build/regen path and confirm
      `public/og.png` and all favicon assets exist in `dist/`.
- [ ] **Step 2:** Spot-check the social preview by loading the built `index.html` and confirming the
      `og:image` resolves (no 404). Optionally validate dimensions with `file public/og.png`.
- [ ] **Step 3: Commit** — `fix(landing): og.png from build + full favicon/PWA set (v19-t2)`

---

## Tranche exit criteria

1. A cold `git clone` + build produces `public/og.png` at 1200×630; `pnpm regen` + `git diff` is clean
   (deterministic).
2. `public/` contains `favicon.svg`, `favicon.ico`, `apple-touch-icon.png`, `icon-192.png`,
   `icon-512.png`, `site.webmanifest`.
3. `index.html` links the manifest + apple-touch-icon + favicons; `og:image`/`twitter:image` resolve.
4. No new runtime dependencies; OG generation reuses the existing `/og` route + Playwright.
