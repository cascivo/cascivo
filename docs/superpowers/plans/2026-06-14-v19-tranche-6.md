# v19 Tranche 6 — Performance / code-splitting

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or
> superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.
>
> The whole app is one eager bundle: `App.tsx` imports every home section (including heavy
> `ChartShowcase` from `@cascivo/charts` and `RelayConsole` with DataTable/Modal/Toast) plus all four
> subpages up-front. Split routes and defer heavy below-the-fold home sections so the initial home
> payload shrinks — without hurting LCP (hero stays eager) or CLS (no late layout shift).
>
> Current-state assumptions — RE-VERIFY before starting:
>
> - `App.tsx` statically imports HomePage sections and the subpage components; no `React.lazy` anywhere.
> - Heavy home sections: `ChartShowcase` (charts), `RelayConsole` (many components + demo regions).
> - LCP element is the hero (`Hero.tsx`, top of home) — must stay eager and not be lazy-loaded.
> - CWV targets (2026): LCP < 2.5s, INP < 200ms, CLS < 0.1; mobile is worse — optimize mobile first.

**Goal:** Non-home routes and heavy below-the-fold home sections are code-split behind `React.lazy` +
`Suspense`; the initial home JS bundle is measurably smaller than baseline; LCP hero stays eager and no
new layout shift is introduced.

**Architecture:** `App.tsx` (lazy routes + Suspense), home section lazy boundaries (around
`ChartShowcase`, `RelayConsole`, and any other heavy below-the-fold section), `landing.css` (reserved
space / skeleton for lazy boundaries to avoid CLS). No package changes.

**Tech stack:** unchanged. `React.lazy`/`Suspense` are **permitted** (not banned hooks). Do NOT add
`useState`/`useEffect`. Keep signal rules in any touched component.

---

### Task 1: Baseline the bundle

- [ ] **Step 1:** Build and record the current home entry/chunk sizes (`vp build` output or
      `dist/assets/*.js` sizes). Note the largest contributors (expect charts + relay demo). This is the
      before number for the DoD "measurably smaller" claim.

### Task 2: Route-level code-splitting

**Files:** `apps/landing/src/App.tsx`

- [ ] **Step 1:** Convert `AccessibilityPage`, `PerformancePage`, `GuidesPage`, `OgCard`, and the new
      `NotFound` (T3) to `React.lazy(() => import(...))`. Wrap the route render in `<Suspense>` with a
      minimal fallback (a centered spinner or empty `main` of reserved height — token-based).
- [ ] **Step 2:** Keep HomePage **eager** (it's the default/LCP route). Confirm `document.title` /
      per-route head (T1) still applies before the lazy chunk resolves.

### Task 3: Defer heavy below-the-fold home sections

**Files:** `App.tsx` / HomePage composition, `landing.css`

- [ ] **Step 1:** Wrap `ChartShowcase` and `RelayConsole` (and any other heavy, below-the-fold section)
      in `React.lazy` + `Suspense`. The hero and above-the-fold content stay eager.
- [ ] **Step 2:** Reserve layout height for each lazy section (min-height skeleton in `landing.css`,
      token-based) so the deferred chunk loading does not cause CLS. Verify visually at mobile widths.
- [ ] **Step 3:** Do NOT lazy-load the hero or anything in the first viewport (protects LCP).

### Task 4: Asset/LCP/font hygiene

- [ ] **Step 1:** Confirm fonts are not render-blocking (the token layer; no external `@font-face`
      blocking paint). If a webfont is fetched, ensure `font-display: swap` or system-font fallback.
- [ ] **Step 2:** Confirm the LCP element (hero text/gradient) needs no preload beyond default; if a
      hero asset exists, `<link rel="preload">` it (research: don't lazy-load LCP).

### Task 5: Measure + commit

- [ ] **Step 1:** Rebuild; record the new home entry/chunk sizes; confirm a measurable reduction vs.
      Task 1 baseline (charts/relay now in separate chunks loaded on demand).
- [ ] **Step 2:** Re-run `pnpm exec playwright test apps/landing/test/mobile.spec.ts` and
      `a11y.spec.ts` — no overflow/axe regressions from the Suspense fallbacks.
- [ ] **Step 3: Commit** — `perf(landing): route + heavy-section code-splitting (v19-t6)` (include
      before/after bundle sizes in the message).

---

## Tranche exit criteria

1. `/accessibility`, `/performance`, `/guides`, `/og`, `NotFound` are `React.lazy` + `Suspense`; home
   stays eager.
2. `ChartShowcase` and `RelayConsole` load in deferred chunks; the hero/above-the-fold stays eager.
3. Initial home JS is measurably smaller than the recorded baseline (numbers in the commit message).
4. No new CLS from lazy boundaries (reserved height); mobile overflow + axe still pass.
5. No `useState`/`useEffect`/`useContext` introduced; signal rules intact in touched components.
