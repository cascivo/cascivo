# v50 — Tranche 2: Drawer/Scrim Breakpoint Reconciliation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the breakpoint drift v49 left behind. v49 T6 moved the nav-collapse boundary to `40rem` (the
hamburger toggle shows, the inline nav hides, and the JS `isMobileNav` flips at `39.99rem`/`40rem`), but the
`.mobile-nav-drawer` / `.nav-scrim` still hide at the old `48rem`. Move that hide cutoff to `40rem` so the whole
mobile-nav system switches at one boundary. Lens: **architect.**

**Architecture:** `landing.css:2346` `@media (min-width: 48rem) { .mobile-nav-drawer { display: none } .nav-scrim {
display: none } }`. v49 set the toggle show + `[role='banner'] nav` hide at `@media (max-width: 39.99rem)`
(`landing.css:3545`) and `isMobileNav = useMediaQuery('(max-width: 39.99rem)')` (`Header.tsx`). Functionally the
drawer cannot open in the 40–48rem band today (the toggle isn't wired there), so this is a dead-rule/consistency
fix with no visible behavior change — but it removes a trap for the next editor.

**Tech Stack:** `landing.css` only. No markup/JS change.

---

## Files

- Modify: `apps/site/src/marketing/landing.css` (the drawer/scrim hide `@media` → `40rem`)

---

### Task 1: Move the drawer/scrim hide to 40rem (M-A2)

**Files:** `apps/site/src/marketing/landing.css`

- [ ] **Step 1:** Change `@media (min-width: 48rem)` at `:2346` (the block that sets `.mobile-nav-drawer { display:
      none }` and `.nav-scrim { display: none }`) to `@media (min-width: 40rem)` — the canonical `md`, matching the
      v49 nav-collapse boundary. Above `40rem` the inline nav shows and the drawer/scrim are hidden; below it the
      hamburger + drawer are the nav. One boundary for the whole system.
- [ ] **Step 2:** Confirm this `48rem` is the **only** one tied to the mobile nav. Leave the unrelated `48rem`/
      `47.99rem` literals alone — `landing.css:2192` (`.console-body`, the Relay console demo) and the `app.css`
      DocsApp shell are a separate scale-pass (out of scope here, noted in the roadmap).

### Task 2: Verify + commit T2

**Files:** —

- [ ] **Step 1:** `pnpm exec vp check` clean; `pnpm exec vp run @cascivo/site#build` succeeds.
- [ ] **Step 2:** Resize across `40rem`: at/just-above 40rem the inline nav shows and no drawer/scrim linger in the
      layout; just-below 40rem the hamburger shows and the drawer opens correctly (below the navbar, per T1). No dead
      band where the toggle is hidden but the drawer rules still apply.
- [ ] **Step 3: Stage and commit**

  ```bash
  git add apps/site/src/marketing/landing.css
  git commit -m "$(cat <<'EOF'
  fix(site): hide the mobile drawer/scrim at the same 40rem the nav collapses (v50 T2)

  v49 T6 moved the nav-collapse boundary (toggle, inline-nav, JS isMobileNav) to
  40rem but left the .mobile-nav-drawer/.nav-scrim hide at the old 48rem. Move it
  to 40rem so the whole mobile-nav system switches at one canonical boundary.
  No visible behavior change (the toggle isn't wired in the 40-48rem band), but it
  removes the stale-literal trap.

  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_016BqjTpvLoeWYn25B7fqT2K
  EOF
  )"
  ```

**Definition of done:** The drawer/scrim hide at `40rem`, matching the toggle/inline-nav/JS boundary; the only mobile
-nav breakpoint is `40rem`; unrelated `48rem`/`47.99rem` literals untouched; `vp check` + build green.
</content>
